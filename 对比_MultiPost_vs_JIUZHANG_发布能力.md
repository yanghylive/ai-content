# MultiPost-Extension vs JIUZHANG AI 发布能力对比 & 提升方案

> 日期：2026-08-01
> 目的：看清差距，明确怎么用 MultiPost 提升我们的发布中心

## 一、核心对比表

| 维度 | JIUZHANG AI（现状） | MultiPost-Extension | 差距/启示 |
|---|---|---|---|
| **执行机制** | Playwright + CDP 连真实浏览器（`chromium.connectOverCDP`）；微信另走官方 API | `chrome.scripting.executeScript` 注入已登录页面 | 我们机制更重（要起浏览器/CDP），它更轻（注入即可）。但它依赖装扩展 |
| **平台数量** | **5 个浏览器平台**：小红书/视频号/抖音/快手/B站；另有微信公众号官方 API | **84 个** | 量级差距最大的一项 |
| **公众号发布** | 走**官方 API**（api.weixin.qq.com draft/add + freepublish/submit）——需服务号+access_token | 走**后台 cgi-bin**（mp.weixin.qq.com，提页面 token）——订阅号也能用 | **我们走官方 API 更正规但门槛高**（要认证服务号）；它走后台接口覆盖订阅号。两条路都可留 |
| **网页→本地通道** | 无通用协议，前端直连后端 3011 | 完整 postMessage 协议 + 18 个 action + 域名白名单授权 | **我们缺这层**——3010 网页没法安全指挥本地执行器 |
| **授权 UX** | 设备授权流（userCode→云端登录→poll） | 首次弹窗点"信任"→域名白名单 | 我们授权为"设备接入"设计；它为"网页调本地"设计，更轻 |
| **文章采集** | 有 sources/auto-acquisition 模块 | scraper/ 从文章页反抓（处理懒加载图/防盗链/代码行号） | 各有采集，它的**页内反抓**（已打开的文章页直接提）我们可补 |
| **图片预处理** | — | publish.tsx 把远程图抓下转 blob 再注入 | 防图片防盗链/跨域，我们发内容必踩 |
| **发布中枢** | 后端 publishing.service 串行调 | 独立 tab 中枢：预处理→开多平台 tab→编组→倒计时关 | 它"一次编辑多发"的编排 UX 更成熟 |
| **账号识别** | auto-upload 账号表（DB 存 appId/apiToken/config） | 从已登录页面抓 username/avatar | 我们存凭证（能真发）；它读登录态（只展示） |
| **稳定性** | 官方 API（微信）稳；DOM 自动化（CDP）受平台改版影响 | 全 DOM 模拟，平台改版即断 | **我们微信最稳**；其他平台和它一样脆 |
| **架构模式** | 平台逻辑散在 auto-upload.client 一个大文件（4600+ 行） | 注册表模式：每平台一文件 + PlatformInfo map | **它的可扩展性完胜**——新平台加一文件，我们得改大文件 |

## 二、我们比它强的地方（别妄自菲薄）

1. **微信走官方 API**——它没这条，只敢走后台接口（灰色）。我们的微信发布合规、稳定、有发布记录回读（readback 校验）
2. **有凭证体系**（CredentialEnvelope 加密存 appId/apiToken）——它只读登录态，不存凭证
3. **发布记录 + 回读校验**（publish-record store，evidence/readback）——企业级审计它没有
4. **后端编排**（多租户/计费/合规已集成）——它是纯前端扩展，无后端

## 三、怎么用 MultiPost 提升我们（按 ROI 排序）

### P0：补"网页→本地执行器"协议（我们最缺）
- 照它的 postMessage + 域名白名单 + 18 action 框架，做 JIUZHANG 的本地伴侣通道
- 让 3010 网页能安全调起本地浏览器/微信发内容、读账号、刷新登录态
- **收益**：打通"网页控制台 → 本地执行"这条我们一直绕的路

### P1：发布函数注册表化
- 把 auto-upload.client.ts（4600 行单文件）里各平台逻辑拆成"每平台一文件 + 注册表"
- 新平台 = 加文件注册一行，不动主干
- **收益**：扩平台成本从"改大文件"降到"加小文件"

### P2：公众号订阅号覆盖
- 我们官方 API 只覆盖认证服务号；参考它 cgi-bin 路径补订阅号的后台发布
- 保留官方 API 为主，后台接口为兜底
- **收益**：公众号覆盖率从"服务号"扩到"订阅号"

### P3：浏览器平台数量 5 → 头部 8-10
- 从它 84 个里挑我们缺的（微博/知乎/头条）；视频号/快手/B站已有则参考其实现做加固
- 优先独立实现；若复制或修改 Apache-2.0 代码，按许可证要求保留归属和变更说明
- **收益**：发布中心卖点直接翻倍

### P4：文章页内反抓（采集增强）
- 参考 scraper/，已打开的文章页一键提取成可编辑稿
- **收益**："看别人的文→一键改写→多发"闭环

### P5：图片预处理（远程图→blob）
- 参考 publish.tsx，发内容前把远程图全抓下转本地
- **收益**：解决图片防盗链导致的发布图裂

## 四、一句话总结

**我们的底子（官方 API + 凭证 + 审计 + 后端编排）比它扎实，它赢在"轻、平台多、网页↔本地通道顺"。**
**最优解 = 保留我们的后端和合规底座，独立实现同类"通信协议 + 注册表"，并在 Apache-2.0 合规前提下选择性参考其平台适配代码。** 不是二选一，是拿它的前端灵活性补我们的前端短板。
