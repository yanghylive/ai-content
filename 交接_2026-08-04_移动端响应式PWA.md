# 移动端（响应式 H5 / PWA）交接文档

> 版本：2026-08-04 · 分支：`codex/content-workspace-20260721`
> 对应 PRD：`3010系统安卓手机端完整PRD.md`
> 风格基线：明德 VP 商会（深蓝 `#17325b` × 金色 `#e39a3e→#f6c478` 玻璃拟态）

---

## 1. 一句话

JIUZHANG AI 前端已支持 **<768px 手机壳**：底部 5 Tab 导航 + 全部 P0 页面移动视图，桌面端零影响（`≥768px` 自动回退原布局）。

## 2. 核心架构

```
前端路由（共用，无 /m 前缀）
  └─ AppShell（桌面壳：左侧 rail + topbar）
       └─ useIsMobile() 断点（<768px）
            └─ MobileShell（底部 5 Tab：今天/内容/发布/消息/我的）
                 └─ 各页面组件内 useIsMobile 分支
                      └─ MobileXXXView（移动视图，复用桌面同一批数据/handlers）
```

**关键文件**：
| 文件 | 作用 |
|---|---|
| `frontend/src/lib/hooks/use-media-query.ts` | `useIsMobile()` / `useMediaQuery()` / `MOBILE_QUERY` |
| `frontend/src/components/shell/mobile-shell.tsx` | 底部 5 Tab 导航 + 角标（今天/发布/消息） |
| `frontend/src/components/shell/mobile.css` | 明德 VP 样式体系：`mx-card` `mx-hero` `mx-control` `mx-svc-grid` `mx-stat-grid` `mx-tabbar` `mx-skeleton` `mx-badge-*` 等 |
| `frontend/public/manifest.json` | PWA manifest（start_url=/today） |
| `frontend/public/sw.js` | Service Worker（只缓存壳+静态，API 永不缓存） |

## 3. 移动页面清单（全部完成）

### 一级 Tab（5）
| 路由 | 移动视图 | 数据 |
|---|---|---|
| `/today` | MobileTodayView | todos/leadCount/publishedToday/materialCount/waitingCount/doneItems |
| `/content` | MobileContentView | 6 宫格入口 + 最近草稿（articlesApi）+ 素材 hero |
| `/distribution` | MobilePublishView | taskPage 真实任务 + 状态筛选 chips |
| `/message` | MobileMessageView | confirmations 真实待确认 + 渠道宫格 |
| `/mine` | MobileMineView | 资料/套餐/功能列表 + PWA 安装入口 |

### 二级页（12）
| 路由 | 移动视图 |
|---|---|
| `/topics` `/topics-v2` | 选题列表 + 全屏详情（五维评分/发布操作） |
| `/topics/new` | 新建选题表单 |
| `/content/articles` | 文章列表（状态徽标） |
| `/materials` `/materials-v2` | 素材库（hero 统计+采集+搜索+列表） |
| `/distribution-v2/scrape` | 文章反抓（hero 输入+结果卡片） |
| `/distribution-v2/tasks` | 发布任务（统计+筛选+失败重试/删除） |
| `/tasks/confirmations` | 待确认（回复确认+高风险确认双按钮） |
| `/crm` | 客户列表（统计+快捷宫格） |
| `/crm/detail` | 客户详情（名片/联系方式/来源/互动/标签） |
| `/platforms` | 平台账号只读列表（状态点+徽标） |
| `/settings` | 设置（资料/密码/通知/数据） |
| `/distribution-v2/articles` | 待发布文章（+去发布） |

## 4. 开发模式（团队新页面照抄）

```tsx
// 页面组件内（useIsMobile 必须在所有 early return 之前！）
const isMobile = useIsMobile();
if (isMobile) {
  return <MobileXXXView {...同一批 state} />;
}
// 桌面布局保持不变
```

- **复用数据**：移动视图直接读桌面组件已有的 state/hooks，零重复请求
- **详情弹窗**：桌面 `fixed inset-0` 弹窗天然全屏，移动端直接复用
- **样式**：只用 `mx-*` 类（mobile.css），不引 Tailwind 新类

## 5. 质量门（每个改动必跑）

```bash
cd frontend
env -u NODE_OPTIONS npx tsc --noEmit      # 0 错
npm run lint:strict                        # 0 warning
node scripts/commercial-copy-guard.mjs     # 文案守卫（避免工程语言泄漏）
```

**⚠️ 踩过的坑**：
1. `useIsMobile` 放 early return 后面 → lint 报 `react-hooks/rules-of-hooks`（conditional hook）
2. 用户可见文案含「真实发布/真实执行/调试/API/后端」→ commercial-copy guard 拦截
3. `manifest.ts`（Next 动态路由）在 `output: export` 下 500 → 用 `public/manifest.json` 静态文件
4. Next `output: export` 下 SW 仅生产 HTTPS 注册（localhost 不注册）

## 6. 验证方法

```bash
# Playwright mock 登录态（dev server 需在跑）
# 390px 视口验证移动壳，1280px 验证回退桌面
# 见历史脚本模式：route mock /api/auth/me + /api/auth/tenants
```

## 7. 后续待办（按 PRD 优先级）

| 项 | 说明 |
|---|---|
| publish-flow 5 步发布向导移动化 | 800 行多步表单，需专门移动设计 |
| ChannelConsole 消息台移动化 | 574 行工作台式布局（会话/私信台） |
| Web Push 通知（MOB-PUSH-001） | P1 |
| 弱网草稿保护（MOB-OFFLINE-001） | P1 |
| 系统分享接收（MOB-SHARE-001） | P1 |
| 真机部署 | 前端静态导出 + HTTPS 域名 + 云端 API（PRD Phase 0） |
