# 商用发布阻断项处置报告（2026-08-24 第二轮收口）

> 触发：大王判定「源码合入 ≠ 商用发布完成」，列出 10 项阻断 + 8 项人工验收。
> 本轮（commit 见文末）处理代码层可立即修的项；外部资源/架构决策项明确标注归属。

---

## 结论一句话

**代码层阻断项 #2/#6/#8/#9/#10 已修；#3 已补声明；#4 量化定位（历史债非本轮引入）；#1/#5/#7 归外部资源/架构决策，仍需大王拍板。**

---

## 逐项处置

### ✅ #2 真实 Octop Agent Gateway 默认未启用 —— 已修

- `agent-gateway.service.ts`：`OCTOP_ENABLED === 'true'` → **`OCTOP_ENABLED === 'false' ? undefined : new RealOctopAdapter()`**
  - 默认启用真实适配器（其内部自带降级：healthy 探活 / 503 降级 token-only / 无凭据回退能力探测）
  - 仅显式 `=false` 才退回 Mock（测试环境）
- `desktop/backend.env.example`：`OCTOP_ENABLED=false` → **`true`**，并补注释

### ✅ #6 用户级隔离未成打包默认能力 —— 已修（代码 + 配置说明）

- `desktop/backend.env.example` 补 `OCTOP_USER_SECRET` / `OCTOP_IDENTITY_MODE` / `OCTOP_ADMIN_USERNAME` / `OCTOP_ADMIN_PASSWORD` 完整说明段
- `frontend/src/hooks/use-octop-launch.ts`：解构后端返回的 `isolated` 字段，`isolated === false`（回退共享账号）时 toast 告警「多用户部署下浏览器会话将跨用户共享」
- 注：**实际填入 `OCTOP_USER_SECRET` 等值属于配置资源，归大王**（多用户商用必须填，单机单用户可留空）

### ✅ #8 会话属主只存内存 —— 已修

- `OctopAdapter` 接口 `tokenExchange(octopSessionId, kaypalUserId?)` / `cancelRun(octopSessionId, reason?, kaypalUserId?)` 增加可选 `kaypalUserId`
- 网关从**持久化**的 session 记录传入 `session.userId` / `task.userId`（persist 模式下经 Prisma mirror + hydrator 重启恢复）
- `real-octop-adapter.ts` 优先用传入 userId，`sessionOwners` map 降级为 fallback（不再承担唯一属主来源）
- 后端重启后，tokenExchange/cancel 靠持久化 session 的 userId 兜底，不再因内存 map 清空而失败

### ✅ #9 业务模式也调 Octop —— 已修

- `agent-gateway.ts createSession`：`if (mode === 'advanced')` 才调 `octop.createSession`
- business 模式不再拉起 Chromium / 增加延迟与资源消耗
- 配套修正 `security-hardening.spec.ts`（原测试靠旧 bug 侥幸通过，现显式 `advanced` 模式）

### ✅ #10 能力接口返回过期状态 —— 已修

- 网关新增 `refreshCapabilities()`：先 `await octop.healthy()` 再 `getCapabilities()`
- `agent-gateway.controller.ts capabilities()` 改 async，调 `refreshCapabilities()`

### 🟡 #3 Octop 未打包/未自动启动 —— 已补声明，待架构决策

- `packager.json` 增 `externalDependencies: ["octop"]` 机器可读声明（`sidecars: []` 保持不变）
- **决策记录**：2026-08-24 Stage 1-3 大王已拍板「连接本机已运行 Octop，不打包 sidecar」（memory 有据）
- **待大王重新拍板**：商用一键安装是否需要把 Octop 打进 sidecar + main.js 自动启动 8088？若否，则需在安装说明里正式声明「Octop 为外部依赖，需用户自行安装启动」

### 🟡 #4 完整 lint 失败 —— 已定位，历史债非本轮引入

- 全仓库 525 problems 中 **97% 集中在 agent-gateway 模块（512 errors）**
- 分类：366 格式类（prettier，可 `--fix`）+ 146 语义类（`only-throw-error` 56 / `no-base-to-string` 37 / `require-await` 23 / `no-unsafe-*` 17 / 其余 13）
- **踩坑**：`eslint --fix` 全模块会触发 `no-unnecessary-type-assertion` **误删必要断言**（`prisma-mirror.ts` 的 `as object`、`auth.ts` 的联合收窄），引入 3 个 tsc 编译错误 → 已回滚
- **本轮保证**：我改动的文件（octop-identity / real-octop-adapter / kaypal-octop-bridge / octop-launch.controller）ESLint 0 error，tsc 0 error
- **结论**：146 语义类 + 366 格式类是**独立历史债清理批次**，需专门策略（`--fix` 时禁用 `no-unnecessary-type-assertion`，语义类逐处人工判断——尤其 56 处 `only-throw-error` 涉及 `throw 'CODE'` 与 catch string 匹配的联动改动，有回归风险），不宜混入本轮商用验收收口

### ⏳ #1 Win 安装包旧产物 —— 待云电脑（外部资源）

- Mac 1.1.96 已重打包 + `check-package-contents` 全绿（含 workspace-tabs.js + sharp-win32-x64）
- Win 1.1.96 仍是 8/22 旧产物，缺 `workspace-tabs.js` / `tab-strip.html` / `tab-strip-preload.js`
- **归大王**：云电脑 `ecd-5gk1odk27jnz1pdol` 跑 `desktop/scripts/build-win-full.js`

### ⏳ #5 完整后端测试未全绿 —— 待测试账号（外部资源）

- 仅 `e2e-full-chain.spec.ts` 4 个测试失败，原因是缺 `KAYPAL_TEST_PHONE` / `KAYPAL_TEST_PASSWORD`（测试显式禁止静默通过）
- 其余 137 passed（含 agent-gateway 34/34 目标 + octop-identity 18/18 + security-hardening 27/27）
- **归大王**：注入隔离的商用测试账号凭据后重跑

### ⏳ #7 Octop token 暴露给 Electron Renderer —— 需架构决策（P0 安全）

- 现状：后端返回 Bearer token → 3010 前端 → IPC → workspace-tabs → `localStorage.auth_token`（Octop SPA 只认 localStorage，见真机验证结论）
- 已做的缓解：loopback URL 白名单、token 不落盘（persist 过滤 octop 标签）、IPC sender 校验
- **根本矛盾**：Octop 前端自己用 `localStorage.auth_token` 判定登录态，token 必然要进 Octop SPA 的 JS 上下文。彻底解法需**改 Octop 前端**或**主进程侧会话交换**（主进程持 token，renderer 只发「打开」IPC，token 不经 3010 前端）
- **归大王决策**：一次性票据 / 主进程 HttpOnly 交换 / 接受当前 loopback+白名单方案（单机桌面）？这是独立安全批次

---

## 人工验收 8 项（未做，归大王/自动化）

| 项 | 状态 |
|---|---|
| 业务标签固定存在 | 待 GUI |
| 高级模式新增独立 Octop 标签 | 待 GUI |
| Octop 真正免登录 | 待 GUI（真机验证脚本已沉淀 `electron-gui-cdp-verify` skill） |
| Network 只向 8088 发 Authorization | 待 GUI（CDP echo 服务器可自动判定） |
| 关闭/重建标签行为 | 待 GUI |
| popup/OAuth 被 setWindowOpenHandler 拦截 | 待 GUI |
| Win 一键安装（含 Octop） | 依赖 #1 + #3 决策 |
| 商用一键链路端到端 | 依赖 #5 测试账号 |

---

## 本轮 commit 文件

```
backend/src/modules/agent-gateway/adapters/octop-mock.ts        #8 接口加 kaypalUserId
backend/src/modules/agent-gateway/adapters/real-octop-adapter.ts #8 kaypalUserId 优先
backend/src/modules/agent-gateway/agent-gateway.controller.ts    #10 async capabilities
backend/src/modules/agent-gateway/agent-gateway.service.ts       #2 默认启用 RealOctopAdapter
backend/src/modules/agent-gateway/core/agent-gateway.ts          #9 advanced-only + #8 传 userId + #10 refreshCapabilities
backend/src/modules/agent-gateway/security-hardening.spec.ts     #9 测试修正
desktop/backend.env.example                                      #2 默认值 + #6 env 说明
desktop/packager.json                                            #3 externalDependencies 声明
frontend/src/hooks/use-octop-launch.ts                           #6 isolated 告警
```

---

## 验证证据

- backend `npx tsc --noEmit` = 0 error
- frontend `npx tsc --noEmit` = 0 error
- jest agent-gateway：137 passed / 4 failed（缺凭据门禁）/ 1 skipped
- octop-identity.spec：18/18；security-hardening.spec：27/27；real-octop-adapter.spec / kaypal-octop-bridge.spec：pass

## 待大王拍板清单（按优先级）

1. **#7 token 暴露**：一次性票据 / 主进程 HttpOnly 交换 / 接受 loopback+白名单（单机桌面）？
2. **#3 Octop 打包**：商用一键安装是否把 Octop 打进 sidecar + main.js 自动启动？
3. **#1 Win 重打包**：授权/由大王在云电脑跑 `build-win-full.js`
4. **#5 测试账号**：注入 `KAYPAL_TEST_PHONE/PASSWORD` 后重跑 e2e-full-chain
5. **#6 配置**：多用户商用填 `OCTOP_USER_SECRET` 等（单机可留空）
6. **#4 lint 历史债**：是否单开「lint 债务清理批次」（需禁用 `no-unnecessary-type-assertion` 的 --fix + 逐处手工判语义类）