# 2026-09-01 工作日志

## 用户体验架构梳理方案（JIUZHANG AI / 3010）

- 大王点名「用户体验架构师」，要求重新梳理产品架构、消除体验断点/重复环节/混乱层级，先出方案不动代码。
- 已产出方案文档：`artifacts/用户体验架构梳理方案-2026-09-01.md`，含 2 张架构图（inline widget）。

### 核心诊断结论（已确认的实锤）
- 主旅程 3 断点：情报→选题、发布→互动、互动→客户 均无打通，靠手动搬运。
- 顶层目录 60+ 个碎片化：内容创作 10 目录、视频 7 目录、CRM 7 目录、智能体/执行 9 目录。
- 双轨镜像：`admin/` 与 `capabilities/` 几乎完全重复（models/memory/plugins/sandbox/users/executor/account/risk 等）。
- 新旧并存：`intelligence/monitor-new` vs `monitors`、`report-new` vs `reports`、`page-legacy` 39 页。
- 职责串味：`intelligence/` 里混着 `leads`（属客户）、`costs`（属我的用量）。

### 目标架构
- 一条主线飞轮 5 站：发现 → 创作 → 发布 → 获客 → 客户。
- 两个支撑面：执行与监控、设置与治理。
- 底座红线（只读不动）：用户/订阅/积分/治理。

### 关键设计原则（消除断点的本质）
- 「把下一步内嵌到当前环节的产物卡片上」：情报条目加「转选题」、已发内容挂「互动回流」、互动线索加「转客户」，而非让用户去侧边栏找下一个模块。

### 待办/风险
- 一期先做：admin↔capabilities 合并 + 3 处断点打通（纯导航+跳转，不动后端）。
- 需建「旧路径→新路径」映射表 + 全局 301，否则上线大量 404。
- 二级页面完整归属需逐页盘点后固化（本方案给的是骨架）。
# 2026-09-01

## P0 首批修复：加载失败不再静默（76b15974 + 4138315c，大王"1开修"）
- 新增 frontend/src/components/load-error-banner.tsx：LoadErrorBanner（heroui Button + border-danger-200 bg-danger-50 项目既有样式）+ useLoadError（report/clear），文案 toPublicError 兜底。
- 16 文件 17 点接入：home/growth/platforms/models/apps/auto-acquisition/crm/wechat-task/engine-health/materials×2/schedules/strategies/styles/templates/topics/channel-console。engine-health-center 复用既有 checkFailed 卡片接原因（loadError ?? 原文案，两处分支）；channel-console 写进既有 error 展示位。成功路径 clearLoadError，retry 直调原加载函数，console.error 保留。
- 验证：frontend tsc 0、eslint 17 文件 0。审计报告 §六第 1 项已勾选。
- 交底：①git add frontend/docs 误把工作区原有无关文件 ai-employee-design-system-apply-plan.md 带进提交，已 4138315c 撤出跟踪（教训：提交前 git status 逐文件核对，别整目录 add）；②一次脚本引号笔误 SyntaxError 空跑重写；③真机断后端负向验证待云电脑 E2E。
- 剩余批次：P1 materials 操作 3 处 + ops 轮询 4 处断流提示；112 文件 toActionableError 分批替换；release-guard 挂"catch 只 console"检查。

## Codex 复核打回 R1 + 同类自查修复（7c14688d，凌晨 01:40）
- Codex 打回 4 条全部成立：Promise.allSettled 的 rejected 不进 catch——auto-acquisition/wechat-task 失败降级成 0 还无条件 clearLoadError = 假成功（P0）；engine-health partial failure 原因不出现（P1）；channel-console 成功后错误残留且 error 是多操作共享状态不能直接清（P1）。
- 举一反三自查 5 处同款：crm-center(P0 同款)/settings-integrations/engine-run-detail/publish-flow/leads-detail/effects 全部零 rejected 处理，一并修；effects 的 catch 还把"加载失败"写进绿色成功样式 msg，加 errMsg 独立 danger 状态修正。
- 修法统一：rejected 守卫（filter PromiseRejectedResult）→ reportLoadError/onError/检查项上屏（全败/部分败文案区分），全 fulfilled 才 clear。
- 踩坑交底：tsc 抓出 3 个漏 import + 1 个 exhaustive-deps warning（脚本批量改的代价），已修。
- 教训固化：①allSettled 的 rejected 永远不进 catch，成功判定必须显式检查，"catch 里有处理"≠"失败有上屏"；②静态门禁全绿 ≠ 分支覆盖，验证须跑分支级负向用例（断单接口）——建议给 16 页 banner 加 jest/断接口 E2E。

## Codex 复核清单 R1 产出（01:57）
- docs/codex-复核清单-20260901-前端报错透明度R1.md：待复核 7c14688d + b0efcb60，A-G 逐项 file:line（b0efcb60 实测行号）+ 门禁命令（含分支覆盖扫描：全仓 allSettled 11 文件，9 个有 PromiseRejectedResult 守卫，2 个例外已有自身处理）+ P0/P1/P2 判定 + 5 条局限交底（部分败降级渲染设计、分支级负向测试未建挂账等）。
- 写清单前自查补刀：channel-console loadAccounts 也是静默 catch { setAccounts([]) }，先修（b0efcb60）再写清单，防 Codex 下轮再打。

## 用户体验架构方案 v2 修订（大王源码复核打回）
- 大王对 UX 架构方案 v1 做源码级复核，列 9+1 处事实错误，我逐条读码核实后**全部认**，v1 现状诊断撤回重写为 v2（同文件覆盖）。
- 核心错误：v1 把「体验入口缺失」误写成「后端完全没打通」。
  - 情报→选题已打通：`topicId`(intelligence.ts:37) + 收件箱「生成选题」按钮(inbox-workbench.tsx:337) + `generateTopicFromItem` 直接 `topic.create` 无幂等(import.service.ts:72)。
  - 发布→互动归因链已存在：`saveInteractionChain`(attribution-event.store.ts:114) + 统一收件箱聚合(interaction-inbox.service.ts:110) + 「内容来源」展示(unified-inbox.tsx:469)。
  - 互动→客户已存在：互动→Growth Lead→CRM Customer（转线索 dispatch-records-page.tsx:244 + syncLeadToCrm growth.ts:594）。
- 其他纠正：一级场景实际 7 个（app-shell.tsx:27，非 60+ 平铺）；monitor-new/report-new 是「新建表单」非旧版；page-legacy 已删(test.mjs:90)；admin/capabilities 已用 `routeAliases` 收口(route-config.ts:11，admin/account 直接 re-export)；`output:"export"`(next.config.ts:25) 静态导出无 HTTP 301。
- 补充遗漏：`/message`（rail 一级互动）与 `/engagement`（独立统一收件箱）canonical 关系未厘清。
- v2 改动分 A 类（纯导航/入口）与 B 类（后端幂等/查询参数/状态机），验收分 A/B 两口径，B 类三条旅程验收（唯一选题/反查互动无归因/互动→线索→CRM 阻塞原因可见）。

### 教训（跨项目通用，建议入 MEMORY）
- 出 UX/架构方案时，涉及「是否已实现」的判断必须读真实源码确认（字段/API/按钮/调用链），不能凭目录名、文档或印象推断。
- 物理目录数 ≠ 用户导航复杂度：先看一级导航（rail/SCENES）再谈层级。
- 静态导出（output:export）无 HTTP 301，重定向靠客户端 router.replace，验收要查「查询参数是否保留」而非 301。

## A 类 canonical 路由映射表（盘点终版）
- 产出 `artifacts/canonical路由映射表-2026-09-01.md`，盘点 240+ 路由。
- 发现三种「别名收敛」机制（代码层已存在的去重）：`routeAliases`(25 条 router.replace) / `redirect()` 页(10 个) / `QueryPreservingRedirect`(customer) / re-export 页(21 个)。
- **v1「重复环节」诊断大半撤回**：CRM/换脸/情报收件箱/合规均已被 redirect 收敛；image-gen vs ai-image-gen 是「图文 vs 单图」两个功能；xiaohongshu vs xiaohongshu-assistant 是「列表 vs 生成器」；视频 7 入口是不同引擎（Wan/Seedance/dashscope/商品剪辑/发布计划/工作坊/studio_core）。
- 真正剩下的 canonical 冲突只有两处：C1 `/message`(rail 互动) vs `/engagement`(统一收件箱) 正门未定；C2 sceneOfPath 6 处过期前缀（含 `agent-console` 死引用，无 page.tsx）。
- 门控清单：gray 9 页(GrayTestOverlay) / desktop 9 页(DesktopOnlyGate) / 占位 2 页(video-studio/video-workshop)。

### 教训补充
- 「看起来重复」≠「重复环节」：功能多样性（多引擎）与路由别名收敛（redirect）都可能被误判成重复。判定前先看是否 redirect 收敛、是否 re-export 复用、是否功能语义不同。

## C2 死前缀清理（已执行，大王「你定就行」拍板）
- 删 3 个 shell 映射里的 9 处死前缀（永远匹配不到，零行为改变）：
  - `app-shell.tsx` sceneOfPath：删 `/report-new`、`/douyin`、`/wechat`、`/confirmations`、`/rpa-workbench`、`/agent-console`。
  - `mobile-shell.tsx` activeTabOf：删 `/confirmations`。
  - `command-palette.tsx`：删 `/douyin`、`/wechat`、`/rpa-workbench`。
- 验证：`npx tsc --noEmit --skipLibCheck` TSC_EXIT=0，0 错误；Grep 确认无死前缀残留（仅剩完整正确路径 /tasks/confirmations、/engagement/wechat）。
- **C1 未动，交底原因**：`/message`(rail 一级) vs `/engagement`(统一收件箱) 的根子不是导航，是**两套互动收件箱数据源并存**——message 用旧 `localEngineApi.tasks`，engagement 用新 `InteractionInboxService`。收敛需先定数据源（属 B 类），且 message 被 6 处引用（桌面 rail/移动 tab/命令面板/2 业务页），且含移动端专属视图，redirect 会误伤移动端。
- **遗留 bug 交底**：`/growth/rpa-workbench` 当前高亮「获客中心」而非注释意图的「执行中心」（被 /growth 前缀先拦截），删死前缀未改变此行为，需单独确认是否修。

## Codex R2 打回：channel-console 共享 loadError 竞态（已修，9d5f9f6c）
- Codex 结论「不能签收 R1 全绿」，唯一阻断项：loadAccounts 与 refreshTasks 经 Promise.all 并行启动却共用一个 useLoadError——账号失败上报后任务成功 clearLoadError() 抹掉账号错误，页面呈现"无错误的空账号列表"（P0 竞态）。
- 修法：拆 accountLoadError/taskLoadError 两实例（:107-118），各自成功只清各自（:163/:184），失败上报各走各（:168/:189），deps 各自补齐；移动分支双 mx-card（:430-438），桌面双 LoadErrorBanner 重试各直调各的加载函数（:637-642）。操作共享 error 不受影响。
- 门禁：tsc 0 / eslint 0；grep 确认无残留裸 loadError（仅注释行）。复核清单补 §七 R2 记录（file:line + 验证命令），E7 标记已修。
- 教训：**共享可清除错误状态 = 并行加载的隐性竞态源**——R1 修"成功清错不残留"时亲手造出了 R2 的竞态，复核链条必须每轮重审交互组合，不是只看单函数。
- 交底：分支级负向测试仍挂账；双路同时失败出两个错误块（预期，未合并）。Codex R3 复核待邀。

## 1.1.111 发版（大王 03:05 说「发」，走 CI 全自动）
- 版本号 6 处同步：desktop/package.json + lock(2处) + packager.json + frontend/src/lib/release-notes.ts DESKTOP_APP_VERSION + release-notes/page.tsx currentVersion + desktop/release-notes.md 顶部新条目。check-version-sync 通过 v1.1.111。
- 更新说明三份内容改写：highlights=报错看得见+获客失败说人话；fixedIssues=platform_id 回填/验证码误判/移动端紫钮/悬浮球清理。
- 货舱转正 6 项：原 5 项 + 报错透明度批次（76b15974/7c14688d/b0efcb60/9d5f9f6c）搭车，release-1.1.111-pending.md 表格已补行并标转正式。
- commit c0d20e2b（版本号+说明）→ push main → tag v1.1.111 → push tag → CI run 33429057374（release-desktop.yml tag 触发=商用发布，KAYPAL 生产域 env 内置）。
- 提交前自查：前端 tsc 0；全仓扫无 1.1.110 硬编码残留（仅历史条目）；login 页脚无硬编码版本（动态读）。

## C1 两套收件箱数据源结论（大王「要」，已盘清）
- **数据关系已确认**：本地引擎任务(5409) 经 `local-engine.persist.mixin.ts` 持久化到后端 `interactionTask` 表（`localTaskId` + `taskTypeToPrisma`/`taskStatusToPrisma` 完整映射，local-engine.service.ts:1868/1897）。后端是云端权威镜像。
- **状态零遗漏**：本地引擎 9 状态 ↔ Prisma 9 状态 一一映射（queued→QUEUED、waiting_for_send_confirmation→WAITING_FOR_SEND_CONFIRMATION 等）。
- **新收件箱是超集**：`InteractionInboxService` 聚合 interactionTask(全状态) + interactionEvent(平台事实) + Lead(线索) + Article(内容来源)，比旧收件箱(localEngineApi.tasks) 多出线索/CRM、内容来源、SLA、分配人、回复草稿、转人工、视图计数。
- **退役边界**：
  - ✅ 可退役：`message` 的 InboxSection（旧"待处理互动"展示层，localEngineApi.tasks）。
  - ❌ 不可退役：`/tasks/confirmations`（待确认 + approve/skip 动作，绑定本地引擎 /local-engine/tasks/${id}/approve）。
- **C1 落地建议**：message 保留渠道卡片(INTERACTION_CHANNELS 导航门户有价值) + 待确认入口(hint→/tasks/confirmations)，只把 InboxSection 换成新收件箱 pending 视图或跳转 /engagement。

## C1 message InboxSection 退役（已执行，大王「要」）
- 改动 `frontend/src/app/(dashboard)/message/page.tsx`：23 增 149 删，tsc 0 错误。
- 删：旧 InboxSection 组件 + slaTint/slaLabel + inbox state + localEngineApi.tasks(100) 调用 + 移动端「待处理互动」区块 + inbox props（桌面/移动）。
- 加：桌面端 ScenePage 用 `before` prop 注入「统一收件箱」入口卡片（跳 /engagement，复用 kx-agg-* 样式 + ShellIcon message/messageSq）。
- 保留：渠道卡片(INTERACTION_CHANNELS，移动端渠道宫格「AI客服」href=/engagement 即收件箱入口)、待确认入口(hint→/tasks/confirmations)、confirmations 数据加载。
- 数据源统一：互动收件箱只在 /engagement 一处（InteractionInboxService），message 不再维护 localEngineApi.tasks 收件箱。
- 未 commit，待大王确认后单独打 commit。

## C1 收尾 + 拆卡片（已 commit de43a0ea，大王「1要 2现在就拆」）
- commit `de43a0ea` `refactor(interaction): message 退役旧收件箱 + 拆「AI客服」为收件箱/机器人`，3 文件 34 增 153 删。
- 拆卡片：nav-registry INTERACTION_CHANNELS 的「AI客服」(key=ai-service) 拆成「统一收件箱」(key=inbox, href=/engagement) + 「客服机器人」(key=ai-service, href=/engagement?tab=bot)。
- engagement/page.tsx 支持 `?tab=bot` 直达客服机器人：`window.location.search` + `useEffect`（静态导出安全，避免 useSearchParams 的 Suspense 限制 + hydration 不匹配）。
- message badge 逻辑 `ch.key==="ai-service"`→`"inbox"`；移动端渠道宫格 `slice(0,8)`→`slice(0,12)`（容纳 9 卡片）。
- 验证：tsc 0 错误；`bot` 图标名有效(icons.tsx:99)。
- **关键经验**：静态导出(output:export)下读 URL query 参数，用 `window.location.search`+useEffect 而非 useSearchParams（后者需 Suspense 边界）；useState 初始函数读 window 会导致 hydration mismatch。

### 1.1.111 CI 发版结果（03:28 收官）
- CI run 33429057374 **success**（含 verify:remote 双通道门禁 job 全绿），全程 ~18 分钟。
- 独立 curl 复核：latest.yml / latest-mac.yml 均 1.1.111；Win exe / Mac zip Range 探测均 206 公开可读。
- 交底：workflow 无 GitHub Release 步骤（1.1.100 起设计为 OSS-only，更新链路只吃 OSS generic feed），gh release list 最新仍 v1.1.99——铁律 6 的"双写"与现行 CI 路径有出入，GH Release 双写挂账 P2。
- 本机自动更新实锤：/Applications 装 1.1.104，更新器缓存 pending/ 已躺 1.1.110 完整包（8/31 19:59 下载+sha512 校验通过），卡在"正常退出才安装"——旧版→新版链路 Mac 端第一次真机走，待大王开 app + Cmd+Q 验证替换/重启环节。~/Applications 残留 1.1.90 老包（不同位置，不影响）。

### 真机验证受阻：3010 TCC 卡死 + next build OOM（环境问题，非代码问题）
- **3010 static-server TCC EPERM 卡死**：日志 `[static-server] uncaught: EPERM: operation not permitted, open 'out/login.html'`。根因 = macOS TCC 拦截 launchd 域进程（com.jiuzhang.ai-content-frontend, PID 61549, KeepAlive=true）读 ~/Documents 下 out 文件。skill「launchd-tcc-service-recovery」的"node 读 Documents 放行"结论当前已失效（授权被重置）。症状：302 重定向正常，但所有 fs.createReadStream 文件读取超时（curl 000）。
- **next build OOM**：`next build && postbuild` 8分42秒后 `Killed: 9`（Turbopack 内存不足）。内存紧张根因：VirtualMachine.xpc 占 8.9% 内存 + 多个 ms-playwright chromium + Codex/ChatGPT helper。
- **发现另有 next-build 进程(92539) 03:46 启动**（疑似大王手动构建），不应再并发起 build。
- **验证脚本已备好**：/tmp/verify-3010-full.cjs（playwright 登录 __REDACTED_TEST_USER__ + 验证 ?tab=bot / message 入口 / 移动端 9 宫格）。待环境修复后可复用：起临时静态服务器 `API_BASE=http://127.0.0.1:3011 node scripts/static-server.mjs --port 3410 --root out`（用户上下文避开 TCC）。
- **结论**：代码改动已 commit（de43a0ea + 1eca2f48），tsc 0 错误；真机验证卡在环境（TCC 授权 + 内存），需大王决策是否关虚拟机/Chrome 释放内存后重试。

### 真机验证第二轮：定位到后端 SQLite 损坏（根因确认）
- out 产物已由大王手动构建更新（03:47，message.html/engagement.html 已含「统一收件箱」「客服机器人」）。
- 起临时静态服务器 3410（用户上下文避开 TCC，curl 200 正常）。
- 登录 API `/api/auth/login` 正常（201，账号 __REDACTED_TEST_USER__ 有效）；静态层带 cookie 访问 /message 200 放行。
- **但前端 JS 加载后 authApi.me 返回 500 → 误判未登录 → 重定向回 /login**。根因 = 后端 3011 SQLite 数据库损坏：
  `PrismaClientUnknownRequestError: QueryError(SqliteError { extended_code: 11, message: "database disk image is malformed" })`，/api/auth/me、/api/growth/home、/api/kaypal/profile、/api/kaypal/subscription、/api/kaypal/billing 全部 500。
- **这是记忆里已知根因**：「Win3011 SQLite 损坏，v1.1.100+ auto-heal 自动修复」（同事机器 15827）。当前 3011（dist-bundle-sqlite）数据库已损坏，需 auto-heal 或手动修复。
- 验证脚本可用版本：/tmp/verify-3010-cookie2.cjs（context.request 登录，cookie 自动保存），等 SQLite 修复后即可跑通三点验证。
- 结论：我的前端改动已过产物层验证（grep 确认改动进 out）；真机渲染验证唯一卡点 = 后端 SQLite 损坏（me 500），与前端改动无关。

### SQLite WAL 修复 + node 路径修复（已执行，大王「修吧」）
- **根因 1（WAL 损坏）**：主库 `~/Library/Application Support/ai-content-desktop/kaypal-ai.sqlite`（166MB）integrity_check=ok（无损），但 WAL 文件（127KB）损坏 → 读「主+WAL」组合报 malformed。历史 orphan 文件（8/26-27 大量 `-shm.orphan`）佐证多次异常退出。
- **修复**：备份主库+WAL 到 `/tmp/sqlite-heal-backup/` → kill 96694 → 删损坏 WAL+SHM（主库无损，仅丢 WAL 未合并事务约 127KB）。
- **根因 2（node 路径升级）**：node 8/31 20:02 从 `22.22.2` 升级到 `22.22.2-2`，两个 launchd wrapper 硬编码旧路径 `22.22.2/bin/node` → 后端重拉 exit 126（No such file）。已修：backend-launchd.sh + static-server-launchd.sh 的 node 路径 `22.22.2`→`22.22.2-2`。
- **「卡点」实为误判（已澄清：后端 96006 已监听 3011、me/growth 均 200，完全恢复）**：后端重拉后那阵「内存 0.1%/CPU 0%/未监听」实为启动初始化（migration/auto-heal）阶段，不是卡死。后确认 CPU 52%、已监听 3011、数据库生成新 WAL 4.2MB，`/api/auth/me` 200、`/api/growth/home` 200。
- **前端 3010 TCC EPERM（未修，交 Codex）**：login.html/index.html 读超时，日志 `EPERM: operation not permitted, open '.../frontend/out/login.html'`。根因 = launchd 域进程读 ~/Documents 下 out 被 macOS TCC 拦；当前 3010 进程 61549 是旧进程（node 22.22.2 的 `.deleting` 句柄，跑 10.5h）。skill `launchd-tcc-service-recovery` 里「node 读 Documents 放行」旧结论已失效（现在读也 EPERM）。修复方案：out 迁出 Documents（参照 kaypal-proxy 先例）。
- 已产出：`artifacts/环境问题修复说明-2026-09-01.md`（交 Codex，含已修复 2 项 + 待修 1 项 + 修复命令 + 验证命令）。
