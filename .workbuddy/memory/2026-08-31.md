# 2026-08-31

## v1.1.106 全量重发（00:51-01:30，Codex 复核 P1-A 驱动）

- Codex 复核：1.1.105 包是旧构建（缺 preload-hoverball.js、旧 getAccounts、悬浮球
  还默认开）——源码修复没进包，必须重建。结论正确。
- P1-A 执行：升 1.1.106 → frontend build → win bundle(runtime/playwright) →
  electron-builder --win → 精确 --installer 解包验证（preload 在包 ✅、bundle 含
  getAccounts 同步 ✅）→ mac 状态 → 全量门禁 16/16（不 skip-build）→ mac 包 →
  推送 → verify --remote 双过（sha256 一致）。
- 同步修 P2-B/C/E（commit 284b45e1）：解包临时目录 finish() 统一清理（Codex 见
  7 个 cfa-installer-* 泄漏）；自愈 isQuitting 提前 + 等 flush + code14(CANTOPEN)
  移除 + P2010 收窄（正则 7/7 验证，P2010 no such column 不再误触发清库）；
  autoDownload=false + 显式 downloadUpdate（跳过版本不再下载）。
- 上传慢：517MB multipart 20 分钟（正常 1-2 分钟）——本次网络慢，非脚本问题。
- 待办：P1-B（门禁 tee 全量日志）、P2-D（Cloud API 决策）、P2-F（OSS readback）。

## v1.1.107（02:28-03:20，Codex 复核 P1 驱动）

- release-guards.test.js 9 个僵尸测试重写对齐当前 API（引用已删的 6 个旧函数）
  → 25/25；门禁 L1 新增 node:test 步骤（此前 jest 不覆盖，16/25 僵尸红灯无人知）。
- **dev.db→seed.db 改名**：seed 模板库（166 表空模板）原名误导审计（"开发库进
  生产包"红项）。全仓库引用同步，**release-guards.js:621 漏改导致 1.1.107
  --installer 检查失败**（读不存在 dev.db）——修后 passed。教训：改名要 grep 全仓库。
- 自愈 sidecar(WAL/SHM) rename 失败 → 阻断重建（残留 WAL 污染新库，数据安全）。
- skipUpdate 补 cancelDownload（跳过版本立即取消下载，防 autoInstallOnAppQuit
  装被跳过的版本）。
- 门禁 17/17（+release-guards）；双通道 1.1.107 已推；verify 远端 passed
  （RELEASE_VERIFY_FEEDS=latest.yml,latest-mac.yml 过滤 Linux 噪音——Linux 停
  1.1.99 非发布目标）。
- 版本轨迹：...→1.1.105→1.1.106(撤)→1.1.107。

## v1.1.108（12:54-13:40，Codex 复核闭环 + 大王两项决策）

- Codex 8 项未闭环 → 全部处理：
  - P1 自愈 sidecar 顺序：先移 sidecar 全成功再移主库，失败回滚+阻断（0d79bb99）
  - P1 跳过竞态：skipUpdate await cancel + 清 downloadedVersion；quitAndInstall
    拒绝已跳过版本（下载完成先于取消也不装）
  - P1 Agent-S 包级证据：commercial-gate 1.1.107 环境 PASS=66/real-execution 5/5
    + 绑定 JSON（installer 文件名+SHA256+task id）→ docs/acceptance-evidence-2026-08-31/
  - P2 db 门禁：check-package-contents 白名单只放 seed.db，普通 .db 全禁
  - P2 门禁日志：13 处 tail 全去 + 每步证据文件 docs/gate-evidence-<ts>-<commit>/
  - P2 顶层 try/finally 清理解包临时目录
  - **大王决策：移除 Linux 发布目标**（CI 矩阵/上传/verify 全删，verify 无过滤绿）
  - **大王决策：移除 Cloud API 死接口**（cloud-api.js 5 方法 + adapter 4 方法删，
    tsc 过；保留 reportClientError）
- 1.1.108 全量重发：17/17 门禁、双包精确检查、双通道 verify --remote 全 passed。
- 版本轨迹：...→1.1.106(撤)→1.1.107→1.1.108。

## 17:00 交接确认（大王指令：确认 git 状态保证信息准确）

- 大壮线程 16:33-16:59 完成 CI 发布路径修复并写交接文档：3d5233e8（Windows
  checkout 换行符根因，v1.1.99 至今 CI 发布从未跑通的根因）、3137f5a5（CI 两老坑：
  frontend base 同源 + Octop sidecar CI 现场生成）、75f3276b（extraResources EEXIST
  根治：7 from 同目标合并单 from + filter；win octop venv GITHUB_PATH 坑）、
  eeb6ee1c（交接文档）。
- **核对发现交接文档 commit 引用错误**：主修复被标为 c78d9bc8（实为 1.1.107
  发布），真实主修复 = 0d303720（release-feed-plan 三通道 + verify remote-only +
  CI 门禁 + Mac 兜底 4/4）。已修正推送（70e395cb）。
- 线上核对：win/mac 1.1.108 ✅、linux 1.1.107（大壮线程 CI ubuntu job 补发，替代
  1.1.99 404）✅、verify:remote passed。
- 交接结论：发布门禁 + Linux 404 + verify 全绿已完成；剩余 mac CI Build artifacts
  环境差异失败点定位 + skip_upload 开关（验证与发布分离），非阻塞。

## 17:06 octop 登录链路修复（大王：octop 应使用 3010 机制，不能有自己的登录页）

- 3010 开发文档机制：/api/octop/launch（3010 登录态）→ KaypalOctopBridge.loginOctop
  → OctopIdentity（per-user 确定性派生 kx_* 账号）→ token 注入 WebContentsView 免登录。
- **实证根因**：octop-bridge DB 密码与 backend/.env OCTOP_ADMIN_PASSWORD 不一致
  （0.9.26 升级后 hash 失配）→ /api/auth/login AUTH_FAILED → token=null → 前端
  降级 → 用户看到 octop 自带登录页（octop 0.9.26 登录 = user_manager.authenticate
  argon2id + JWT；/dashboard 未认证返回 SPA，未登录跳 /login）。
- 修复：`~/.octop/venv/bin/octop user passwd octop-bridge --password <env值>`
  重置对齐 → login 200 → /api/octop/launch 恢复（healthy+token+isolated）。
- 防复发：octop-launch.controller catch 加显式 warn（healthy 但 token=null =
  凭据问题，标注排查），不再静默降级。commit 已落。
- 待大王定：octop 自带登录页（直接开 8088）——3010 集成已免登录；彻底禁需
  部署层方案（8088 不对外只走桌面注入 / octop OIDC-only / 接受独立登录页）。

## 17:11 octop 登录页整改（大王选 1：8088 不对外，只走 3010 桌面端注入）

- 落地确认：bind_host=127.0.0.1（网络层已不对外）✅；cors_origins=[] ✅；
  token 链路恢复（launch healthy+token+isolated）✅；桌面端注入代码完整
  （workspace-tabs.js：Bearer 同源注入 + setOctopCookie + localStorage.auth_token
  自举，1.1.108 包内含）✅。
- 验证：playwright 模拟桌面端注入（launch token → localStorage.auth_token →
  reload）→ octop 落在 /chat 工作台（对话/专家/任务/远程桌面），**非 /login** ✅。
  桌面端 UI 真机（app 不开 CDP、dev electron 前端 /api 反代缺失）受阻，用注入
  模拟等价覆盖 octop 侧认证行为。
- 流程坑记录：正式 app open --args --remote-debugging-port 不生效（9333 不开）；
  dev electron 前端固定外部 3010（next dev，/api 无反代 → auth/me 404）；
  验证 octop SPA 免登录用 playwright 注入最直接。
