# 2026-09-02

## 复核第二轮修复（ed2d4fa4，13 文件）
- P1-A 重启串库根治：guard 每请求 ensureAccountDatabase + 路由映射缺失拒绝（不回退全局）；新增独立套件测试 A/B 交替访问。
- P1-B wan-i2v 归属键统一本地 user.id + 旧任务 fail-closed；P1-C 工坊/上传无归属旧任务拒绝 + 手机上传全链校验；P1-D StudioCore 项目认领制（studio_project_owners 系统库表 + 全项目 ID 操作校验）。
- P2-1 迁移/恢复前 wal_checkpoint(TRUNCATE)（一致性快照）；P2-2 video 列表 503 上屏 + me.tsx 三路错误 + home 分区级显示。
- 门禁：lint 1 错修复（persist.mixin 多余断言）；client 还原 postgres；全量 235/2673/0。
- 3011 已同步新 bundle（SHA 9c514f3a）+ 重启，认领表随启动建表。

## 复核第三轮修复（84a54f88，7 文件）
- P0 StudioCore 授权重做（放弃认领制）：创建入口（video-workshop jobs）原子登记 owner（INSERT OR IGNORE+回读防并发抢占）；列表按创建者过滤；详情/下载/导入/SSE assert 校验（无记录拒绝）；jobs import-material 补校验；受控迁移端点 migrate-owner（仅 admin）。历史项目默认拒绝，可迁移归属。
- P1 工坊旧任务：listTasks 删除 !task.userId（上轮替换未生效被复核抓出——教训：python 批量 replace 后必须 grep 验证生效！）+ 负向测试。
- P1 一致性快照：VACUUM INTO 替代串行复制（事务内一致，失败阻断）。
- P2：persist.mixin 映射表声明改 PrismaInteractionTask* 类型；agent-gateway 7 失败根因=build 传 KEEP_SQLITE 跳过 client 还原（**以后 build 不传 KEEP，脚本自动还原 postgres client**）；listPipelines 抛 503。
- 全量 235/2674/0 + 29 skip；tsc/eslint 0；3011 已同步重启。
- **教训固化**：①批量 sed/python replace 后必须 grep 验证实际生效（listTasks 漏改一轮）；②build:bundle 不传 KEEP。

## 复核第四轮修复（a6c6c437，7 文件 + jest.setup.ts）
- P1-1 创建入口归属：video/generate + product-cut（都走 postGenerate 创建项目）成功后登记 owner（服务端认证用户）。
- P1-2 认证缺失失败关闭：resolveUserId 缺失/异常抛 401（不再退化为无过滤访问）。
- P1-3 jest 门禁根治（无 DATABASE_URL 环境 runInBand 连跑 3 次 exit 0）：jest.setup.ts 默认 URL + PrismaService 构造期 JEST_WORKER_ID 兜底（防收尾 P1012）+ spec:314 还原 + 接线测试 override PrismaService 空壳。
- P2 migrate-owner 仅 admin/SUPER_ADMIN。
- **教训**：复核环境无 DATABASE_URL——本地全量过 ≠ 复核过；门禁验证必须用 `env -u DATABASE_URL jest --runInBand` 复现复核环境。

## 复核第五轮修复（5d70326c，3 文件）
- P1-1 lint：Unnecessary import + prettier 清零。
- P1-2 认证缺失失败开放（**上轮 resolveUserId 替换没生效**——python replace 目标文本与实际不符，复核抓出）：本次 Edit 直改 + service userId 必填 + if(userId) 清理。
- P1-3 owner 登记失败错误回退：generate/productCut 拆分 catch（只捕获引擎失败；register 失败原样抛错）。
- P2 migrate-owner 补 kaypalRole/kaypalPlatformRole SUPER_ADMIN。
- **教训固化**：python 批量 replace 不可靠（静默不匹配）——关键安全改动必须 Read+Edit 直改 + 改后 grep 验证生效。
- 全量复核环境 runInBand 235/2674/0 exit 0；video 61/61。

## 复核第六轮修复（2945943e）+ Windows 1.1.112 发版
- P1 业务拒绝不回退：StudioCoreBusinessError（4xx）区分引擎错误，generate/productCut 只对引擎失败回退/降级。
- P1 migrate-owner 判定对齐 guard（admin/super_admin 小写 + kaypalRole??kaypalPlatformRole SUPER_ADMIN），提取 service.isAdminForProjectMigration 可测。
- P2 回归 +8：video.service.spec（引擎回退/业务拒绝不回退/owner 登记失败不回退/productCut 不降级/四类管理员）+ 新建 video.controller.spec（resolveUserId 401）。video 69/69，全量 236/2682/0。
- Windows 1.1.112 发版：交叉构建（seed.db 合规 166 表 0 账户）→ OSS updates/ 上传 6 文件（win exe 543MB multipart）→ 远端校验全过（latest.yml/exe 200/mac）。构建后恢复本地 native bundle + 同步 3011（未认证项目接口 401 生效）。
- 推送 main（0 待推）。

## 复核第七轮修复 + 1.1.113 重发（99d13fc4 / 4cd10484 / 974b564c）
- P0 3011 Agent-S blocked：根因=win 交叉构建污染 playwright-browsers（chrome.exe 进了 mac 运行副本）+ 缓存整树复制混入双平台。恢复 mac chromium + sync 3011 → agent-s ready、mcp online。根治 prepare 脚本 cpSync filter（只复制目标平台 chrome-* 目录）。
- P1 migrate-owner 控制器没接 helper（python 替换第三次没生效！）：Edit 直改 + 真实控制器集成测试（真 VideoService，消除假绿）。
- P1 StudioCore 登录 4xx → StudioCoreBusinessError（不回退云端计费）+ proxy 分类测试。
- P2 release-notes.md 补 1.1.112/1.1.113 条目 + check-version-sync 门禁升级（首标题版本==包版本）。
- 1.1.113 双平台构建（Mac 625MB 正常、Win 543MB）+ OSS 上传 + 远端校验全过（feed/exe/releaseNotes 标题一致）。构建后恢复本地环境（mac chromium + native bundle + 3011 验证 ready/401）。
- ⚠️ 教训：工作区曾有并发未提交修改（auto-upload.client/local-browser-engine），已恢复到工作区未动——非我职责范围，由改动方自行提交。
