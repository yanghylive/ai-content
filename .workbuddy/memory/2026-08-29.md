# 2026-08-29 工作日志

## 安装并审计 skill：天美AI（hirui-tianmei-video v1.7.2）

- 大王要求按 skillhub.md 安装 `@user_101bb978/hirui-tianmei-video`，并评估能否提升生图生视频质量。
- 结论：该 skill 已通过 WorkBuddy skillhub 商店机制装好（`~/.workbuddy/skills/hirui-tianmei-video__skillhub/`，v1.7.2，source=skillhub）。
- 安全审计（临时下载到 /tmp 逐文件审计）→ **P2 安全**：仅 Python 标准库、只连 tmeiai.com + api.qrserver.com 两域名、无 subprocess/eval/pickle/base64 混淆、文件写入仅限产物下载/task 信息/config.json、webbrowser 只开微信授权页。
- 本质：天美AI（海睿数科，腾讯 WorkBuddy 授权伙伴）第三方 SaaS 生图生视频 API 封装，非"质量增强插件"。
  - 卖点：真人口播/数字人带货短视频、中文文字渲染稳定的营销海报/产品图/信息图/封面图、商用授权、批量生产。
  - 计费：微信扫码登录取 API Key（tmei_ 前缀），积分制 ¥1=1 积分；视频 1~3 积分/秒，图片 1~2.5 积分/张。
  - 与内置 ImageGen/VideoGen 是**互补**关系，非替代：擅长营销向口播/带货/中文海报，通用艺术向生图不强。
- 遗留：该 skill 尚未登录（无 config.json）；当前会话未加载（新会话/重启后才会出现在可用 skill 列表）。

## 评估：天美AI 是否适合接入 ai-content（3010）

- 摸清 ai-content 现有生图生视频能力（后端 modules）：DashScope 通义万相（wanx）文生/图生视频 + Qwen-Image（qwen-image-3.0-pro）生图 + qwen3-tts 配音，8/9 起统一走 kaypal 模型台网关 + 云端积分结算（不再百炼直连）。
- 视频链路另有 studio_core 引擎：`video.service.ts` buildProductCopy 是模板带货文案（usedAi:false）+ `studioCoreProxy.postGenerate({pipeline:'promo'})` 商品视频自动剪辑（product-cut 页）。
- **关键盲区**：ai-content 现有是「万相画面 + TTS配音 + studio 剪辑」，**没有真人口播/数字人带货**能力。天美AI 差异化正好在这。
- 判断：工具层（团队出素材）✅ 可立即用；产品层（集成给用户）⚠️ 要过三件套重复建设关（天美是独立 tmei_ Key+微信登录+自积分，与 kaypal 计费不通）+ 确认 tmeiai.com 是否有企业级 API（skill 是本地 Python 脚本形态）。

## Win 真机 3011 启动崩溃（15827 同事机）+ error-reports 盲区发现

- 同事 Windows 机装 v1.1.99 正式包，3011 后端启动即崩（退出码 1，Prisma Client 查询报错，弹窗堆栈被 main.js:1602 截断）。已给同事自救命令：改名 `%APPDATA%\ai-content-desktop\kaypal-ai.sqlite*`（含 -wal/-shm）重建库；并索要 logs 目录三件套。
- **error-reports 自动上报存在结构性盲区**：链路 = 前端 bridge → POST /api/error-report/client → **本地 3011 后端**（持 OSS 凭据）转发落 OSS。后端启动崩溃 = 转发者自己死了 = 零上报。主进程 main.js 无任何上报逻辑，cloud-api-server 无接收接口。
- OSS 实测：error-reports/ 8/20~8/28 每天都有（2~59 条/天），**8/29 = 0 条**，坐实盲区。
- 待办：主进程崩溃兜底上报（backend close 事件把 stderrTail+launchLog POST 云端转发），云端 cloud-api-server 加接收接口；desktop/.env 的 OSS AK 无 ListObjects 权限（403），list 用 aliyun CLI（kaypal profile）。
- **兜底上报已落地（未 commit、未发版）**：
  - 云端 `cloud-api-server/server.js`：匿名 `POST /api/v1/client-error`（注册在 requireAuth 之前）+ IP 限流（60s/20 条，同后端匿名端点）+ **零依赖手写 OSS V1 签名 PUT**（不加 ali-oss 依赖，部署免重装），落 `error-reports/<ymd>/<uuid>.json`，格式对齐 error-report/v1（kind/exitCode/launchLog/system 扩展字段）。
  - 桌面 `desktop/cloud-api.js`：CloudAPI 加 `reportClientError()`（fire-and-forget，失败静默）。
  - 桌面 `desktop/main.js`：`reportBackendCrash()`（会话频控 3 条 + 同指纹去重，防 MAX_RESTARTS=3 重启循环刷 OSS）+ 两个挂点：`backendService.on('close')` code≠0（崩溃）、`waitForBackendReady` 失败（backend-not-ready）。上报内容含 stderrTail 8K + backend-launch.log 尾 4K + 版本/平台/dataPath。
  - **E2E 已验证**：本机起 server.js（3127 端口 + desktop/.env OSS 凭据）→ curl POST 模拟 15827 崩溃 → aliyun oss cp 拉回，JSON 结构完整；测完已删 OSS 测试文件 + 杀测试进程。
  - 生效条件：随下个桌面版本发版（客户端）+ cloud-api-server 重新部署（云端）。

## v1.1.100 发版落地（2026-08-29 下午，自愈+上报闭环）

- **门禁修复 3 项后 15/15 全绿**：①jest spec 漏改（8/27 /api/ai→/api/v1 收敛没同步 spec）②pre-release-core-verify 灰度抽查里的企微助手 8/24 已转正，换 /mobile-capabilities ③全路由扫描排除 /case-admin（admin 接口对 operator 验收账号 403 属权限正常）。/tmp/electron-test-token.txt 丢失（重启被清）→ 从 3011 /api/auth/login 拿 ai_content_session cookie 重新生成。
- **数据库自动自愈**：main.js backendService.on('close') 里 stderr 命中 SQLite 损坏特征（malformed/not a database 等，覆盖数据页损坏这类 schema 预检查盲区）→ 自动备份 kaypal-ai.sqlite* 为 .corrupt-<ts> → createEmptySqliteDatabase → 走现有重启链路。每会话限 1 次。
- **发版**：Win 包 530MB（Setup 1.1.100.exe + blockmap）推 updates/，latest.yml SHA512 校验一致 + 公网可读；Mac zip 654MB 推 updates/，latest-mac.yml → 1.1.100。
- **云端 cloud-api-server 部署（/opt/ai-content-cloud-api，118 prod）**：scp server.js + compose 加 OSS_* env（.env 600）+ **HOST=0.0.0.0（新代码默认 127.0.0.1 会致 docker-proxy Connection reset，踩了）**。/api/v1/client-error E2E 通。
- **nginx 新增反代**：kaypal.cn 443 块加 `location /cloud-api/ → 127.0.0.1:3100/`（第一次插错到 80 兜底块，307 zh-CN 排查后挪正）；kaypal.conf 备份 .bak-20260829。api.kaypal.cn DNS 指向 101 测试机——不是 prod 入口！
- **CloudAPI URL 拼接 bug 修复**：`new URL('/api/v1/x', 'https://host/cloud-api')` 丢前缀 → 保留 base path 拼接。本机 store endpoint 从失效的 enterprise-test 换成 enterprise.kaypal.cn/cloud-api。
- **遗留交底**：①假旧版升级 E2E 未跑（会打断宿主会话），下次云电脑窗口补 ②真机自愈链路靠同事 15827 升级后验证 ③生产 compose 里 API_KEY 未配置（/api/v1 鉴权端点仍 503，既有状态未动）。

## v1.1.101 发版（2026-08-29 傍晚）：更新自动下载

- 起因：大王问「3011 起不来能自动更新吗」→ 链路确认能（更新在主进程直连 OSS，不依赖本地后端），但 autoDownload=false 需用户手点下载 → 大王拍板改自动下载。
- 改动：auto-updater.js `autoDownload=true`（下载自动、安装仍由用户触发 + autoInstallOnAppQuit 退出即装）。版本号同步 1.1.101 双端四文件。
- 门禁 15/15 全绿 → Win exe（530MB）+ Mac zip（654MB）推 updates/，双通道 latest*.yml 均 1.1.101，206 可读验证通过。
- 更新链路要点（auto-updater.js）：启动后 5s 首查 + 每 2h 轮询；3011 死时启动流程多等 60s 才到检查，正常可达。
- 待观察：OSS error-reports/ 后续日期（15827 升级后自愈/上报是否生效）。


