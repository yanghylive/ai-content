# 2026-08-24 工作日志

## 自动化：error-reports 检查 + 修复 kaypal 409 幂等重放（commit 0aa12fa4）
- OSS error-reports 发现 21 条新报告（08-23，全为 dev 构建 version 0.0.1，hostname 192.168.1.3），全部同一错误：POST /api/ai-models/v1/chat/completions → 503「409 BILLING_IDEMPOTENCY_REPLAY」。
- 根因：`backend/src/modules/ai-models/ai-client.service.ts` generate/streamGenerate/generateWithImage 每次 `randomUUID()` 生成 kaypal 计费幂等键；同内容重试（外部调用方 3 连发、间隔 4-6s）键不同被 kaypal 网关判定计费重放 → 409。
- 修复：新增 `buildKaypalIdempotencyKey()`（scene+userId+modelId+内容 sha256 确定性键），替换 3 处随机键；与 ai-gateway 计划二.C.6「整次请求稳定幂等键」策略对齐。chargeCloudAiCredits 扣积分幂等键（行 722）保留随机未动。
- 验证：后端 tsc ✅；ai-client.service.spec 19/19 ✅（新增确定性单测）；L1 门禁 tsc/vitest/循环依赖 ✅。agent-gateway 4 spec 11 用例失败为**既有失败**（stash 对照确认，lead_discover 真实闭环依赖外部环境）。
- 状态：已 commit 未发版（模式 B，等大王确认发版）。
