# 2026-09-03

## 浏览器面板项目·第一轮（commit 22ed31d2，8 文件，不改业务代码不发版）
- 工作流文档：`/Users/yanghy/Documents/ChatGPT/ai-content/browser-panel-workbuddy-implementation-workflow.md`（大王定稿，三方协作：Workbuddy 实现 / Codex 复核 / 用户决策）。
- 阶段 0 基线 + 阶段 1 同页控制 P0 **全部通过（Mac）**：Broker 契约测试 11/11 + Electron smoke 11/11。
- 核心证据：`panelId=panel-p0 / sessionId=sess-p0-001 / webContentsId=3` 三方绑定；CDP 点击/输入/截图真实生效；用户→Agent 同帧一致；截图 sha256 留档；29 条事件全带三方 id。文件在 `docs/browser-panel-baseline/`。
- 关键坑（固化）：①Electron 窗口 `show:false` 时 `Page.captureScreenshot` 挂起、真实输入不派发 → smoke 必须可见窗口 + `backgroundThrottling:false`；②本 shell 有 `ELECTRON_RUN_AS_NODE=1` 残留会让 electron 退化成 node → 启动前 `env -u ELECTRON_RUN_AS_NODE`。
- 待大王定：直接进阶段 2（右侧 UI），还是先在 jz-win11 云电脑补一次 Windows smoke。

## 浏览器面板·第二轮（阶段 2 UI，commit 3b631feb，13 文件）
- 大王选 A 直接进阶段 2。右侧面板：`desktop/browser-panel-manager.js`（会话/布局/状态机/零特权面板视图）+ `browser-control-strip.html/preload`（本地受信控制条）+ `workspace-tabs` rightInset 让宽不遮挡主内容 + `main.js` 面板 IPC（open/state 双通道 sender 校验；导航类 strip-only）+ preload `browserPanel` API + 3010 `BrowserPanelDock`。
- 测试：manager 契约 11/11（首跑抓到换账号 partition 未更新真 bug，已修）+ Electron stage2 smoke 9/9（真实加载/零重叠/URL 回读一致/面板内无特权 API 实证/状态机/destroy 无泄漏）+ broker 11/11 回归 + release-guards 全过 + 前端 tsc/eslint 0。报告 `docs/browser-panel-baseline/round2-report.md`。
- ⚠️ 既有问题（非本轮引入，HEAD~2 同样红）：`node --test desktop/scripts/build-win-full.test.js` 在 mac 上加载模块即被平台守卫崩（测试未包 try）。本轮未动 build-win-full.js。
- 下一步待批：阶段 3（会话与安全边界 + Broker×manager `resolvePanelTarget` 正式接线）。

## 浏览器面板·第三轮（阶段 3 会话安全边界，commit 74cb4430，6 文件）
- 大王"继续"= 进阶段 3。`desktop/browser-broker-wiring.js`：manager×broker 接线——**capability token 只存活主进程内存**（handles/IPC 永不回传）；会话事件（opened/account-switched/hidden/shown/destroyed）自动同步/撤销 Broker 会话；`*ForAgent(panelId, actor{ownerId,tenantId}, …)` 入口全先 `assertActor`（跨 owner/跨租户 fail-closed）；`approveActionForAgent` **默认拒绝**（Agent 不得自我批准，硬约束 5）。
- broker：`assertActor`/`hasPendingHandle` + 事件流 URL 全面脱敏（`redactUrlForEvidence`：token/code/secret/sid 类 query→***）。manager：`onSessionEvent` 钩子（destroy 先发事件再清会话）。main.js：实例化接线（阶段 4 的 3011 Agent 桥唯一持权入口）。
- 测试：wiring **11/11**（跨owner/跨租户/换账号token失效/过期/销毁失效/脱敏实证/自批拒/缺actor拒/幽灵面板拒）+ broker 11/11 + manager 11/11 + 阶段1 smoke 11/11 + 阶段2 smoke 9/9 **全量回归绿**。报告 round3-report.md。证据目录滚动清理只留最新一轮。
- 首跑抓到 wiring bug（manager destroy 事件时序：先 fire 再清 session，reconcile 走不到全撤）→ 显式处理 destroyed 分支。
- ⚠️ 并发方工作区有 growth/rpa 未提交改动（非我动，提交严格逐文件、不碰）；main 已含其 78c384ec/06337f43 抖音获客提交（大王线并行推进）。
- 下一步待批：阶段 4（3011 Agent 桥协议 + observe/navigate/click 首批接入 + 审批 UI + 证据链落 3011）。

## 浏览器面板·第四轮（阶段 4 地基：Agent-S 上行桥，commit 175b5a35，9 文件）
- 大王"继续"= 进阶段 4。本轮**只建地基，不改 Agent-S 执行路径**（AGENTS.md 强监管）：`desktop/browser-agent-bridge-server.js`（127.0.0.1 随机端口 + randomBytes(24) token；路由只三条 health/observe/action-request，**不代理任意 CDP**；校验链 token timing-safe → nonce 防重放 5min → 时钟偏差 ±60s → 路由白名单 → actor 必填；action-request 只签确认单、服务端拒绝自批）+ `desktop/browser-agent-bridge-client.js`（3011 侧 SDK：构造期 fail-closed、只允许回环地址、4xx 抛 BridgeError 不重试不降级）。
- main.js：新增 `ensureBrowserBridge()/getBrowserBridgeInfo()/closeBrowserBridge()` 懒起停，**本轮默认不启动**，endpoint+token **不投递 3011**；`app.on('before-quit')` 加 `closeBrowserBridge()` 收尾。
- 测试：桥服务端 **12/12** + 桥客户端 **12/12**（对真实桥服务器端到端，非 mock）+ broker 11/11 + manager 11/11 + wiring 11/11 + 阶段1 smoke 11/11 + 阶段2 smoke 9/9 **全量回归绿**。报告 `docs/browser-panel-baseline/round4-report.md`。
- **本轮抓到的真 bug（值得记住）**：Node 19+ `http.globalAgent` 默认 `keepAlive:true`，`server.close()` 只停 accept 并等空闲连接结束 → **close 返回后旧 socket 仍可复用发请求**（应用退出后本地桥仍可调）。修法：置 `closed` fail-closed 标志 → 销毁全部在途 socket（`server.on('connection')` 台账）→ `closeAllConnections()` → `close()`，幂等。服务端+客户端各一条断言锁死。
- 顺手统一协议：`observe` 原只回 `target`、`action-request` 回 `binding` → 两条路由都回 `binding{panelId,sessionId,webContentsId,url}`。
- ⚠️ 待大王批准才能推进的硬卡点：**把 endpoint+token 跨进程投递给 3011 Agent-S**（候选 0600 临时文件 / env 注入，两者都要解决 3011 与 desktop 生命周期不同步）；批准后才有审批 UI → observe→act 闭环 → 证据链落 3011 evidence 目录。
- ⚠️ 欠账不变：Windows 真机 smoke 阶段 1~4 全未跑（阶段 6 前必补）；`build-win-full.test.js` 在 mac 既有失败未动；后端 growth/rpa 并发方未提交改动未触碰。

## 浏览器面板·第五轮（阶段 4/5 闭环，commit ccdf1c8c 25 文件 + c67514f9 清旧证据）
- 大王**"批准"**投递 endpoint+token 后推进。三件事：①0600 凭据文件投递；②桥生命周期；③写动作审批闭环 + 后端面板路由。
- 新增 `desktop/browser-panel-bridge-registry.js`（userData 下 0600 凭据文件；**读存量文件时也强制 chmod 0o600**，否则磁盘上已存在的 0644 老文件一直裸奔）+ `browser-panel-bridge-runtime.js`（生命周期编排，**main.js 与 E2E 共用同一模块**，避免"E2E 验副本、生产跑另一份"）+ 阶段 5 E2E 两个脚本（真实 Electron 真窗口 + **真实子进程扮演 3011**）。
- broker 增 `actionState`（查询**不消费**确认单）/ `listPendingActions`（不含 token）；`approveAction` 记 `approvedAt` + `approvalContext.channel`（区分"用户点批" vs "Agent 自批"）。wiring 增**用户通道** `approveActionAsOwner`（阶段 4 审批 UI 主进程接缝）。桥路由 3→6 条（+ `/execute`、`/pending-actions`、`/action-state`），**写动作缺 actionId 即 403**，仍不代理裸 CDP。
- 后端：新 `agent-panel-bridge.service.ts`（3011 侧客户端服务）+ `AgentBrowserExecutor` 面板路由，开关 `KAYPAL_AGENT_PANEL_MODE`（**默认 off 纯透传，非法值显式失败，不猜配置**）；`local-engine.module.ts` 注册桥服务；loop 从**会话租约**取 actor 身份透传。
- 测试全绿：broker 24/24、manager 11/11、wiring 11/11、bridge-server 19/19、bridge-client 17/17、registry 11/11、runtime 8/8；阶段1 smoke 11/11、阶段2 9/9、**阶段5 11/11**；backend `tsc --noEmit` 干净 + `jest src/modules/local-engine` **25 suites / 425 tests**。
- 阶段 5 E2E 核心事实：`observe` 的 webContentsId 4 == 面板真实 `webContents.id` 4；导航后 `afterWcId` 仍 4 且 markerFound（同页控制成立）。
- **本轮抓到的真 bug（值得记住）**：①`Page.navigate` 返回时新文档常未提交 → `webContents.getURL()` 滞后一拍，会产出"内容是新页、URL 是旧页"的**自相矛盾假证据** → 回读必须轮询（6s/300ms，URL 与文本同时满足才退出）；②`ELECTRON_RUN_AS_NODE` 传**空串等于没设**，子进程以 GUI 模式静默挂死 2 分钟 → 必须显式 `'1'` + 子进程 20s 超时 SIGKILL + 脚本 150s 全局看门狗（macOS 无 `timeout` 命令）；③给 `/execute` 补测试时假桩**没做 actor 断言**，"跨 owner"返回 200 而非 403 → **新路由的假桩必须自带 actor 断言**，否则测试里看起来安全、实则是绕过缝；④先宣判再在 finally 收尾，但 `app.exit()` 立刻终止进程 finally 永远跑不到 → `finish()` 先收干净再宣判。
- 环境坑（非代码）：**本 shell 默认继承 `ELECTRON_RUN_AS_NODE=1`**，本轮一度所有 Electron smoke 316ms 内全 EXIT=1（`app` undefined）。所有 electron 命令统一加 `env -u ELECTRON_RUN_AS_NODE`。
- ⚠️ **不宣闭环的硬欠账**（对齐大王第七轮批评）：①**桌面端审批 UI 尚未渲染**——只有主进程接缝，渲染器没界面，E2E 的"用户批准"是脚本代表用户直接调函数，真实用户目前无法点批；②面板模式**仅支持 extract / goto**，click/fill_form/press_key/wait_for/tabs 明确 `ok:false` 且写明"未回退"；③后端 `AgentConfirmation`（schema.prisma:1556）与"面板确认单"是**两套并存机制**，互不同步，面板 actionId 没落库；④默认 off，线上执行路径零变化；⑤Windows 真机 smoke 阶段 1~5 全未跑。
- 报告 `docs/browser-panel-baseline/round5-report.md`；skill `browser-panel-stage-verification` 已同步（回归 7→12 条、坑 4→6 条、阶段表与欠账更新）。

## 15:35 自动化：错误报告检查（模式 B）
- **无新错误报告**（09-02/09-03 目录均 0 条）；无需修复、无 commit、未发版。
- 顺带核查历史：09-01 唯一 1 条 getSetupStatus 500（本机旧 bundle v0.0.1）已由 commit 36797553 修复；08-31 刷屏 4335 条为另一 host（192.168.1.175）SQLite 本地库损坏（database disk image is malformed）→ /api/kaypal/* 全 500，环境问题非代码 bug，该 host 已停报。
- 沉淀复用工具 `scripts/oss-error-report-scan.cjs`（dirs/list/get），替代每次手写。

## 15:45 浏览器面板第六轮收官（1A 2A 3A 全落地，commit 4433bb06）
- ③灰度开关全链路：desktop `browser-panel-mode-registry.js`（0600+protocol+7天老化+pid探活）→ manager setAgentMode（**必须 _emitState 广播，stage7 真机抓的 bug：不广播=点两下开两次**）→ IPC `browser-panel:toggle-agent-mode`（stripOnly）→ 控制条紫色按钮；backend `readPanelModeRegistry`（1s缓存+fail-closed）+ `readAgentPanelMode` 优先级 env 显式 > 文件 > 默认 off（非法 env=invalid 不猜）。
- 测试新增：backend panel-confirmation-merge 13 条、executor.panel +6（③优先级）、bridge spec +22（②落库12+③registry10，合计42）；desktop mode-registry 16、manager 17。全量：backend 26 suites/465 tests、desktop 9 spec/143 条、真机冒烟 stage1/2/5/6/7 = 11+9+11+9+7 全过；tsc EXIT=0；git diff --check 干净。
- 提交 4433bb06（23 文件，逐文件 add，并发方 growth/discovery/rpa/project-paths/ai-employee/frontend ~16 文件全程未触碰）。
- 交底：3011 跨进程读真实 userData 未真机验证（两侧常量靠测试对齐）；开关删除后 1s 缓存窗口（闸门在，无风险）；click/type/press_key 映射已给、桥未开通；jest teardown 泄漏既有。
- skill `browser-panel-stage-verification` 已更新：回归命令 16 条（+mode-registry/approval-overlay/stage6/stage7）、坑位扩到 10 个（+改状态必广播/并发编辑先读现状增量补齐/hydrate 就地突变→工厂函数/makeLoop 前向引用）、欠账区重写、补实现索引段。

## 浏览器面板·第七轮（阶段 7：click 动作接通面板桥，commit e62b17a7，9 文件 +997/-10）
- 大王指令"先开发"（在"接 click"与"补 3011 跨进程验证"中选先开发）。全链路落地：
- broker（desktop/browser-panel-broker.js）：**一次批准=一次逻辑点击**——pressed 消耗单后登记 `{panelId,x,y,expiresAt+10s}`，released 走 `_consumeClickPair`（**先 delete 烧单再校验**：同面板/坐标≤4px/10s 内/一次性，fail-closed）。released 携带完整新单仍可走 _consumeApproval（stage1 smoke 双单基线依赖，有意保留）。
- executor（backend）：`AgentBrowserExecuteInput.actionId`（loop 锁定单透传）+ `clickViaPanel`：无单→先 probe（元素不存在不签单，用户不看到死卡片）→签语义级单（selector+targetText，不带坐标）；approved→markApproved→**执行时重新 probe 解析坐标**→pressed+released 同单；rejected→终态收口；执行时元素消失→显式失败不盲点。`buildSelectorProbeExpression`（text= 精确→包含两轮匹配、可见元素过滤、JSON.stringify 防注入、仅主 frame）。
- **修真断链**：loop `executeWithRetry` 加第 7 参 panelActionId ← lockedConfirmationId——此前锁定单从未传 executor，重试会再签新单死循环（C3 未暴露因 executor 是桩）。
- 坑：AiBrowserAction 类型**没有 fill_form**（输入动作叫 type）；`export function` 不能写进 class 内。
- 测试：backend 26 suites/**470**（executor.panel 29、broker 29、loop spec 补 actionId 断言）；desktop 9 spec/**148**；冒烟 stage1/2/5/6/7 = 11+9+11+9+7 + **stage8（新）6/6**（S3 单单一次点击计数恰好+1、S4 偏移拒+烧单、S5 无单拒、S6 配对一次性）；tsc EXIT=0；diff --check 干净。
- 提交 e62b17a7（逐文件 add，并发方 ~40 文件未触碰）。报告 round7-report.md。
- 交底：probe 仅主 frame；stage8 probe 表达式是同构拷贝非源码单源；3011 跨进程 + Windows smoke 欠账照旧；loop 门禁判断靠 message 字符串匹配（脆弱）。
- 下一步：type → press_key/wait/tabs → extract_text。

## 浏览器面板·第八轮（阶段 7 续：type 动作接通面板桥，commit 066bb88a，7 文件 +787/-19）
- 大王"继续"= 按文档 §4 顺序接 type（输入）。全链路落地：
- broker：确认单 method 指纹 `panelMethodForAction('type')='Input.insertText'`（loop 按此比对，type 单必须签 insertText 否则闸门不放行）；`_consumeApproval` 加第 5 参 params 支持 **method 组匹配**——insertText 型单可被 `dispatchMouseEvent + params.type==='mousePressed'`（聚焦半步）消耗，**仅此一种**；released 借道被负向用例拦死。`_consumeClickPair` 复用为通用配对：insertText 配对**免坐标**（聚焦时已带坐标消耗单），但 **released 无坐标 fail-closed 拒绝**（堵住借 insertText 免坐标通道绕过偏移校验的洞）。先烧单再校验纪律不变。
- executor（backend）：`typeViaPanel`——无单→probe（元素不存在不签单）→签语义级单（label 输入文本 + selector + textPreview 40 字符截断）；approved→markApproved→重新 probe 聚焦坐标→pressed（消耗单）+ insertText（配对免坐标），同单同参；rejected 终态；元素消失显式失败。文案改"当前仅支持 extract / goto / click / type"。
- **首版真 bug（spec 抓出）**：组匹配漏限 params.type，released 也能消耗输入单 → `_consumeApproval` 加 params 参数限定 `type==='mousePressed'`。
- **insertText 语义交底（stage9 真机实证）**：CDP insertText = 拟真键入（input 事件链完整，React/Vue onChange 收得到）、**不清空已有内容**（光标处追加）；真实点击聚焦会把光标放到点击位置，插入位置随点击落点。旧无头路径 Playwright fill() 是"清空后设值"——"替换"语义上层需先清空，后续议。
- 测试：backend 26 suites/**476**（executor.panel 35、broker 34）；desktop 9 spec/**153**；冒烟 stage1/2/5/6/7/8 + **stage9（新）6/6**（S3 聚焦+insertText→field 值 + `#userops='input:...'` input 事件链实证、S4 无单拒、S5 配对一次性长度守恒断言、S6 released 借道拒）；tsc EXIT=0；diff --check 干净。
- stage9 首跑 4/6 两条是**冒烟脚本断言错**（读 `__panelProbe()` 不存在的 userops 字段→改 Runtime.evaluate 直读；S5 断言忽略"光标随点击位置插入"的正确拟真行为→改长度守恒断言），非产品 bug。
- 提交 066bb88a（逐文件 add，并发方未触碰）。报告 round8-report.md；skill 已更新（进度表 4.7/4.8、回归命令 +stage8/stage9 共 14 条、broker 34、click 配对纪律扩为含 type 三条新纪律、欠账区 + 清空语义未做 + Windows smoke 1~9）。
- 交底欠账照旧：probe 仅主 frame；probe 表达式双端两份非单源；insertText 无清空/替换语义；3011 跨进程真实 userData 验证；Windows 真机 smoke（1~9 全未跑）；loop 门禁 message 字符串匹配。
- 下一步：press_key（dispatchKeyEvent 的 keyDown/keyup 两步，需同款配对或单单放行设计）→ wait / tabs → extract 增强。

## 浏览器面板·第九轮（阶段 7 续：press_key 动作接通面板桥，commit cd17b831，15 文件 +1844/-13）
- 大王"继续下一步"= 按文档 §4 顺序接 press_key。全链路落地，**loop 零改动**（panelMethodForAction('press_key')='Input.dispatchKeyEvent' 映射早已存在，两阶段锁定/断链透传全泛化复用）。
- broker：keyUp 进配对通道（isPairableFollowUp +keyUp）；keyDown 成功登记按键配对（`kind:'key'`，键位非空才登记）；`_consumeClickPair` **通道隔离**——keyUp 只认按键配对且键位一致，insertText/mouseReleased 借按键配对、keyUp 借鼠标/输入配对四个方向全被负向用例拦死（先判 type==='keyUp' 再判 kind，顺序别反）。
- executor：`pressKeyViaPanel`——无单签语义级单（label 按下按键+key，无 selector 不探测）；approved→keyDown 消耗单+keyUp 配对同单同键位；rejected 终态；pending 停留。**可打印单字符 keyDown 补 text**（CDP 无 text 不触发文本插入，对齐 Playwright 拟真键入）；"暂不支持"文案更新。
- **真坑（值得记住）**：CDP `Input.dispatchKeyEvent` 不带 `text` 时 Chromium 只发 keydown/keyup 事件、不插入文本——可打印键必须补 text，否则"按了但没输入"的假成功。
- 设计确认：loop `isWriteAction` 对 press_key 只认 Enter/Tab（allowWrite 门禁窄语义，有意保留）——非 Enter 键不被 allowWrite=false 阻断，但 broker 侧 dispatchKeyEvent 是 MUTATION，面板模式任何按键都要确认单（fail-closed 兜底无洞）。
- 测试：backend 26 suites/**481**（broker 39、executor.panel 40）；desktop 9 spec/**158**；冒烟 8 套全过：stage1/2/5/6/7/8/9 + **stage10（新）6/6**（Enter keydown 事件链实证 #keylog、可打印字符 'x' input 事件链、无单拒、一次性+键位不匹配拒+烧单）；tsc EXIT=0；diff --check 干净。stage10 首跑即 6/6（无脚本断言返工）。
- 提交 cd17b831（逐文件 add，15 文件；并发方 discovery 两文件未触碰）。证据滚动清理只留 09:3x 最新一轮。报告 round9-report.md；skill 已更新（进度 4.9、回归 16 条、⑧按键纪律 3 条、欠账 +keyCode 未合成/组合键不支持 + Windows 1~10）。
- 交底：windowsVirtualKeyCode 未合成（keyCode=0，个别页面逻辑收不到）；组合键（Ctrl+C 等）需多步编排不支持；欠账照旧（probe 仅主 frame/双端两份/insertText 无清空/3011 跨进程/Windows smoke 1~10/loop message 匹配）。
- 下一步：wait / tabs（无 CDP mutation，设计待定）→ extract 增强 → 3011 跨进程验证 / Windows smoke 补账。

## 浏览器面板·第十轮（阶段 7 续：wait 免审批 + extract 定向提取，commit 7b6dd3d3，5 文件 +654/-30）
- 大王"继续"= 推进第十轮：wait + extract 定向增强。**backend-only**（broker/desktop 运行时零改动），支持列表扩至 6 动作（extract/goto/click/type/press_key/wait），仅剩 tabs/screenshot。
- wait 免审批设计：无 CDP 副作用（不动页面、无白名单命令）→ 不签确认单（"等待 N 毫秒"审批卡是骚扰），身份/面板校验仍走 executeViaPanel 前置；`clampPanelWaitMs`：floor、非法/负数→0、上限 PANEL_WAIT_MAX_MS=30_000（对齐旧无头 Math.min），防天文数字卡死会话状态机；off 透传失败不变。
- extract 语义替换：旧 observe textSample 整页快照 → `extractViaPanel` 定向提取（对齐旧无头 locator.first().textContent() → trim → slice(0,2000)）；`buildTextExtractExpression`：text= 精确→包含两轮+可见元素过滤（probe 同构）、CSS querySelector first 语义；未命中 found:false → 显式失败不回退。loop 的 observe() 走 playwright-mcp 独立路径，与 extract 解耦（替换无影响）；loop 零改动。
- **语义差异交底**：extract 候选带可见性过滤——不可见元素提取不到（旧无头 locator 不要求可见）；当前以"probe 同构、显式失败不回退"为准则接受，真实页面踩到再议。**债扩大**：probe/extract 表达式双端两份非单源（backend 源码 vs stage8/9/11 冒烟拷贝）。
- **坑（spec 抓出 2 个）**：① ts-jest 未开 --experimental-vm-modules，测试动态 import 报错 → 纯函数用例改文件头静态 import；② extract 从 observe 改 execute 后 PanelBridgeError 用例旧桩缺 execute → 桩同步改（报错类型 POLICY_DENIED→PANEL_ACTION_FAILED 才发现）。
- 测试：backend 26 suites/**485** + tsc EXIT=0；desktop 9 spec/**158**（本轮 broker 未改，收尾跑一遍确认无波及）；冒烟 9 套 71 项全过：stage1/2/5/6/7/8/9/10 + **stage11（新）6/6 首跑全过**（CSS 提取 #title、text= 精确、text= 包含、未命中 found:false、3000 字符→恰 2000 截断+remove 无残渣）。diff --check 干净。
- 提交 7b6dd3d3（逐文件 add，5 文件；并发方 growth/discovery/frontend 文件全程未碰）。报告 round10-report.md；skill 已更新（进度 4.10、回归 17 条/合计 71、支持列表 6 动作、wait 免审批+extract 语义差异纪律、欠账 +tabs 难点+Jest 动态 import 坑+Windows 1~11）。
- tabs 设计难点（本轮交底，未做）：需扩展 CDP 白名单 Target 域（createTarget/closeTarget）+ 破坏 panelId→单 webContents 绑定前提（manager 多 view 模型），单独一轮做。
- 交底欠账照旧：keyCode 未合成/组合键不支持/insertText 无清空/3011 跨进程真实 userData/Windows smoke 1~11 全未跑/loop 门禁 message 字符串匹配。
- 下一步：tabs（Target 域白名单+manager 多 view，单独一轮）→ screenshot → 3011 跨进程验证 → Windows smoke 补账。

## 第十一轮：tabs(new/switch/close) 接通面板桥（完成，提交 fe439888）
- 设计定案：**放弃 CDP Target 域**（broker 通道是 webContents.debugger target 级 session，Target.createTarget browser 级发不出去，probe 实证）→ **Panel.tabs 主进程伪 method** + manager 原生台账 `_panelTabs/_activeTabIndex`，broker 经注入 tabsHandler 回调 tabsOperation；panelView 恒=active tab 视图，navigate/goBack/relayout 零改动自动生效。
- 语义：new=空白页+置 active（**必须 loadURL('about:blank')**——否则 getURL 空串导致 binding 回落陈旧 URL，stage12 实证）；switch 也签单、越界显式失败（收紧旧无头静默 fallback）；close 缺省关 active、最后一个不可关；一次批准=一次操作；执行后 fresh target 重解析。后台 tab 事件不污染 session（push 加 active-only 判断）；hide() 遍历台账。
- 改动：desktop manager/broker/wiring/server + backend executor/panel-bridge + 3 个 spec（manager 24 / broker 44 / wiring 13；executor.panel 44 + merge 17）+ stage12-smoke.mjs（新，8 项）。
- 排障三轮收敛：S4 = mutation done.target 是**执行前** binding（断言改锁语义 about:blank）；S5 "No target available" = smoke 裸调 debugger.sendCommand 未 attach 换绑后的新 active（harness 补 isAttached 检查，产品链路无此问题）；probe 教训 = Electron 下 ESM 加载错误挂住不退出 + 同步返回值调 .catch FATAL。
- 测试：backend local-engine 26 suites/**489** + tsc EXIT=0；desktop 9 spec/**172**；**stage12 8/8**（真实 manager+wireBrowserPanel+双页 fixture，跨 tab 同页控制成立）。backend 全量 3 个失败（douyin/growth/local-bridge）非本线，已交底。
- 提交 fe439888（逐文件 add，14 文件 +1217/-25；并发方文件未碰）。报告 round11-report.md；skill 已更新（进度 4.11、回归 18 条 smoke 合计 79、desktop 172、支持列表 7 动作、Panel.tabs 三纪律、binding 语义/attach/ESM 挂死坑）。
- 交底欠账（本轮新增 2 笔）：控制条 tab 条 UI 未做（用户无法手动切 tab）；windowOpenHandler 未动（window.open 仍 deny）。累计欠账照旧：keyCode 未合成/insertText 无清空/probe+extract 表达式双端两份/3011 跨进程真实 userData/Windows smoke 1~12 全未跑/loop 门禁 message 字符串匹配。
- 下一步：screenshot（Page.captureScreenshot 已在白名单，补执行器路由+二进制落盘语义）→ 3011 跨进程验证 → Windows smoke 补账。

## 第十二轮：screenshot 接通面板桥——阶段 7 八动作全通（完成，提交 8804d904）
- 设计：**readonly 免单**（同 extract/probe/wait 先例）——Page.captureScreenshot 早已在 desktop 白名单 READONLY_METHODS，`panelMethodForAction('screenshot')` 保持 null（merge spec 原有锁定），**desktop 产品代码零改动**。
- executor `screenshotViaPanel`：execute(Page.captureScreenshot {format:png}) → result 加 `screenshotBase64` 专用字段；message/日志/事件流只报字节数**绝不携带 base64**（stage13 S7 实证事件流只带 method+target）；空数据/缺失显式失败不回退。
- 敏感度交底：截图是全页视觉（可能含侧边聊天/自动填充），敏感度高于定向 extract，按 extract 先例免单；「敏感页提示」增强记欠账；screenshotBase64 上层消费（落盘/多模态观察）未接——旧无头 screenshot 本就是空操作。
- stage13 冒烟三轮收敛：①S8"未登记"= S7 先 destroy 销了会话（场景顺序错）→ 互换 S7/S8（事件流校验提到销毁前）；②S7 仍挂 = smoke 硬编码 panelId='panel-1' → 改 manager.session.panelId；③8/8。
- 测试：backend local-engine 26 suites/**491**（489+2）+ tsc EXIT=0；desktop 9 spec/**172**（零波及）；**stage13 8/8**（免单直执行/PNG 魔数/尺寸/goto 后 fresh binding/无台账副作用/事件流无 base64/销毁后 fail-closed）。
- 提交 8804d904（逐文件 add，5 文件 +533/-6；漏删一份中间失败证据已补删，滚动保留最新）。报告 round12-report.md；skill 已更新（进度 4.12、回归 20 条 smoke 合计 87、backend 491、8 动作全通、screenshot 免单纪律）。
- **阶段 7 支持列表收官：extract / goto / click / type / press_key / wait / tabs / screenshot 全通。**
- 累计欠账：控制条 tab 条 UI 未做；windowOpenHandler 未动；screenshotBase64 上层消费未接；probe/extract 表达式双端两份；3011 跨进程真实 userData 验证；Windows smoke 1~13 全未跑；loop 门禁 message 字符串匹配；keyCode 未合成；insertText 无清空。
- 下一步：3011 跨进程真实 userData 验证 → 控制条 tab 条 UI → Windows smoke 补账。

## 第十四轮：3011 跨进程真实 userData 验证——抓到 P1 断链并修复（完成，提交 1612bcfd）
- **P1 实锤**：打包版 Info.plist CFBundleName='JIUZHANG AI 内容创作平台' → macOS 打包版默认 userData 落中文目录，而 backend `resolveDesktopUserDataDir`（project-paths.ts）darwin 硬编码 `ai-content-desktop` → 面板模式开关/面板桥凭据两条跨进程链在生产 macOS 全断。dev 版 name 恰好一致故测试从未暴露；`JiuZhangAI` 目录是九章管家的（排查干扰项，已排除）；Windows 既有 configureStableUserDataPath 已兜住。
- **修复**：新增 desktop/user-data-path.js 纯函数（win 语义原样平移 + darwin 打包版固定 ai-content-desktop + migrateFrom 决策）+ main.js 接线（macOS 老数据一次性 rename 迁移，stable 已存在则不迁，失败不阻塞不丢数据）+ user-data-path.spec.js 11 条；backend 零改动。
- **stage14 真机写读闭环 10/10 首跑全绿**：manager.setAgentMode（真实 writeMode 0600+pid）→ backend **dist 真实编译产物** readPanelModeRegistry 经 HOME 推导读同一目录（临时 HOME 隔离，零 env 覆盖零生产污染）；pid 探活/8 天老化/0644 收紧 0600/1s 缓存窗口/目录对称性全实证。跑前需 nest build 刷 dist（旧 dist 缺 resolveDesktopUserDataDir）。
- main.js 入口真实启动冒烟（铁律 2.5）：22s dev 启动零 FATAL、零 [user-data] 异常；3011 副本 EADDRINUSE 退出=既有预期（端口被真实 3011 占）。
- 回归：desktop 10 spec **183**（172+11）；backend local-engine+common 32 suites **522** 全绿。
- 提交 1612bcfd（逐文件 add，6 文件 +674/-24；先核对 main.js diff 只含本轮改动防裹挟并发线）。报告 round14-report.md；skill 已更新（进度 4.14、回归 21 条 smoke 合计 97、desktop 183、backend 522、欠账划掉+新增打包版 userData 通用坑）。
- 遗留交底：darwin 打包分支真机打包验证未做（下次发 Mac 包时实测迁移+面板链）；迁移 rename 一次性。
- 下一步：控制条 tab 条 UI（round11 遗留）→ Windows smoke 1~14 补账。

## 第十五轮：控制条 tab 条 UI——用户手动切/关 tab（round11 遗留闭环，提交 b03b7ddc）
- 设计：多 tab 时控制条两行（tabbar 26px + 工具行 40px=66px），**单 tab 不渲染、高度不变**（零干扰）；active 紫 #722ed1 高亮 + ✕ 关闭（最后一个不显示）；publicState 新增 tabList（title 截 40 字符/url）驱动渲染。
- 通道：`browser-panel:switch-tab/close-tab`（**stripOnly** 门禁，前端 3010 不得替用户操作）；manager `switchTabByUser/closeTabByUser` 复用 tabsOperation 语义校验，错误转 {ok:false} 不抛。**用户操作不走 Agent 审批闸门**（审批只约束 Agent）。
- manager：_stripHeight() 动态高度 + relayout 动态值；close 后台 tab 分支补 relayout（strip 高度随 tab 数变）；page-title-updated 事件 active-only（后台标题变化不广播）。
- Agent 链与用户点击同一台账：Agent new/goto 后 tab 条同步出现/高亮（stage15 S2/S5 实证）；用户切 tab 后 Agent 动作落在新 active（既有 panelView 模型语义，交底）。
- stage15 三轮收敛：①首跑 S3~S7 全挂 = **漏注册 registerBrowserPanelIpc**（strip invoke 无人接，"No handler registered"）——新冒烟带 IPC 层是 stage6/7 既定姿态，别再漏；②S6 断言修正：handler 内转错误对象 → stripOnly 包装 success:true + result.ok=false（比预设更正确）；③S7 用 getBounds() 非 fake 的 _bounds。8/8。
- 回归：desktop 10 spec **187**（manager 28 +4 ⑮）；backend 零改动未波及。
- 提交 b03b7ddc（逐文件 add，8 文件 +734/-16）。报告 round15-report.md；skill 已更新（进度 4.15、回归 22 条 smoke 合计 105、desktop 187、tab 条欠账闭环）。
- 累计欠账：windowOpenHandler 未动 / tab 条无拖拽重排 / screenshotBase64 上层消费 / 表达式双端两份 / **Windows smoke 1~15 补账** / loop 门禁 message 匹配 / keyCode / insertText 清空 / darwin 打包分支真机打包验证。
- 下一步：Windows smoke 1~15 补账（jz-win11-*）→ Mac 打包时验证 darwin userData 分支。

## 第十六轮：真实任务全链（「你妈的。你去测」闭环）
- **P1-A 破案**：3011 登录 500 `this.syncTenantOrgTables is not a function`——根因是 PrismaService 自定义 Proxy 的 TARGET_ONLY 白名单漏登新方法，proxy 把 `this.syncTenantOrgTables` 路由到账号库 PrismaClient（无此方法）；相邻行 heal 在白名单内所以成功（一成一败之谜）。堆栈帧 `Proxy.ensureAccountDatabase` 是决定性线索。修复+护栏用例（静态扫控制面方法体 this.* 全登记）→ 大壮已提交 `8aef0c14`。
- **P1-B**：面板桥 `Page.captureScreenshot` 3s 全局超时打不住大 payload base64 → 截图必失败。差异化超时（截图 10s/其余 3s），桥 spec 43 全绿，负向验证过。提交 `2473d6fd`。
- **stage16 真机冒烟 7/7**：真实任务全链（登录→dom-agent 建会话→桥 binding 0600 落盘→run「提取 h1，然后截图」→succeeded→凭据清理）。新脚本 desktop/scripts/browser-panel-stage16-smoke.mjs。
- **运维事实**：dom-agent 灰度=AGENT_BROWSER_MODE=dom-agent（3011 生产保持 legacy，3013 隔离实例验证）；面板模式=userData browser-panel-mode.json（pid 探活 fail-closed）；desktop spec 跑法=纯 node 直跑（desktop 无 jest，npx 缓存版全挂）；kaypal.cn 本机证书被代理劫持。
- **构建事故交底**：重建 bundle 产物曾坏（playwright external 缺失/client 变 postgres），sync 进 runtime 致 3011 crash loop，靠源码树 00:20 产物恢复。教训：sync:runtime-bundle 前必须验产物。另有并发构建（00:19~00:22 三份备份，疑大壮会话）。
- 回归：desktop 10 spec 187 全绿（纯 node）/ backend local-engine+prisma 502 全绿。
- 下一步欠账：Playwright 引擎 isAlive 假阳性待查；sync 验机固化；Windows smoke 1~16 补账。

## 第十七轮：文档对照收尾（大王批准按建议执行）
- 文档对照结论：阶段 0~4 闭环，未干=阶段 5 平台迁移/阶段 6 打包跨平台/阶段 7 发布门禁。
- 本轮干掉 5 项：①3011 生产灰度开 dom-agent（env.secure 追加+重启，真机 3/3 succeeded，env 备份 .bak-20260904）；②sync 验机两道闸+自动回滚 `b5742ac4`；③type 替换式语义（清空后输入）`1ec382dd`；④面板截图证据落盘同 evidence 链 `afb29de1`；⑤popup 转受控 tab 不外逃 `929cafe5`。
- 回归：desktop 10 spec 189 全绿、panel spec 52、桥 spec 43、tsc 零错。
- 待办大战役：Windows smoke 1~18 补账（云电脑，发版 gate）、阶段 5 平台迁移（general-web→小红书）、Mac 打包 darwin userData 验证。3011 灰度已开=现在整链可用。
