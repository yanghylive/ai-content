# kaypal.cn 官网首页修复方案

> 日期：2026-08-30 · 状态：**待决策，未动代码**
> 涉及仓库：`github.com/yanghylive/kaypal-ai`（main 分支）
> **硬约束（大王 2026-08-30 明确）：方案不得影响技术底座**
> 「技术底座」= **用户、订阅、积分、治理** 四条核心业务链路。
> 展示站（官网前端）本身不属于底座，可以动。
> 本方案对四条链路**全部零改动**，详见第四章评估。

---

## 一、一句话结论

kaypal.cn 打不开官网，**不是官网前端被删了，是根路由入口被两次提交改成了 redirect**，最后一跳的落点需要登录。落地页组件本身完好无损，把它重新渲染回来即可。

---

## 二、问题定位

### 2.1 线上实测（2026-08-30）

| 访问路径 | 实测结果 |
|---|---|
| `https://kaypal.cn/` | 307 → `/zh-CN` |
| `https://kaypal.cn/zh-CN` | 200，但 HTML 里带 `meta refresh → /zh-CN/dashboard/apps` |
| `https://kaypal.cn/zh-CN/dashboard/apps` | 200 → `meta refresh → /zh-CN/auth/login?returnUrl=...` |
| 最终游客看到 | **登录页** |
| `https://vip.kaypal.cn/` | 同样跳 `/dashboard/apps`（同一份代码） |
| `https://enterprise.kaypal.cn/` | 跳 `kaypal.cn/zh-CN/auth/login`（正常，企业版要登录） |
| `https://opensource.kaypal.cn/` | 正常（独立站点，不受影响） |
| `https://kaypal.cn/zh-CN/pricing` | 200 正常，说明营销页体系还在 |

### 2.2 根因：`src/app/[locale]/page.tsx` 的两次改动

| 时间 | 提交 | 改动 | 影响 |
|---|---|---|---|
| 04-22 前 | `5cfd4d37` | `return <PublicHomeLanding locale={locale} />` | ✅ 官网正常 |
| 08-09 | `f8c591b8` | 改成 `redirect('/applications')` | ⚠️ **官网消失**。提交信息写的是「**vip** 根路径直达应用页」，但改的是**全局** `[locale]/page.tsx`，未做域名判断 |
| 08-18 | `aaf9d265` | 改成 `redirect('/dashboard/apps')` | ❌ 应用频道下架，新目标页需登录 → 游客被踢到登录页 |

**核心错误**：`kaypal.cn`（官网域名）和 `vip.kaypal.cn`（应用门户域名）共用同一个 `[locale]/page.tsx`。当初为了 vip 域名的需求，直接改了全局根路由，官网被"顺手"带走了。

### 2.3 好消息：落地页代码完好

| 文件 | 状态 |
|---|---|
| `src/frontend/components/mission-home/PublicHomeLanding.tsx` | ✅ 在（331B，转发层） |
| `src/frontend/components/workspaces/home/PublicMissionFrontdoor.tsx` | ✅ 在（25KB / 666 行，完整官网） |
| `src/frontend/content/public-site.ts` | ✅ 在（533 行品牌文案） |

落地页链接全部指向现存路由（`/marketplace`、`/pricing`、`/enterprise`、`/help`、`/about`、`/auth/login`、`/auth/register`），**无死链**，未引用已下架的 `/applications`。

官网主文案（节选）：
- Hero：「让重要的事，有一个值得信赖的伙伴」
- 副标：「让 AI 真正交付结果，而不是只会回答问题。」
- 企业版：「在可治理的前提下，让 AI 进入真实业务执行」

---

## 三、影响面：不只是"看不见"

### 3.1 新访客转化断了
官网是获客入口。现在任何人（含潜在客户、投资人、合作方）打开 kaypal.cn 只看到登录框，第一印象等于"这是个内部系统"。

### 3.2 正在持续丢 SEO（这条最严重）
`src/app/sitemap.ts` 的 `PUBLIC_PATHS` 里**明确声明 `'/'` 是公开页**：

```ts
const PUBLIC_PATHS = ['/', '/about', '/pricing', '/support', '/enterprise', ...]
```

但爬虫访问 `/zh-CN` 会被 redirect 到需登录的 `/dashboard/apps`，**抓不到任何官网内容**。sitemap 声明与实际行为矛盾 → 首页从搜索结果里掉出、收录被降权。8-09 至今已持续三周。

### 3.3 已登录用户也在被误伤
已登录用户访问 kaypal.cn，同样被送进 `/dashboard/apps`，而不是官网。恢复后行为会反转（见风险 6.1）。

---

## 四、修复方案

### 方案 A′：页面内按域名分流（**推荐**）

> 已按「不动技术底座」的要求重做。**不再触碰 `src/middleware.ts`**，host 判断下沉到页面组件内部。

判断逻辑写在 `src/app/[locale]/page.tsx` 一个文件里：读 host → 是 vip 域名就 `redirect('/dashboard/apps')`，否则渲染官网落地页。

| 域名 | 修复后行为 |
|---|---|
| `kaypal.cn` / `www.kaypal.cn` | → 官网落地页 `PublicMissionFrontdoor` |
| `vip.kaypal.cn` | → `/{locale}/dashboard/apps`（保持 8-18 定下的"打开即应用中心"） |
| `enterprise.kaypal.cn` | → 不变，仍走 `/chat/v3` |

**优点**
- 官网回归 + vip 需求都保住
- **只改 1 个业务页面文件**，middleware / nginx / 网关 / 认证 / i18n / 容器 / DB 全部零改动
- 不引入任何新依赖、新机制、新共享模块
- 回滚成本极低：单文件 revert 即可

**代价**
- 需读一次 `headers()` 取 host（约 1 次字典查询，可忽略）
- host 读取失败时有兜底（见下）

### 方案 B：全部回退官网

所有域名根路径一律渲染落地页，删除 vip 直达逻辑。
**优点**：改动同样只有 1 个文件，且逻辑更简单。
**代价**：放弃 8-18 你定的 vip 直达应用中心，vip 域名也变官网。

### 方案 A′ 的具体改动（唯一一个文件）

**`src/app/[locale]/page.tsx` — 全文替换**

```tsx
import { headers } from 'next/headers';
import { redirect } from 'next/navigation';
import { PublicHomeLanding } from '@/frontend/components/mission-home/PublicHomeLanding';
import { withLocalePrefix } from '@/lib/intl/paths';
import type { Locale } from '@/i18n/routing';

type Props = {
  params: Promise<{ locale: Locale }>;
};

/** 打开即应用中心的域名，不占用官网根路径。 */
const APP_LAUNCHER_HOSTS = new Set(['vip.kaypal.cn', 'www.vip.kaypal.cn']);

/**
 * 取值顺序与 middleware.getRequestHost() 保持一致，兼容 nginx 反代。
 * Next 15 起 headers() 为异步 API，此处与项目内 sitemap.ts 的 await headers() 写法一致。
 */
async function readRequestHostname(): Promise<string> {
  const headerList = await headers();
  const forwarded = headerList.get('x-forwarded-host') ?? '';
  const rawHost = forwarded.split(',')[0].trim().toLowerCase()
    || (headerList.get('host') ?? '').split(',')[0].trim().toLowerCase();
  return rawHost.split(':')[0];
}

/**
 * 默认渲染官网落地页；仅 vip 域名直达应用中心。
 * host 读取失败时兜底为官网——宁可漏跳 vip，也不能把官网访客送进登录页。
 */
export default async function HomePage({ params }: Props) {
  const { locale } = await params;

  if (APP_LAUNCHER_HOSTS.has(await readRequestHostname())) {
    redirect(withLocalePrefix('/dashboard/apps', { locale }));
  }

  return <PublicHomeLanding locale={locale} />;
}
```

### 核心业务链路影响评估（用户 / 订阅 / 积分 / 治理）

> 口径说明：以下按大王界定的「技术底座」= **用户、订阅、积分、治理** 四条核心业务链路逐项核对。展示站（官网前端）不在底座范围内。

#### 结论：四条链路全部零改动，仅治理侧有一个**只读旁支**需知会

| 核心链路 | 是否改动 | 依据 |
|---|---|---|
| **用户**（认证 / 账号 / OIDC） | ❌ 零改动 | 方案只动展示页组件；未触及 `src/lib/auth`、`getCurrentUser`、OIDC 流程；落地页不调用任何用户接口，游客访问不产生登录态变化 |
| **订阅**（subscription / 套餐） | ❌ 零改动 | 落地页不调用订阅接口，「价格方案」CTA 仅跳转已存在的 `/pricing` 页面 |
| **积分**（credits / 计费扣减） | ❌ 零改动 | 落地页无任何计费、积分查询或扣减路径 |
| **治理**（审批 / 预算 / 边界 / 规则） | ❌ 零改动 | 未触及任何治理规则、审批流、预算控制 |

#### 唯一交集：落地页埋点会碰一下 audit log（需你知会）

落地页组件内含 `FrontdoorViewTracker`（`src/frontend/components/mission-home/FrontdoorTracking.tsx`），会在页面浏览 / 链接点击时 `POST /api/telemetry/task-funnel`。

该端点行为（`src/app/api/telemetry/task-funnel/route.ts`）：

- **游客（未登录）**：`getCurrentUser()` 返回 null → **直接 204，不写库**
- **已登录用户**：写入一条 `resource: 'task_funnel'` 的 audit log

**线上实测（2026-08-30，游客身份无 cookie）**

| 请求 | 响应 | 说明 |
|---|---|---|
| `POST /api/telemetry/task-funnel` | **204** | 静默丢弃，**不写 audit log、不报错、不刷 401** |
| `GET /api/subscription`（对照组） | **401** | 证明网关的 required 策略确实在生效 |

网关侧该路由显式配置为 `auth: 'optional'`（`routes-config.ts` 第 722 行），且按声明顺序优先于 `requiredApiPrefixes`，游客不会被拦截。

**评估**
- 这不是新引入的行为，是落地页**原有设计**，04-22 之前官网在线时同样在跑
- 游客侧零写入、零错误
- 已登录用户侧只是往审计表追加事件记录，**不改任何治理规则，不影响审批 / 预算 / 边界**
- 若你不想让官网访问进 audit log，落地页不渲染 `FrontdoorViewTracker` 即可（一行改动，可选，不阻塞主修复）

#### 附带确认：其余基础设施同样零改动

| 组件 | 是否改动 |
|---|---|
| `src/middleware.ts`（认证 / 网关 / i18n / 企业版分流） | ❌ 零改动 |
| nginx / 反向代理 | ❌ 零改动（复用现有 `x-forwarded-host`） |
| 数据库 / Prisma schema | ❌ 零改动 |
| `PublicMissionFrontdoor` 落地页组件 | ❌ 零改动（仅复用） |
| 共享模块（`withLocalePrefix` 等） | ❌ 零改动（仅 import 调用） |
| 新增依赖 / 容器 / 部署配置 | ❌ 无 |

#### 性能影响：实测无回退

这是唯一需要留意的技术点，已用线上实测数据排除：

**线上实测（2026-08-30，`https://kaypal.cn/zh-CN` 响应头）**

```
cache-control: private, no-cache, no-store, max-age=0, must-revalidate
vary: rsc, next-router-state-tree, next-router-prefetch, ...
（无 x-nextjs-cache 头，无 age 头）
```

结论：**该路由当前已经是逐请求动态渲染，一次都不缓存**。原因是 middleware 在每个请求上都会写入 `NEXT_LOCALE` / `kaypal-locale` / `kaypal-region` / `kaypal-currency` 四个 cookie，Next.js 因此把整个路由标记为 dynamic。

所以新增一次 `headers()` 读取**不改变这个页面的渲染模式**，不存在"静态变动态"的性能回退——它本来就是动态的。落地页组件（666 行纯 JSX + 静态文案）SSR 一次的服务端开销很小。

---

---

## 五、风险交底（必须知道的坑）

### 5.1 已登录用户行为会反转 ⚠️
- **现在**：已登录用户访问 kaypal.cn → 直接进工作台。
- **改后**：已登录用户访问 kaypal.cn → 看到官网落地页。

落地页是**纯静态组件，没有登录态判断**，CTA 只有「登录」和「免费试用」。已登录用户也得手动点才能回工作台。

→ 建议（可选增量）：给落地页加登录态判断，已登录用户右上角 CTA 换成「进入工作台」。**此项需你确认是否要做，不做也不阻塞主修复。**

### 5.2 落地页内容可能有信息滞后
组件最后一次真实渲染是 08-09 之前，中间产品迭代过（九场、桌面版、企业版能力等）。文案骨架和链接都健康，但内容是否还准确需要你过一眼。**建议上线后你本人打开看一遍再决定是否调文案。**

### 5.3 vip 域名的游客行为不变
`vip.kaypal.cn` 改后游客仍会被踢到登录页——与现状一致，**不算回归**。如果你希望 vip 游客也看到点别的东西，另说。

### 5.4 SEO 恢复需要时间
修复后首页可正常抓取，但重新收录/排名回升通常要几天到几周，不是上线即恢复。

### 5.5 部署有短暂中断
生产跑在 Baota docker（`kaypal-app-baota` 容器），改完需重新构建镜像并重启，期间服务会短暂不可达。建议选低峰时段。

### 5.6 我尚未验证的事
- 未在真实浏览器里渲染过恢复后的落地页（只做了静态代码核查 + 线上跳转复现）
- 未确认生产容器跑的代码是否与 main 分支一致（是否存在服务器热改）。**这点部署前必须先核实**，否则可能出现"改了代码但线上没变"。

---

## 六、验证清单（部署后逐条过）

- [ ] `https://kaypal.cn/` → 200，title 为官网，HTML 内**无** `meta refresh` 跳 dashboard
- [ ] `https://vip.kaypal.cn/` → 302 到 `/zh-CN/dashboard/apps`
- [ ] `https://enterprise.kaypal.cn/` → 仍跳 `/chat/v3`（**不能**回归）
- [ ] 落地页内链全部 200：`/marketplace`、`/pricing`、`/enterprise`、`/help`、`/about`
- [ ] `https://kaypal.cn/sitemap.xml` 中首页 URL 可访问且返回官网内容
- [ ] PC + 手机两个尺寸渲染正常
- [ ] 构建 / 类型检查 / lint 全绿

---

## 七、www.kaypal.cn（按你决定：砍掉）

现状：`www.kaypal.cn` 的 SSL 证书域名不匹配，浏览器直接报"连接不安全"，访问失败。

两种"砍"法，建议选 (b)：

| 选项 | 做法 | 效果 |
|---|---|---|
| (a) 什么都不做 | 保持现状 | 用户手输 www 会看到红色安全警告，**观感差，像站点被黑** |
| (b) **删 DNS 解析**（推荐） | 去域名控制台删掉 `www` 的 A/CNAME 记录 | 访问直接"找不到服务器"，比挂个证书错误页干净 |

> 需你在域名注册商/解析控制台操作，我这边改不了 DNS。
> 另：如果哪天想要 www 能用，得补签一张覆盖 `www.kaypal.cn` 的证书，或在 nginx 加一层 www → 主域名的 301（前提是 www 也要有有效证书，否则 301 之前的 TLS 握手就先失败了）。

---

## 八、我的建议

结论：**选 A′，其余三项按下面执行。**

| # | 事项 | 建议 | 理由 |
|---|---|---|---|
| 1 | 方案选型 | **A′** | vip 直达是 08-18 你定的产品行为，没理由为修 bug 把它牺牲掉。A′ 只多 6 行判断，官网和 vip 都保住。B 更简单，但那是"为了省事改需求"，不划算 |
| 2 | 「进入工作台」入口 | **这次不做** | 已查证：已登录用户点官网右上角「登录」→ `LoginPage` 检测到 `user?.id` → 直接 `redirect` 回 dashboard（`resolveLoginReturnTarget` 默认 fallback 就是 dashboard 入口）。**路径是通的，只是多点一下**。加入口会把改动从「1 个文件」膨胀成「改落地页组件」，风险上升。建议官网跑稳一周看数据再定 |
| 3 | 官网埋点 | **保持开启** | 游客侧 204 静默，零成本；官网刚恢复，你最需要的恰恰是"多少人看、点了哪个 CTA"的数据，关掉等于蒙眼开车；已登录用户写 audit log 是产品原有设计，官网访问量远低于应用内操作量，增量可忽略 |
| 4 | www.kaypal.cn | **删 DNS 解析** | 挂证书错误页比解析不到更伤品牌——红色安全警告看起来像站点被黑 |

---

## 九、执行记录（2026-08-30 上午）

### 9.1 备份已完成 ✅

备份目录：`kaypal-prod-new:/www/backups/kaypal-predeploy-20260830`

| 项 | 内容 | 校验 |
|---|---|---|
| 数据库 | `kaypal_db.dump`（custom-format，290 表） | `78f230ef147839ac2cb9eaa5daed2222` |
| 应用镜像 | `kaypal-app-baota:rollback-20260830` | 由运行镜像 `a78396faaf16` 固化，可秒级回退 |
| 环境变量 | `.env.prod`（70 行） | `d5a407517dca8f164eb5ce111ed33cb7` |
| nginx 配置 | `nginx-conf.tar.gz` | `8016a37b3ef49390cdbbd3858a74a7a8` |
| 调用基线 | `api-calls-baseline.txt`（29 个接口） | 供部署后对比 |

底座表核对（290 张表全在）：`users`、`organizations`、`UserSubscription`、`SubscriptionPlanProduct`、`SubscriptionOrder`、`CreditReservation`、`ChannelCreditTopup`、`OrgCreditPool`、`AuditLog`、`UnifiedAuditLog`、`payment_order`、`payment_refund`。

`BACKUP_MANIFEST.md` 已随备份落盘，含完整回退命令。

### 9.2 外部应用调用面（已查清）

生产对外域名：`kaypal.cn`、`vip.kaypal.cn`、`enterprise.kaypal.cn`、`video.vip.kaypal.cn`（九场）、`aicontent.vip.kaypal.cn`、`api.kaypal.cn`、`knowledge/moodle/paperless/vikunja.vip.kaypal.cn`、`md.kaypal.cn`。

今日真实 API 调用 TOP（来自 nginx 日志）：

| 接口 | 次数 | 归属 |
|---|---|---|
| `GET /api/subscriptions/current` | 1504 | 订阅（底座） |
| `GET /api/auth/me` | 790 | 用户（底座） |
| `GET /api/billing/balance` | 756 | 积分（底座） |
| `POST /api/desktop-auth/password` | 16 | 桌面端 |
| `GET /api/auth/wechat/url` | 9 | 微信登录 |
| `POST /api/ai-content/knowledge/search` | 4 | ai-content |

**不影响的依据**：上述全部走 `/api/*`，middleware 里 `pathname.startsWith('/api')` 直接交给 `apiGatewayHandler` 返回，**永远到不了页面组件**。本次只改页面组件，零交集。
（日志中另有 `/api/.env`、`/api/.git/config` 等为扫描器垃圾流量，非业务调用。）

`api.kaypal.cn` 走 `127.0.0.1:3100`（独立 cloud-api 服务），`kaypal.cn/cloud-api/` 在 nginx 层即转走，均不经过 Next.js 应用。

### 9.3 生产基线核查（两个意外发现）

**发现一：生产落后本地 HEAD 1 个提交**

生产运行 `dbfdccc6`（08-26 11:43 部署，镜像 `a78396faaf16`），本地 main HEAD 是 `5b173366`。差异为：

```
src/lib/gateway/routes-config.ts      |  9 +++++++++   ← 订阅链路放行（底座）
src/lib/gateway/routes-config.test.ts | 21 +++++++++++
```

即 **`/api/pricing/subscribe` 的 edge 放行修复尚未上生产**。若直接拿本地 HEAD 部署会夹带底座改动。

→ **处置**：修复分支基于 `dbfdccc6` 创建，只带 `page.tsx`，保持生产现状 + 本次修复，零夹带。gateway 那个修复留给大王单独决定何时上。

**发现二（已纠正）：我一度误判为"服务器热改"**

`/www/wwwroot/kaypal-ai-main-build/src/app/[locale]/page.tsx` 是**渲染落地页**版本，与 Git 不一致，我初判为热改。进一步核查：**该目录所有文件 mtime 统一为 2026-07-27 12:12:15，08-26 之后零改动**，是 07-27 的废弃旧目录（真正的构建目录是 `/tmp/build-app-<tag>`，符合部署 skill 的描述）。

最新构建目录 `/tmp/build-app-card3-1926`（08-26 19:27）的 `page.tsx` = **redirect 版本，与 Git 完全一致**。

→ **结论：生产没有热改，运行代码与 Git 一致。** 上述误判已纠正。

### 9.4 一条决定性证据：项目规约测试

`src/lib/experience/locale-homepage-mainline-proof.test.ts` 明确断言首页**必须**渲染落地页：

```ts
expect(source).toContain("import { PublicHomeLanding }");
expect(source).toContain('return <PublicHomeLanding locale={locale} />;');
expect(source).not.toContain("import { getCurrentUser }");
```

即 08-09 的改动**违反了这个规约并把测试打挂**；本次修复正好让规约恢复。

顺带印证第 2 项建议：该规约明确 `not.toContain('getCurrentUser')`，**首页不应做登录态判断**——所以「进入工作台」入口本来就不该加在这里。

### 9.5 本地验证结果

| 检查 | 结果 |
|---|---|
| `tsc --noEmit -p tsconfig.preflight.json` | ✅ 退出码 0，0 错误 |
| `locale-homepage-mainline-proof.test.ts` | ✅ PASS（1/1） |
| `locale-route-smoke` + `locale-authenticated-api-matrix` | ✅ PASS（11/11） |
| `npm run build`（本地） | ⚠️ 放弃 —— 跑 15 分钟无输出、next 进程 CPU 仅 0.6%（卡死）。判断为本地环境依赖（需 prod env/DB）问题，非代码问题。构建验证移至服务器 docker build（用真实 env，部署时必跑） |

### 9.6 改动内容

分支 `fix/public-homepage-root`（基于 `dbfdccc6`），**仅改 `src/app/[locale]/page.tsx` 一个文件，+32/-3**，实现见第四章方案 A′。

- commit：`72bf5026`
- PR：**https://github.com/yanghylive/kaypal-ai/pull/161**

---

## 当前状态：✅ 已上线（2026-08-30 11:50）

| 步骤 | 结果 |
|---|---|
| 备份 | ✅ `/www/backups/kaypal-predeploy-20260830/`（DB 290 表 dump + 旧镜像 tag rollback-20260830 + .env + nginx + 调用基线） |
| 构建 | ✅ 新镜像 `71225d44a627`，防缓存陷阱验证通过（新镜像含 host 判断特征=1，旧镜像=0） |
| 容器切换 | ✅ 11:50:46 完成，downtime 秒级（旧容器改名 `kaypal-app-baota-old-0830` 留档） |
| **官网验证** | ✅ `kaypal.cn` 200，官网标题，0 处 dashboard/apps，hero/技能市场/价格方案文案齐 |
| **vip 直达** | ✅ → `/zh-CN/dashboard/apps` |
| enterprise | ✅ 307 行为不变 |
| 底座 API | ✅ subscriptions 401 / auth/me 200(null) / billing 401，OIDC 200，微信登录 302 |
| 落地页内链 | ✅ marketplace / pricing / enterprise / help / about 全 200 |
| chamber 业务 | ✅ vip 的 8088 路径 200，不受影响 |
| PR | ✅ #161 已合并 main（`f419603c`），DEPLOYED.md 已固化 |

### 部署中挖出并修复的隐藏 bug（nginx）

首次部署后发现 vip 直达不生效、渲染了官网。二分定位：宿主机直连 3000 模拟 `Host: vip.kaypal.cn` → 应用判断正确；走 nginx → 失效。根因：`/etc/nginx/conf.d/chamber-vip.conf` 里 `location = /zh-CN` 等 **7 处裸 proxy_pass 没有 `proxy_set_header Host`**，nginx 默认把 Host 改写成 `127.0.0.1:3000`，应用收到假 host 走了兜底逻辑。旧代码无条件 redirect 不看 host，所以这个坑从未暴露过。

修复：给该文件走 3000 的裸 location 补 `proxy_set_header Host $host` + `X-Forwarded-Host $host`，**不碰 8088 的 chamber 业务路径**；`nginx -t` 通过后热 reload（不断连接）。改前单文件已备份。

### 回滚（如需）

```bash
# 应用回退（秒级，不动数据）
docker tag kaypal-app-baota:rollback-20260830 kaypal-app-baota:mainline
docker stop kaypal-app-baota && docker rename kaypal-app-baota kaypal-app-baota-old-$(date +%m%d)
docker compose -p baota --env-file deploy/baota/.env -f deploy/baota/docker-compose.baota.yml up -d --no-deps app
# nginx 回退
cp /www/backups/kaypal-predeploy-20260830/chamber-vip.conf.bak-homepagefix /etc/nginx/conf.d/chamber-vip.conf && nginx -t && nginx -s reload
```

### ⚠️ 遗留交底

PR 合并后 main 里带了 gateway `routes-config.ts` 的 pricing/subscribe 放行改动（来自 `5b173366`，订阅链路），**尚未部署到生产**。下次从 main 部署会自动带上——需要你知情后决定时机。

### 建议跟进

1. 你本人无痕窗口打开 https://kaypal.cn 过一遍落地页文案（组件 8-09 后就没渲染过，可能有信息滞后）
2. 向搜索引擎提交首页重新收录（已丢三周）
3. www.kaypal.cn 删 DNS 解析（你去域名控制台操作）
