#!/usr/bin/env node

console.error(
  'JIUZHANG AI one-click desktop packages must not publish external dependency installers. Bundle the required runtime resources inside the app package instead.',
);
process.exit(1);
