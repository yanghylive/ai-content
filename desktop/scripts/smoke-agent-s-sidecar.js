#!/usr/bin/env node
/**
 * Legacy disabled.
 *
 * The one-click desktop package no longer starts a Python Agent-S sidecar.
 * Runtime automation is handled by the Node/CDP adapters.
 */
console.error('smoke-agent-s-sidecar is disabled: the one-click desktop package no longer starts the Python Agent-S sidecar.');
process.exit(1);
