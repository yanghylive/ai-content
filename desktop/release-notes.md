## v1.1.110（2026-08-31）

- 数据安全：数据库自愈失败完整回滚，不留半残状态或被旧日志污染的新库
- 更新：跳过版本立即停止下载（含进行中的下载）
- 发布：Linux 通道彻底移除，发布计划与远端门禁只覆盖 Windows / macOS

## v1.1.109（2026-08-31）

- 稳定：Octop 高级模式登录态保障（凭据失配显式提示）
- 发布：CI 发布路径修复（Windows 换行符文件名、打包资源合并）
- 安全：跳过版本彻底不下载/安装；数据库自愈更保守

## v1.1.108（2026-08-31）

- 安全：跳过版本后彻底不再下载/安装（含下载已完成的情况）
- 稳定性：数据库自愈顺序修复（旧日志移不动时不破坏数据）
- 清理：移除遗留云端登录/用量/订阅死接口调用

## v1.1.107（2026-08-31）

- 发布质量：数据库模板更名、打包清单收紧（1.1.106 用户请升级到本版）
- 安全：跳过版本时立即取消下载
- 稳定性：数据库自愈更保守（临时文件移不动时不强行重建）

## v1.1.106（2026-08-31）

- 修复：微信登录成功后跳回原页面（此前回跳目标被双重编码，登录后落到首页）
- 修复：悬浮球不可用问题（preload 组件补齐、拖拽修复）；悬浮球默认隐藏，完善后重新开放
- 修复：发布中心账号列表与本地浏览器账号实时同步（普通列表即同步）
- 稳定性：旧库升级自动补字段；自愈判定收窄（仅明确库损坏触发，权限/锁库不误清库）

## v1.1.105（2026-08-30）

- 关键修复：Windows 安装包重新构建——1.1.104 的 Win 包误含 Mac 版内置组件（会导致 Windows 后端无法启动），1.1.104 用户请务必升级到本版
- 旧库升级自动补字段：1.1.99 及更早版本的数据库升级到 1.1.102+ 后启动即恢复（自动补 device_token_hash 等列，不丢数据）

## v1.1.104（2026-08-30）

- 关键修复：旧版数据库升级后「应用起不来」——1.1.99 及更早版本的库升级到 1.1.102+ 时，新代码要求的字段（如 device_token_hash）在旧库里不存在，启动建索引即崩；现启动时自动补齐缺失字段（列收敛顺序前置），无需手动清库或改库

## v1.1.103（2026-08-30）

- 安全修复：数据库自愈改为只识别本次启动产生的报错——修复历史日志可能误触发「清库重建」的隐患（1.1.102 用户建议升级）

## v1.1.102（2026-08-30）

- 关键修复：重新构建 Windows 安装包——此前 1.1.100/101 的 Win 包误含 mac 版内置组件，会导致后端在 Windows 上无法启动（1.1.101 用户请务必升级）
- 自愈加固：数据库自愈失败可重试、旧库备份更可靠、损坏识别更精准，减少误清库风险
- 更新通道修复：补齐差分更新元数据（blockmap），检查到新版后自动后台下载

## v1.1.101（2026-08-29）

- 更新体验：检查到新版本后自动在后台下载，准备好后提示一键安装（此前需手动点下载）
- 配合 1.1.100 的后端自愈与崩溃自动回传，故障机器升级更省心

## v1.1.100（2026-08-29）

- 启动自愈：本地数据库文件损坏导致的「应用起不来」不再需要手动处理——后端异常退出时自动识别库损坏（数据页损坏/WAL 合并失败等 schema 预检查覆盖不到的场景）、自动备份旧库（.corrupt-<时间戳>）并从零重建，重启即恢复
- 崩溃自动回传：后端启动崩溃/就绪超时的错误摘要自动经云端转发落 OSS error-reports/（修复此前「后端起不来=零上报」盲区），并加会话频控防重启循环刷爆

## v1.1.99（2026-08-28）

- 视频生成 Windows 真机打通：修复网关地址拼错、轮询鉴权不一致、StudioCore 异常逃逸三大问题；无本地视频引擎时自动回退 kaypal.cn 云端计费通道（实测真机 2 分钟出 1080p 成片）
- 稳定性：应用启动前自动清理崩溃残留的后端进程，修复偶发「应用起不来/功能全挂」；所有云端出站失败不再报笼统的「服务器内部错误」，改为带原因的明确提示

## v1.1.98（2026-08-27）

- UI 助手 AI 对话修复：流式对话补网关别名映射
- 前端版本号与包版本同步

## v1.1.97（2026-08-27）

- 错误自动上报链路修复：修复打包配置双源覆盖导致商用包丢失 OSS 凭证、真机报错无法回传的问题
- AI 出站链路加固：登录后自动同步 Kaypal 云端模型清单，AI 对话/视频生成统一走 kaypal.cn 计费网关

## v1.1.96（2026-08-22）

本版为发版前安全与稳定性收口版：桌面安装包不再携带任何服务端凭据，Agent Browser 与 AI 草稿链路完成三轮共 20 项修复，桌面 SQLite 库表结构全量对齐 Prisma 模型。

- 安全加固：安装包打包源切换为占位符模板（不再包含 OSS/RedFox/好单库/聚推客/Kaypal 等真实密钥），新增发布守卫拦截任何真实凭据指纹；测试脚本内硬编码的会话 token 全部改为环境变量注入。
- Agent Browser（网页代操作）：导航后基于真实页面 URL 重新决策；暂停后可断点续跑；幂等重试只补失败动作、不重放已成功动作；确认单两阶段锁定与消费；error 终态禁止重跑；进程重启后暂停/部分成功会话可恢复。
- AI 任务草稿：并发确认原子抢占（无孤儿配置）；执行副作用防重复；崩溃残留自动回收；补偿配置立即关联草稿。
- 桌面库与平台：SQLite fallback 补全 growth_task_drafts 表（150 模型全对齐）；MAI-UI 视觉模型断链修复（别名列表 + enabled 过滤 + 动作序列校验）；套餐门禁本地旁路绑定开发环境，生产不受影响。
- 发版门禁：全量门禁脚本启用 pipefail，上游测试/构建失败不再被 tail 掩盖。

## v1.1.90（2026-08-20）

本版修复 Windows 非 C 盘安装（尤其 Program Files 目录）导致的操作失败问题。

- 根因：应用装在 D 盘等 Program Files 目录时，系统 UAC 写保护禁止程序写入安装目录，导致创建获客任务、补充线索、任务状态切换等操作报「服务器内部错误」。
- 修复：后端数据（获客 store/素材/视频工作台等）与日志（互动资产/浏览器证据/AI 员工产物等）统一迁移到用户数据目录（可写），不再依赖安装目录。
- 通过错误自动上报定位：真机 8 条 500 报告全部指向 `EPERM: mkdir ...\resources\data`，一次修复全部覆盖。

## v1.1.89（2026-08-20）

本版新增错误自动上报能力：应用运行中出现异常时自动采集错误详情并上传云端，无需手动收集日志，便于团队快速定位修复。

- 后端 500 错误自动上报（requestId/路由/堆栈/版本/系统信息 → OSS error-reports/）。
- 前端全局异常（window.onerror / unhandledrejection / API 5xx）自动上报，带 requestId 可精确关联后端日志。
- 上报静默失败不影响业务；内置限流防错误风暴。

## v1.1.88（2026-08-20）

本版修复 Windows 真机「登录平台账号」浏览器空白不跳转问题。

- Windows 点登录后浏览器停在新标签页不跳转：增加 JS 导航兜底（CDP goto 未离开新标签页时，用浏览器自身跳转打开登录页）。
- 登录导航增加诊断日志（落点 URL/兜底前后 URL），真机异常可快速定位。

## v1.1.87（2026-08-20）

本版为安装包稳定性紧急修复版：修复桌面端启动崩溃、登录页图标丢失、抖音登录浏览器空白。

- 修复安装包启动崩溃（sqlite-empty-template 缺失导致双击打不开）。
- 修复登录页两个图标丢失（next/image 优化端点在桌面端返回 404）。
- 修复抖音登录浏览器空白：探活浏览器进程无限堆积（资源耗尽）+ CDP 端口并发竞态。
- 安装包打包不再携带本地运行时数据（打包守卫）。

## v1.1.86（2026-08-20）

本版完成 AI 获客体验全面升级：AI 长期记忆、价值感知 12 项、灰度功能整页遮罩、抖音自动触达防风控与视觉兜底。

- AI 长期记忆：接入 kaypal 服务器记忆系统，跨会话记住行业、关键词、话术风格，二次创建任务自动预填。
- AI 价值感知：首页「今日 AI 简报卡」+「AI 获客价值账单」；线索评分给自然语言理由 + 原始出处直达 + 一键重评。
- 灰度功能整页遮罩：BOSS 直聘/企业微信/省钱比价/视频工作室/朋友圈发布等 10 页，全貌可见 + 背景模糊 + 操作锁定。
- 抖音自动触达防风控：同账号执行节流 + 人类化随机延迟；反爬拦截时截图 + 视觉模型（qwen-vl-max）自动恢复候选。
- 全量修复 14 项：复盘口径统一、话术过滤空值、评分可信度（版本/置信度/时间）、任务状态真实化、账号健康一致、首页颜色语义、加载体验、路由 404 等。

## v1.1.85（2026-08-15）

本版修复发布中心闭环，并统一移动端界面图标与骨架屏。

- 发布中心「确认发布」「重试」按钮接上真实行为；账号失效后可一键恢复任务。
- 任务状态新增「执行中」「已取消」展示，卡住的任务会提示并支持重试。
- 发布任务列表支持「加载更多」分页，历史任务不再只显示前 80 条。
- 内容工作区「新建」入口修复，正确创建新草稿。
- 修复 Local Engine 健康检查无超时导致无限挂起（补超时 + 防并发 + 后台暂停轮询）。
- 移动端手写图标收敛为统一图标库，品牌 logo 与骨架屏抽成共享组件。
- 修复无缝滚动内容读屏重复播报（补 aria-hidden）。
- MCP 安全加固（限流 + 权限分级 + token 轮换）。

## v1.1.84（2026-08-14）

本版修复登录页微信登录入口，并收口一批旧入口路由与使用细节。

- 登录页「微信扫码」改为「微信登录」跳转入口：去掉扫码后无反应的二维码展示，改为桌面端跳转微信扫码、登录后自动回到本页。
- 旧任务控制台 / 文章库 / 客户详情 / 企微助手等旧入口统一重定向到规范路由。
- 修复语音识别/合成接口 `/api` 前缀重复导致请求失败。
- 修复企微 CRM 页面切换配置不刷新数据（effect 依赖缺失）。
- 修复静态服务器遇到畸形 URL 崩溃（decodeURIComponent 未捕获异常）。
- 趋势雷达、报告生成等情报文案去掉第三方品牌露出；比价/返利页补「返回上一级」按钮。

## v1.1.83（2026-08-14）

本版落地一期「统一线索对象」+ 二期「AI 质量观测」+ 多租户双维度，并修复 Windows 真机实测的一大批使用问题。

- 统一线索对象：评论/私信/导入三套线索表收敛为统一 leads 表，线索从评论到客户的完整链路可追溯；新增 lead 创建/转化事件流。
- 平台互动适配器统一契约：小红书/抖音/视频号评论读写走统一契约，消除 `platform === 'xiaohongshu' ? ... : ...` 平台分支。
- AI 质量观测：新增 AiCallTrace 表，每次 AI 调用的 prompt/回复快照、耗时、成败、失败原因自动落库，AI 回复质量可量化、失败原因不再被吞。
- 多租户双维度：CRM 与增长任务补「操作者维度」actorUserId（区分数据归属 vs 谁操作），成员邀请/移除闭环。
- 登录状态滑动续期：7 天内用过一次自动续满 14 天，不再定期被强制登出。
- 修复 Windows 任务栏点图标无法唤起主窗口（补单实例锁 + 二次唤起）。
- 修复新建获客任务平台与账号无关联、前几个账号同时选中无法单选。
- 修复线索池只能新增不能删除（补单条删除 + 批量删除）、自动评分无验证入口（线索卡片展示评分依据）。
- 修复生视频走 kaypal 网关必失败（网关通道补全提交+轮询+入库）。
- 修复 FLAGSHIP 订阅用户误报「缺少有效商用授权」（登录时同步 kaypal 订阅为本地商用授权）。
- 修复打包版缺 RedFox/好单库/聚推客/模型同步等配置导致第三方能力（热榜/去水印/比价/返利/AI 助手）全断。
- 回复截图/回读证据自动落库，真实送达有存证（证据链补全）。
- 增长控制台获客策略/工作流/账号健康/复盘四入口直达；素材库补「本地上传」入口。

## v1.1.82（2026-08-12）

本版修复真机反馈的多处功能断裂，并将 AI 生图/生视频切换为百炼引擎。

- 修复素材库「始终 20 篇」：前端默认只拉 20 条，实际素材 300+，改为分页加载更多、一次看全。
- 修复文章反抓正文恒空：正文容器 fallback 逻辑错误，改为按容器长度判断 + 支持公众号正文容器。
- 修复抖音私信误报「视频号未登录」：平台未传时默认走视频号分支，改为按账号类型自动推断。
- 修复微信群发入口误标「路线图」：实际能力已具备，接入真实群发向导。
- 修复全网采集翻页不累加、去水印识别不了分享链接、素材库选完无下一步等闭环断裂。
- AI 生图切换百炼 qwen-image（可选方图/竖图/横图尺寸），生视频切换百炼 happyhorse（可选 3-15 秒时长），稳定出图出片。
- 视频一键成片独立为专属产品页（studio_core 独立产品，上线后接入链接）。
- 功能子页统一补齐「返回上一级」按钮；页面顶部图标在桌面端不再撑大。

## v1.1.81（2026-08-12）

本版修复微信联系人同步与客户端版本号显示问题。

- 修复微信联系人同步失败：微信数据组件云端化后，OCR 兜底引擎路径推导错误（指向不存在的用户数据目录），已改为优先读取随包组件。
- 修复微信 OCR 组件下载失败：client_configs 残留本地测试地址（127.0.0.1:8899）且该资源未发布到生产 OSS，已过滤本地/回环测试地址。
- 修复客户端左下角/登录页仍显示旧版本号：改为动态读取应用真实版本，web 环境回退到写死常量。

## v1.1.80（2026-08-12）

本版聚焦「增长工作流」与「AI 助手」两大能力升级。

- 增长工作流 = 行业方案库：14 行业（美业/餐饮/教育/医疗健康/家装/房产等）× 每行业 2 套获客场景，一键创建带行业话术、平台组合与合规风控的获客流水线。
- 工作流执行引擎上线：启动后按序真执行——获客步骤调用真实评论/私信触达，自动推进下一步；需人工确认的步骤停下来等确认；执行失败自动暂停并标记；画布节点实时显示进度。
- 工作流画布流水线化：单列顺序排列 + 步骤序号 + 自动连线，执行顺序一目了然，上下拖动即可调整顺序。
- AI 助手接入 6 个增长获客工具：一句话就能「开一条美业获客流水线」「查我的获客任务」「我有哪些线索」，AI 直接调系统功能执行。
- AI 助手记忆升级：多轮上下文修复（记得对话里说过的话）+ 接入 TencentDB Agent Memory 四层长期记忆（对话/原子记忆/场景/画像）。
- 修复：工作流行业标签不落库不显示、AI 意图路由边界（无 ID 启动降级/体检误触发合规/行业口语映射）、AI 助手记不住上下文。

---

## v1.1.79（2026-08-11）

本版完成真机回归问题修复与功能开放。

- 全功能开放：获客/CRM/情报模块登录用户默认可用所有功能，不再按角色拦截（修复 owner 账号因云端角色同步异常无法创建获客任务）。
- AI 客服页返回按钮修复：有历史时返回上一页，深链直达兜底回消息页。
- 消息页企微助手入口恢复，移动端渠道宫格 4 格扩到 8 格。
- 微信扫码登录回调加固：异常时给出明确提示而非 500 白屏，错误可追踪。
- 评论获客数据加载修复（BigInt 序列化崩溃导致 500）。
- 微信工作台入口跳转修复（废弃路由 /workbench/wechat → /engagement/wechat）。
- 后端全量自动化测试 1556 项全绿 + 真机回归守卫（建表/权限/序列化/路由）全过。

---

## v1.1.74（2026-08-10）

- 账号密码登录接入云端真实授权：登录时向云端兑换桌面令牌，登录后正确显示旗舰版套餐与积分余额（16037.01），不再回落免费版。
- 微信扫码登录回调链路优化：云端回跳白名单已部署（含本地开发 origin），扫码授权后可正常回跳完成登录。
- 后端全量自动化测试 1464 项全绿。

---

## v1.1.73（2026-08-10）

本版完成炼刀能力逐项补齐与新品牌图标。

- 应用图标更换为九章智能三玖回旋纹：桌面安装包图标、工作台左上角；登录页保持原版。
- 个人资料编辑：昵称/头像（PATCH /api/auth/me）。
- 群发计划：新增取消与详情编辑（排队/暂停中可改文案/目标/限额/间隔）。
- 商品剪辑配置：带货成片模板 CRUD，一键复用提交成片。
- BGM 曲库预设与视频发布计划视图、素材删除/重命名、合成分类。
- 曝光能力补齐：曝光账号管理、评论扩散、文案扩展、曝光记录。
- AI 调用自动统计 Token 用量（每日配额、明细可追溯，与云端计费同口径）。
- 登录与云端能力统一走生产环境（kaypal.cn）。
- 后端全量自动化测试 1464 项全绿。

---

## v1.1.72（2026-08-10）

本版完成炼刀竞品差距闭环与生产环境修复。

- 登录与云端能力统一走生产环境（kaypal.cn）：修复测试残留域名（KAYPAL_AUTH_BASE_URL 默认与本地配置从 test.kaypal.cn 切回生产；AI 网关与桌面证据浏览器标记同步补生产域；测试环境仍可显式 env 切回）。
- 新增 AI 网页代操作：自然语言指令驱动真实浏览器执行（打开/点击/输入/截图/提取/等待），逐步截图留证；支持 LLM 动作解析（配置默认模型后自动启用），失败自动降级规则解析；DISPATCH_MOCK 下硬失败防伪。
- 新增桌面悬浮球（hoverBall）：透明置顶小窗，随时唤起 AI 网页代操作，输入指令即执行并查看分步结果与截图证据；可拖拽，默认开启（hoverBallEnabled 关闭）。
- 新增 Token 用量追踪：每日配额（默认 200 万）/调用前预检/调用后上报，明细可追溯（/api/usage/token）。
- 新增门店 POI 管理：门店点位 CRUD + 按城市/分类聚合报告，对接探店内容场景（/api/poi）。
- 新增商品视频一键剪辑：商品信息自动生成带货口播文案（钩子/卖点/价格/CTA 分镜），一键提交成片（/api/video/product-copy、product-cut）。
- 群发计划新增默认配置查询端点；完成态列表走状态过滤。
- 修复扫码/密码登录后套餐显示免费版：localOnly 会话带真实云 token 时改走云端真实等级（FLAGSHIP 显示旗舰版）。
- 后端全量自动化测试 1451 项全绿，验收门禁 PASS=59 FAILED=0（真实发布待现场账号/云端积分确认）。

---

本版继续对齐商用交互闭环，并完整保留现有导航、模块、字段和业务入口。

- 桌面授权会先创建并选中当前工作区，再进入业务页面；增长、CRM、应用安装和平台账号统一使用经过成员资格校验的租户上下文。
- 增长账号健康读取当前租户、当前用户在发布中心的全部账号，不再被旧占位记录遮挡。
- 视频号无法提取二维码时保留同一受控浏览器会话，等待网页登录完成后自动保存绑定结果。
- BaiLongma 本地语音运行时随桌面端真实监听 3721；只有服务在线且账号授权验证通过时才显示可用。
- Agent 工作台在没有可用模型时阻止新建、发送和重试；正常空产物不再冒成 500，真实服务错误仍会显示。
- 客服机器人新建流程改为本地草稿，取消不落库，明确点击保存才创建记录。
- 旧登录接口缺少请求体或字段时统一返回 400，不再触发服务端 500。
- Windows 真机测试候选连接当前已在线的 JIUZHANG AI 账号服务，修复授权码无法获取的问题。
- 桌面端按 Windows/macOS 真实平台登记授权设备，不再错误登记为 Web 设备。
- 本地 3011 后端始终随桌面应用启动；“自动恢复连接”只控制异常后的自动重连。
- v1.1.56 是真机测试候选包；正式发布前仍需上线生产账号域名、修复官网证书并完成 Windows 代码签名。
- RedFox 数据能力仍需租户安全配置合法 RedFox API Key；JIUZHANG AI 登录、套餐和积分不能替代第三方凭据。
- 短视频评论获客新增“创建并立即执行”和“保存并立即执行”，原有每天定时启动能力继续保留。
- 立即执行增加服务端一次性确认、重复点击保护和租户归属校验，不会把预览误当成真实触达。
- 浅色模式统一侧栏、顶部栏和导航层级配色，深色模式保持原有视觉与操作状态。
- 新增 AgentWaker 小红书运营助理与公众号运营助理，支持生成内容包、保留执行过程并在发布前等待人工确认。
- 公众号运营助理支持官方草稿写入与标题回读、正式发布前独立一次性确认和发布状态回查；失败会保留记录并禁止盲目重试。
- 平台授权不再向前端返回 Access Token，编辑时留空可保留原令牌；自定义发布地址只允许 HTTPS 白名单域名。
- 内容库 HTML 编辑与预览加入 no-referrer 和 iframe 脚本隔离，阻止预览内容主动执行脚本。
- 同步平台授权、公众号助理、小红书助理、内容库和 API 客户端的最新前端修正，真机安装包不再沿用 1.1.51 的旧构建。
- Windows 安装包不再在解压前启动阻断式 PowerShell 预检；必需运行时改由构建前和打包后守卫验证。
- 修复 Windows PowerShell 解析安装脚本失败，导致 Windows 10/11 在文件解压前被错误拦截的问题。
- 情报搜索只展示本次真实返回的数据，报告与任务运行链接可以恢复到指定记录。
- 增长任务统一为“预检、确认、真实执行”单一链路，结果区分完成、部分完成、失败与缺少证据。
- 微信任务补充逐对象结果和危险动作确认；Agent-S 主执行链、默认自动发送和三种操作模式保持不变。
- 设置与客户详情支持页签深链、错误重试和未保存修改提醒。
- Windows 安装包以 Win10 真机账号验收为当前放行标准，Win11 不在本轮验收范围。
