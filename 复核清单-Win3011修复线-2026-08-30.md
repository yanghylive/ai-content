# 复核清单：ai-content 桌面端 Win 真机 3011 崩溃修复线（v1.1.100 / v1.1.101）

> 交给 Codex 独立复核用。项目根：`/Users/yanghy/Documents/New project/ai-content`（注意 `New project` 带空格）。
> 所有验证命令可直接复制执行。复核人视角：**假设写代码的人（另一个 AI）会自欺，每个结论都要用命令重新证明。**

---

## 0. 背景与主张（被复核的核心论断）

1. 同事 Windows 真机（用户 15827）安装 v1.1.99 后，本地后端（3011 端口，NestJS+Prisma+SQLite）启动即崩（退出码 1），弹窗堆栈为 Prisma Client 运行时错误。
2. 根因判断：**本地 SQLite 库文件损坏**（数据页损坏/WAL 残留，启动前的 schema 字符串预检查覆盖不到），同事截图堆栈与真机人工砸库后的报错 `P2010 / database disk image is malformed` 特征一致。
3. 修复三件套：
   - **自愈**：后端崩溃且 stderr 命中 SQLite 损坏特征 → 自动备份旧库为 `.corrupt-<ts>` → 从零重建 → 走现有重启链路（v1.1.100）
   - **兜底上报**：主进程在崩溃/就绪超时两挂点把错误摘要 POST 到云端 `POST /api/v1/client-error`，云端转发落 OSS `error-reports/<日期>/<uuid>.json`（v1.1.100）。此前上报链路的转发者是本地 3011 后端进程本身，后端起不来=零上报（OSS 实测 8/29 为 0 条，8/20~8/28 每天有）
   - **更新自动下载**：`autoDownload=true`（v1.1.101），故障机器无人值守拿到修复版
4. 发版：v1.1.100 与 v1.1.101 均已推生产 OSS `updates/` 双通道（Win exe + Mac zip）。
5. E2E：无影云电脑 jz-win11-accept-0828 真机验证——人工砸库 → 首启崩（同特征报错）→ AutoHeal 备份+重建 → 重启 → 3010/3011 全部恢复（150 秒内）。

---

## 1. 代码改动核对（git）

```bash
cd "/Users/yanghy/Documents/New project/ai-content"
git log --oneline -4
# 预期（自 1.1.99 起）：
# 818134c9 feat(desktop): 更新自动下载（v1.1.101）
# c526f46d fix(desktop): CloudAPI URL 拼接保留 endpoint 路径前缀 + 门禁脚本修正
# a6ef57ad fix(desktop): 后端崩溃自愈 + 错误自动回传兜底（v1.1.100）
# 7808a14f chore(release): 1.1.99 …（基线）

git show a6ef57ad --stat
git show c526f46d --stat
git show 818134c9 --stat
```

涉及文件（改动面全景）：
- `desktop/main.js`（自愈 + 上报挂点）
- `desktop/cloud-api.js`（reportClientError + URL 拼接修复）
- `desktop/auto-updater.js`（autoDownload）
- `cloud-api-server/server.js`（匿名 client-error 端点 + OSS 签名 PUT）
- `desktop/package.json`、`desktop/package-lock.json`、`desktop/packager.json`、`desktop/release-notes.md`
- `frontend/src/lib/release-notes.ts`、`frontend/src/app/(dashboard)/release-notes/page.tsx`
- 门禁脚本修正：`scripts/ci/pre-release-core-verify.mjs`、`frontend/scripts/console-quality-browser-scan.mjs`、`backend/src/modules/ai-models/kaypal-model-sync.service.spec.ts`

⚠️ 复核点 1.1：`git status` 确认上述文件无未提交残留：
```bash
git status -s | grep -vE '^\?\?'
```

---

## 2. 自愈逻辑复核（最高优先级）

位置：`desktop/main.js`
- `DB_CORRUPT_SIGNATURE`（正则）≈ 1647 行
- `healCorruptedDesktopDatabase()` ≈ 1649 行
- `reportBackendCrash()` ≈ 1697 行
- close 事件挂点 ≈ 1600-1615 行（code≠0 → reportBackendCrash + 特征命中则 heal）
- `lastBackendDatabasePath = desktopDatabasePath` ≈ 1360 行（startBackendService 内赋值）

关键设计参数（逐条审）：
| 项 | 当前值 | 复核问题 |
|---|---|---|
| 损坏特征正则 | `/malformed\|not a database\|database disk image\|unable to open database\|sqlite_corrupt\|databasefileerror\|file is not a database/i` | 会不会**误伤**：非 DB 原因的崩溃里 stderr 恰好含这些词？→ 清库丢用户数据。建议评估：是否需要再加「且重启 N 次后才允许自愈」的门槛 |
| 自愈频次 | `backendDbHealUsed` 每会话 1 次 | 首次自愈失败（如 rename 被锁）后永不重试是否合理 |
| 备份方式 | `fs.renameSync` 到 `.corrupt-<ts>` | 用户数据"丢"了但在备份文件里，UI 无任何告知——产品侧是否需要提示 |
| 重建 | `createEmptySqliteDatabase`（user_version=1 空库，后端启动时 migrate） | 依赖后端 migrate 健壮性；seed 逻辑 8/27 重写过（commit f7ce3e2e，166 表） |
| 触发链 | close(code≠0) → heal → scheduleRestart（MAX_RESTARTS=3） | 自愈后重启走 scheduleRestart 会消耗 restart 次数，首次自愈成功即占用 1/3，确认无冲突 |

⚠️ 复核点 2.1（**重点**）：`stderrTail` 只保留最后 4096 字节（main.js stderr data handler）。Prisma 错误若出现在更早的位置（被后续日志挤出窗口），特征匹配会漏——评估是否该把特征匹配改为直接读 `backend-stderr.log` 文件尾部（比如最后 64KB）。

⚠️ 复核点 2.2：`healCorruptedDesktopDatabase` 里 `createEmptySqliteDatabase` 之后**没有主动 restart**，依赖 close 处理器里已有的 `scheduleRestart`。确认两条路径不会双启动（close → heal → scheduleRestart 只有一条链）。

---

## 3. 兜底上报复核

### 3.1 桌面端
- `desktop/main.js` `reportBackendCrash()` ≈ 1697 行：频控 `maxPerSession: 3` + `signatures` Set 同指纹去重（签名 = kind + detailText 前 300 字符）
- 挂点 1：close 事件 code≠0（≈1608 行）
- 挂点 2：`waitForBackendReady` 失败（whenReady 里，搜 `backend-not-ready`）
- `desktop/cloud-api.js`：
  - `reportClientError()`（文件尾）fire-and-forget，失败静默
  - **URL 拼接修复** ≈ 30-36 行：endpoint 带路径前缀（如 `/cloud-api`）时 `new URL('/api/v1/x', base)` 会丢前缀 → 改为保留 base.pathname 拼接

验证（本机）：
```bash
node --check desktop/cloud-api.js && node --check desktop/main.js && node --check desktop/auto-updater.js
node -e "
const base = new URL('https://kaypal.cn/cloud-api');
console.log(new URL(base.pathname.replace(/\/+$/, '') + '/api/v1/client-error', base.origin).href);
// 预期: https://kaypal.cn/cloud-api/api/v1/client-error
"
```

⚠️ 复核点 3.1（**重点**）：URL 拼接修复**改变了所有 CloudAPI 调用的目标**（generate-reply/check-content/check-dedup/mark-sent/auth/*）。此前这些调用在 endpoint 带前缀时一直打到错误地址（401/404）——修复后打到正确地址。需要确认：(a) 之前有没有任何调用方**依赖**旧的错误行为；(b) 新地址（kaypal.cn/cloud-api/api/v1/*）上的这些业务接口是否存在且可用（cloud-api-server 只实现了 generate-reply/check-content/check-dedup/mark-sent/health/client-error，**没有 auth/login 等端点**——`CloudAPI.login()` 调 `/api/v1/auth/login` 会 404！确认桌面端登录是否真的走 CloudAPI）。

### 3.2 云端
- `cloud-api-server/server.js`：
  - 匿名端点 `POST /api/v1/client-error` ≈ 205 行，**注册在 `app.use('/api/v1', requireAuth)` 之前**（匿名靠注册顺序）
  - IP 限流 ≈ 128 行：60s 窗口 20 条（与 backend 的匿名端点同级）
  - `ossPutJson()` ≈ 152 行：零依赖 V1 签名 PUT，**签名串 Content-Type 必须与请求头一致**（`PUT\n\n{CT}\n{exp}\n/{bucket}/{key}`）
  - 落 OSS：`error-reports/<ymd>/<uuid>.json`，schema `error-report/v1`，与 backend 的 `backend/src/common/filters/error-report.ts` 格式对齐

验证（生产真实链路）：
```bash
curl -s https://kaypal.cn/cloud-api/health
# 预期 {"status":"ok",...}

curl -s -o /dev/null -w "%{http_code}\n" -X POST https://kaypal.cn/cloud-api/api/v1/client-error \
  -H 'Content-Type: application/json' \
  -d '{"kind":"review-check","message":"codex review verification","version":"review"}'
# 预期 204；随后 OSS error-reports/<今天>/ 应出现一条 kind=review-check 的记录（用 aliyun oss ls 查，验完删）

aliyun oss ls "oss://kaypal/error-reports/" | tail -5
```

⚠️ 复核点 3.2（**重点**）：匿名端点的滥用面。无限流白名单、无 body 大小上限（全局 express.json 100KB 兜底）、OSS 凭据在服务端（不下发客户端，安全）。评估：恶意刷接口 → 限流 20/min/IP；分布式刷 → OSS 写入成本。可接受与否给结论。

⚠️ 复核点 3.3：`server.js` 里 **OSS 凭据来自环境变量**（容器 .env，chmod 600）。确认代码里没有任何硬编码凭据：
```bash
grep -nE "LTAI|AccessKey|Secret" cloud-api-server/server.js | grep -v "process.env"
```

---

## 4. 版本号一致性（发版铁律：多处同步）

```bash
cd "/Users/yanghy/Documents/New project/ai-content/desktop"
node scripts/check-version-sync.js
# 预期: Version sync passed: v1.1.101; updater notes=release-notes.md
grep '"version"' package.json package-lock.json | head -3   # 均 1.1.101
grep '1\.1\.101' packager.json | head -2
grep -n "currentVersion" "../frontend/src/app/(dashboard)/release-notes/page.tsx"  # 1.1.101
grep -n "DESKTOP_APP_VERSION" ../frontend/src/lib/release-notes.ts                # 1.1.101
```

历史坑：1.1.99 发版时漏了 package-lock.json 和 packager.json（当时 version-sync 门禁没拦住或没跑），1.1.100 门禁 `L2 desktop 版本号一致性` 红灯才补上。复核门禁脚本本身是否可靠覆盖全部版本号位置：`cat scripts/check-version-sync.js`。

---

## 5. 门禁与发版通道

```bash
# 门禁（约 12-15 分钟，需 3010/3011 本机已起 + /tmp/electron-test-token.txt 有效）
# token 生成方法（若 /tmp 被清）：
curl -s -c /tmp/wb-cookie.txt -X POST http://127.0.0.1:3011/api/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"username":"__REDACTED_TEST_USER__","password":"__REDACTED_TEST_PASS__"}' -o /dev/null
awk '$6=="ai_content_session" {print $7}' /tmp/wb-cookie.txt > /tmp/electron-test-token.txt

cd "/Users/yanghy/Documents/New project/ai-content" && node scripts/pre-release-full.mjs
# 预期 15/15 通过（v1.1.100 和 v1.1.101 各跑过一次全绿）
```

发版通道验证：
```bash
curl -s "https://kaypal.oss-cn-hangzhou.aliyuncs.com/updates/latest.yml" | head -3
# 预期 version: 1.1.101
curl -s "https://kaypal.oss-cn-hangzhou.aliyuncs.com/updates/latest-mac.yml" | head -3
# 预期 version: 1.1.101
# 安装包可读性（206 = OK）：
curl -s -o /dev/null -w "%{http_code}\n" -r 0-1023 \
  "https://kaypal.oss-cn-hangzhou.aliyuncs.com/updates/JIUZHANG%20AI%20%E5%86%85%E5%AE%B9%E5%88%9B%E4%BD%9C%E5%B9%B3%E5%8F%B0%20Setup%201.1.101.exe"
# SHA512 抽查：latest.yml 里的 sha512（base64）解码后应等于本地文件的 shasum -a 512
```

---

## 6. 云端部署状态（kaypal-prod-new = 118.178.108.44）

```bash
ssh root@118.178.108.44 "
  docker ps --format '{{.Names}} {{.Status}}' | grep cloud
  grep -E 'HOST|OSS_' /opt/ai-content-cloud-api/docker-compose.yml
  ls -la /opt/ai-content-cloud-api/.env   # 600 权限
  grep -n 'location /cloud-api/' /etc/nginx/conf.d/kaypal.conf
  ls /etc/nginx/conf.d/kaypal.conf.bak-20260829   # 改动前备份
"
```

复核点 6.1：`HOST=0.0.0.0` 是本次部署加的（新 server.js 默认 127.0.0.1，容器内回环导致 docker-proxy Connection reset——部署时踩过，已修）。确认 compose 里在。
复核点 6.2：`/etc/nginx/conf.d/cloud-api.conf` 是 `api.kaypal.cn → 3100`，但 **api.kaypal.cn 的 DNS 解析到 101.37.128.23（另一台测试机）**——域名指向与 nginx 配置机器不一致，历史遗留，本次未动。确认这不会影响本次链路（本次走 kaypal.cn/cloud-api）。

---

## 7. E2E 证据（云电脑 jz-win11-accept-0828，ecd-e27fekkiz0ni39j9l）

关键回传文件（OSS `updates-test/diag/`，可用 aliyun oss cp 拉回）：
- `accept-0828-probe2.txt`——测试前状态（1.1.99、WAL 残留、3010/3011 未监听）
- `accept-0828-e2eB.txt`——**核心证据**：砸库 → 首启崩（P2010 malformed）→ `[AutoHeal] ... backed up to ...kaypal-ai.sqlite.corrupt-2026-08-30T02-22-34-920Z, recreated from scratch` → 重启 → `PORT3011=True PORT3010=True`

```bash
aliyun oss cp "oss://kaypal/updates-test/diag/accept-0828-e2eB.txt" /tmp/e2eB.txt && cat /tmp/e2eB.txt
```

时间线核对（在 e2eB.txt 的 backend-launch.log tail 里）：
- 02:22:26 spawn pid=1476 → 02:22:34 崩（malformed）→ 同秒 [AutoHeal] → 02:22:35 spawn pid=6604 → 端口全活。全程 150 秒内无人值守。

⚠️ 复核点 7.1（诚实交底）：HEALTH 检查返回 404（探测用的 `/api/v1/system/health` 路径不存在）——端口监听为真，但"健康端点 200"未验证。复核者可下发脚本试 `curl 127.0.0.1:3011/api/` 下的实际路由确认 200。

---

## 8. 已知遗留与存疑点（复核重点关照，按风险排序）

| # | 遗留 | 状态 | 风险 |
|---|---|---|---|
| 1 | **NSIS 安装包在 SYSTEM 会话 /S 挂死**（oneClick=true 也没救） | 未解 | 只影响 ECD 无人值守安装测试；真实用户会话安装正常（1.1.99 装上了）、electron-updater 的 quitAndInstall 在用户会话跑。**8/26 的"oneClick 终局验证"从未真正成功过**——当时的结论"oneClick=true 能静默"未经 SYSTEM 会话实测 |
| 2 | 假旧版升级 E2E 未跑 | 未做 | 铁律 4 要求的完整升级链路（旧版检查→下载→SHA512→验签→替换→重启）只在最新版间验证过覆盖面，未端到端真机跑过。变通：真旧版（1.1.99）→ 新版的"检查+下载"段由 autoDownload 覆盖，"安装"段风险同 #1 |
| 3 | jest spec 修改方向 | 已改 | `kaypal-model-sync.service.spec.ts:94` 期望值从 `/api/ai` 改 `/api/v1`——依据是实现注释"2026-08-27 网关端点收敛 /api/v1"。复核：确认生产网关确实只认 /api/v1 |
| 4 | 门禁脚本被本次修改 | 已改 | `pre-release-core-verify.mjs` 把企微助手移出灰度抽查（依据：8/24 路由收敛后该页为功能页无遮罩）；`console-quality-browser-scan.mjs` 排除 `/case-admin`（admin 接口对验收账号 403）。复核：这两个"产品已变"的判断是否属实（看 8/24 的 git log） |
| 5 | autoDownload=true 行为变化 | 已上生产 | 自动下载占用户带宽；安装仍需用户触发 + autoInstallOnAppQuit 退出即装。评估：正在干活的用户被"退出即装"打断的体验风险 |
| 6 | 自愈误伤面 | 待评审 | 见复核点 2.1/2.2 |
| 7 | 生产 compose 的 API_KEY 未配置 | 既有 | `/api/v1/generate-reply` 等鉴权端点在云端一直 503（requireAuth 在 API_KEY 缺失时 503）。本次未动。历史状态 |
| 8 | 云电脑 accept-0828 处于 Running | 待释放 | 测试机保持运行中（应用在跑），测完需 stop-desktops 释放 |

---

## 9. 一句话验收标准

**复核通过的判定**：§1-§7 的命令全部按预期输出 + §2.1/2.2/3.1/3.2 四个重点复核点给出明确结论 + §8 表中每条遗留的风险评级被独立确认或推翻。

复核产出物建议：按本清单编号逐条给 ✅/❌/⚠️ + 命令实际输出摘录，最后给「可发布/需修复」结论。
