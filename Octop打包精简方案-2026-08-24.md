# #3 Octop 打包 —— 精简方案（响应「体积太大」）

> 大王拍板：①体积要精简 ②Win 云电脑可以 ③升级随发版重打包。
> 本方案把 Octop sidecar 从 1.1GB 分层压缩，每层标注收益/风险，大王选目标体积。

---

## 体积构成（实测）

| 组成 | 大小 | 可否压缩 |
|---|---|---|
| Octop venv（398 依赖） | 720 MB | 可裁剪 |
| ├ 无损：`__pycache__` 61 + `pip` 11 + `PyObjCTest` 16 | **-88 MB** | ✅ 零风险 |
| ├ connector（google 109 / lark 48 / openai+anthropic 32 / langchain 21） | **-210 MB** | ⚠️ 需验证 |
| Playwright Chromium（Octop 用 headless_shell-1228 = 192MB，full=344MB） | 536 MB | 可压缩 |
| ├ 只打 headless_shell（Octop 只用 headless） | **-152 MB** | ✅ 低风险 |
| ├ 复用桌面端 chromium（Octop playwright 1.61→1.62） | **-536 MB** | ⚠️ 高风险 |

---

## 分层方案（从 1.1GB sidecar 往下压）

| 层 | 动作 | 收益 | 风险 | sidecar | 安装包 |
|---|---|---|---|---|---|
| 现状 | — | — | — | 1.1 GB | 1.5 GB |
| ① 无损裁剪 | 去 `__pycache__`/`pip`/`PyObjCTest` | -88MB | 零 | 1.01 GB | 1.41 GB |
| ② headless_shell only | 只打 chromium headless（192MB），不打 full | -152MB | 低 | 860 MB | 1.26 GB |
| ③ connector 裁剪 | 去 google/lark/openai/anthropic/langchain | -210MB | 中（需验证启动 import） | 650 MB | 1.05 GB |
| ④ chromium 复用 | Octop playwright 1.61→1.62 + 指向桌面端 chromium | -536MB | 高（版本兼容） | 114 MB | **~530 MB** |

**关键**：①+②+③ = 1.05GB（风险可控）；④ 单独能再省 536MB → 总包 **~530MB**（接近现有 420MB 水平）。

---

## 各层可行性依据（已实测）

- **① 零风险**：`__pycache__`/`pip`/`PyObjCTest` 纯运行时冗余，删了 Octop 照常跑。
- **② 低风险**：Octop 浏览器会话日志明确用 `chromium_headless_shell-1228`（headless），不需要 full chromium（344MB）。
- **③ 中风险**：Octop 核心代码对 connector 的 import 极少（仅 `infra/agents/providers/probe.py` + 一处 `lark_oapi_csrf_token` 是**字符串**非 import）。大头 google/lark/openai/anthropic 由 `orcakit-harness-agent[all]` 的 `[all]` extra 拉入。需在隔离副本验证「删掉后 Octop 能启动 + browser 会话能用」。
- **④ 高风险**：Octop 声明 `playwright>=1.40`（无上限），理论支持 1.62；但需验证「升级 1.62 后 browser 会话正常」+「headless_shell vs 桌面端 full chromium 的差异」——桌面端打的是 full chromium（chromium-1234），Octop 用 headless_shell，两者 shell 类型不同，复用要处理。

---

## 我的建议

**推荐做 ①+②+③（→ 1.05GB），并把 ④ 作为第二阶段验证**：

- 先落地 ①+②（零/低风险，-240MB），马上见效；
- ③ 在**隔离 venv 副本**验证「删 connector 后 Octop 启动 + browser 会话通」→ 通过就落地（-210MB）；
- ④ chromium 复用单独验证「Octop 升 1.62 + 指向桌面端 chromium」→ 通过能到 ~530MB，最接近现状。

**目标体积给大王选**：
- A. 保守（①+②）：~1.26GB
- B. 推荐（①+②+③）：~1.05GB
- C. 激进（①+②+③+④）：~530MB

---

## 动手顺序（等大王选目标）

1. 隔离 venv 副本 → 验证 ③（删 connector）和 ④（升 playwright 1.62）可行性
2. 落地通过的优化 → 打包脚本 + main.js spawn + 健康检查
3. Mac 侧跑通 → 云电脑出 Win（同 #1 合并，大王已确认可以）