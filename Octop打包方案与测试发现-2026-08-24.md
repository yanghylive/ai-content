# #3 Octop 打包技术方案 + #5 测试发现（2026-08-24）

> 大王拍板：#3「Octop 直接打包，不外部依赖」；#5 测试账号 __REDACTED_TEST_USER__/__REDACTED_TEST_PASS__ 已实测。
> 本报告给出 #3 打包的真实规模评估与方案，以及 #5 实测暴露的 2 个真实问题。

---

## 一、#3 Octop 打包 —— 规模实测（关键，大王拍板前需知）

Octop 不是轻量 sidecar，是**完整 agent 平台**（`infra/server.py` = `OctopServer process-level orchestrator`，含 agents / browser / cron / db / gateway / mobile / proactive / users 全套）。

| 项 | 实测大小 |
|---|---|
| Octop venv（398 依赖） | **720 MB** |
| └ 精简后（去 `__pycache__` ~61MB + `pip` + `PyObjCTest` 等） | ~550-600 MB |
| Playwright Chromium（Octop 用 1.61 = `chromium_headless_shell-1228`） | **536 MB** |
| **合计（sidecar）** | **≈1.1 GB** |
| 加上现有 Mac 包 420 MB | **≈1.5 GB 安装包** |

**三个必须摊开的硬约束：**

1. **Playwright 版本冲突**：Octop 用 Playwright **1.61.0**（chromium 1228），桌面端 backend 用 **1.62.1**（不同 chromium build）→ **不能复用**桌面端已打包的浏览器，必须单独打包 536MB。

2. **架构反转**：当前 `main.js` 已明确禁用 Python sidecar（`ensurePythonVenv` 返回 `legacy_python_sidecar_disabled`，注释「Packaged desktop uses Node Agent Runtime; legacy Python sidecar is disabled」）。打包 Octop 需要**重新引入并改造这套 Python sidecar 机制**。

3. **跨平台**：Mac arm64 venv（本机现有）+ Win x64 venv（需云电脑 `pip install` 一套全新 Win venv + Win chromium），两套各 ~550MB + 536MB 浏览器。

---

## 二、打包方案（我的推荐）

**选型：精简 venv 直接打包（不用 PyInstaller）**——PyInstaller 对 FastAPI/uvicorn/playwright 动态导入有坑，且启动解压慢；venv 直打最稳。

```
sidecar/octop/
├── venv/                    # 精简后的 Python venv（去 __pycache__/pip/PyObjCTest，~550MB）
├── browsers/                # chromium_headless_shell-1228（536MB）
├── entry.sh                 # 启动脚本：venv/bin/python -m uvicorn octop... --port 8088
└── octop-home/              # 空数据目录模板（DB/配置首次启动生成）
```

**main.js 启动逻辑**（复用现有 `spawnBackendServiceProcess` 模式）：
- spawn `octop/entry.sh` → 轮询 `http://127.0.0.1:8088/api/health` 直到 200（超时 ≥90s，Octop 冷启动含 DB 迁移）
- 退出时 kill 子进程树 + 清理
- 端口冲突检测（`isPortInUse` 已有）

**packager.json**：`sidecars: [{ name: 'octop', dir: 'sidecar/octop' }]`，`maxSizeMB` 500 → **1800**

---

## 三、需大王拍板的决策点（我不擅自定）

1. **体积**：安装包 420MB → **1.5GB**（Mac/Win 各），`maxSizeMB` 放宽到 1800。这是分发成本（OSS 存储 + 下载时长），是否接受？
2. **跨平台资源**：Win venv + Win chromium 需云电脑 `ecd-5gk1odk27jnz1pdol` 构建（归大王，同 #1 Win 重打包合并做）。
3. **升级策略**：Octop 独立版本（0.9.26），打包后跟桌面端版本锁定；Octop 升级需随桌面端发版一起重打包。

**我的建议**：先做 **Mac 侧完整链路**（本机能干，验证 venv 打包 + spawn + 健康检查 + 免登录），跑通后 Win 侧复用同一脚本在云电脑出包。体积放宽是硬前提。

---

## 四、#5 测试账号实测 —— 暴露 2 个真实问题（P0，非缺凭据）

用 `__REDACTED_TEST_USER__/__REDACTED_TEST_PASS__` 跑 `e2e-full-chain`，Kaypal 鉴权通过，但暴露：

### 问题 1：余额门禁误伤商用账号（P0）

- 现象：六步闭环第 4 步「发布」返回 `INSUFFICIENT_BALANCE`（期望 `OCTOP_DEGRADED`）
- 根因（`agent-gateway.service.ts:109-131` balanceGate）：门禁用**本地 users 表**查 `ctx.userId`（kaypal 用户 id），查不到 → **fail-closed 拒绝**（「无法确认用户商用资格（本地无记录）」）
- 本质：**kaypal 用户体系（kaypal.cn）与本地 users 表（PG 5432）不是同一套**。商用账号 __REDACTED_TEST_USER__ 登录后 `ctx.userId` 是 kaypal 的 user id，本地 users 表没有该记录 → 商用账号被当成「无资格」拒掉。
- 影响：**真实商用账号无法执行高风险写工具（发布/CRM 等）**，这是商用发布硬阻断。
- 修复方向（需大王定）：
  - A. balanceGate 改用 kaypal 侧套餐/余额判定（调 kaypal.cn 用户接口），本地 users 表仅作 fallback
  - B. 本地 users 表同步 kaypal 用户（登录时 upsert）
  - C. 商用账号走白名单（kaypal 套餐 = FLAGSHIP 即放行）

### 问题 2：Memory 链路降级（P1）

- 现象：`recall` 返回 `degraded=true`，但实际召回了 1 条真实记忆（`count:1`）
- 根因：测试 `kctx = { tenantId, userId, agentId }` **没带 `kaypalAccessToken`** → `remote.search` 拿不到 token → tokenProvider 返回 undefined（环境没配 `KAYPAL_MEMORY_TOKEN/PASSWORD`）→ 回退 api-key → 401 → orchestrator 软降级
- 本质：测试 kctx 缺 token（测试设计问题），但暴露了「无 token 时 memory 静默降级到本地」的行为符合 fail-soft 预期
- 修复：测试 kctx 补 `kaypalAccessToken`（用 login 拿到的 accessToken），或环境配 `KAYPAL_MEMORY_TOKEN`

---

## 五、本轮结论

- #3 打包：**方案已出，规模 1.1GB + 架构反转 + 跨平台**，3 个决策点需大王拍板后动工（先 Mac 侧跑通，再云电脑出 Win）。
- #5 测试：**账号可用**，但暴露 2 个真实 P0/P1（balanceGate 误伤商用账号 + Memory token 缺失），前者是商用硬阻断，需大王定修复方向。
- 其余 #2/#6/#8/#9/#10 已修（上轮 commit `16ed46be`）。

**未宣称商用完成**，符合大王判定。待大王拍板：#3 三决策点 + #5 问题 1 修复方向。