# JIUZHANG AI 桌面壳 · 内置浏览器面板 设计说明

> 配套总览图：`桌面壳内置浏览器-总览.png`（浅色 / 深色整窗）
> 适用范围：Electron 桌面壳的顶部标签条、浏览器面板分区、控制条、审批浮层
> 更新日期：2026-09-04 · 对应提交：`be2df59a` → `c547ccc9`

---

## 1. 设计目标与原则

本轮改造解决三个连续暴露的问题：

| 问题 | 用户反馈 | 解法 |
|---|---|---|
| 面板调宽位置不可发现 | "拖拽的那个位置不明显，用户怎么能看出来" | 全高分区沟槽替代 40px 行内小把手 |
| 分区视觉不成体系 | "仿照 TraeWork Design 的视觉，浏览器分区上" | 复刻 TraeWork「两块卡片之间留一条背景色沟」的分区语言 |
| 浏览器入口太深 | "不要悬浮球了，更复刻 TraeWork 的逻辑和布局" | 浏览器成为顶栏标签条一等公民，悬浮球退役 |

四条贯穿原则：

1. **分区即把手**——不画分割线，让"空隙"本身可读、可拖。
2. **浏览器 = 标签**——入口、状态、关闭全部走浏览器标签语义（TraeWork 同款）。
3. **按需展开**——常驻只留必要元素；非常驻功能收进 ＋/ 菜单，用户选择才常驻。
4. **所见即所得**——每个壳组件带预览垫片（非 Electron 环境 mock 渲染同一份 HTML），改样式即在浏览器里看真件。

---

## 2. 整窗布局架构

```
┌──────────────────────────────────────────────────────────────┐
│ 顶部标签条 tab-strip（38px，通栏）                              │
│ 玖logo │ 业务标签×N │ 浏览器chip │ ＋ │ [半宽][加宽] │ ⋯        │
├──────────────────────────────┬─┬─────────────────────────────┤
│                              │沟│ 控制条 strip（40px 基础）     │
│  业务标签 WebContentsView     │槽│  [tab条26px 多tab时]         │
│  （宽度 = 窗口宽 − 面板宽 − 12）│12│  [快捷行30px 聚焦时]         │
│                              │px│  [活动条30px 有记录时]        │
│                              │  ├─────────────────────────────┤
│                              │  │ 面板 webContents（目标网页）  │
│                              │  │        └ 审批浮层（右下角）   │
└──────────────────────────────┴─┴─────────────────────────────┘
```

- 面板与业务区之间固定让出 **12px 沟槽视图**（`PANEL_GUTTER`），业务区 rightInset = 面板宽 + 12。
- 控制条是独立受信视图，高度按状态动态组合：基础 40 + tab 条 26（多 tab）+ 快捷行 30~160（地址栏聚焦）+ 活动条 30（有记录）。
- 审批浮层贴面板右下，仅在存在待批动作时可见。

## 3. 设计 Token（浅色 / 深色成套）

全部壳视图共用一套中性 token，`prefers-color-scheme: dark` 自动切换：

| Token | 浅色 | 深色 | 用途 |
|---|---|---|---|
| `--bg` | `#f2f3f5` | `#17181a` | 壳底色（顶栏/控制条/沟槽浅档） |
| `--card` | `#ffffff` | `#23242a` | 卡片面（激活标签、输入框、浮层） |
| `--text` | `#1f2329` | `#f2f3f5` | 主文字 |
| `--text-dim` | `#646a73` | `#bbbfc4` | 次级文字 |
| `--text-faint` | `#8f959e` | `#8f959e` | 占位/时间戳 |
| `--border` | `rgba(31,35,41,.09)` | `rgba(255,255,255,.10)` | 描边 |
| `--hover / --press` | `.06 / .10` | `.08 / .13` | 交互反馈底 |
| `--accent` | `#722ed1` | `#a171f0` | 主色（品牌紫） |
| `--gold` | `#b06e10` | `#e0a44a` | Octop / 工作区徽章 |
| 状态色 | `ok #34a853` · `warn #e6a23c` · `danger #f54a45` | 同左 | 就绪/待接管/阻断 |

字体：`13px/1 -apple-system, "PingFang SC", "Segoe UI", sans-serif`；数字时间戳用 `ui-monospace`。
动效统一 `.12s` 过渡；进度线流动 `2.2s` 循环、完成补满后 `420ms` 淡出。

## 4. 顶部标签条（tab-strip.html）

**结构**：品牌区（玖 logo + JIUZHANG AI）→ 业务标签 → 浏览器 chip → ＋ → 宽度预设 → ⋯。

- **业务标签**：前置类型圆点（紫=业务 / 金=Octop / 灰=web）；激活态白卡 + 轻投影 + 600 字重；关闭钮 hover 才显现；双击改名、右键菜单（重命名/绑定工作区/关闭）、WS 徽章点击弹绑定气泡。
- **浏览器 chip（一等公民）**：
  - 图标 = 站点 favicon 块（内置品牌映射，见 §7），标题 = 面板当前活动页标题；
  - 打开态：白卡激活样式 + hover 出 ×（收起面板）；收起态：降透明度（dim），点击恢复；
  - 数据源：主进程 `browser-panel:state` 广播直通顶栏（tab-strip-preload 订阅）。
- **＋ 菜单（浏览器式新建）**：新建工作区标签(⌘T) / 在浏览器面板打开当前页 / 新建浏览器标签 / 打开 Octop。
- **宽度预设**：仅面板可见时出现；半宽 = 窗口 50%，加宽 = 上限 60%。
- **⋯ 菜单**：纯设置（Octop 入口常驻勾选，localStorage `octopPinned` 持久化）。
- 按需展开原则：Octop、绑定、宽度都不常驻；入口在 ＋/⋯/右键里。

## 5. 分区沟槽（browser-panel-gutter.html）

TraeWork 分区语言的直接复刻，实测校准过的关键值：

| 属性 | 值 | 依据 |
|---|---|---|
| 沟宽 | 12px | 4px 灰线读不出"可拖"；12px 是热区下限 |
| 沟底色 | 浅 `#d8dbe1` / 深 `#0d0e11` | 与业务底色差 ≥25 明度档才读作"空隙"（首版差 15 档被否） |
| 握柄 | 4×34px 胶囊，`rgba(31,35,41,.46)` | 常态可见、居中、暗示"拖我" |
| hover/拖拽 | 整条 `rgba(114,46,209,.14)` + 握柄变主色 5×52px | 反馈即时 |
| 面板左缘 | 1px 卡片边 + 向左渐变投影 | 面板读作浮在业务区之上的卡片 |

**交互**：拖拽全程由主进程 16ms 轮询系统光标驱动（视图只有 12px 宽，渲染层 pointer 一出界就断流——这是"拖到一半断"的根因）；按下记录 grabOffset 防跳宽；松手一次性写 store（electron-store 同步落盘，不逐帧写）；1.2s 光标静止看门狗兜底"松手点在别的视图上"；双击回默认 480px。

**宽度记忆**：`browserPanelWidth.<workspaceId>` 按工作区持久化，无值回落全局 `browserPanelWidth`，再无则 480；范围 = **手机比例宽** ~ 窗口 60%；业务标签切换时主进程按新 scope 重读并重排。

**最窄 = 手机视口比例**：往左滑的落点不再是固定 360，而是 `页面区高 × 393:852`（iPhone 15 Pro 逻辑视口）——页面区高按 relayout 真实几何现算（窗口高 − 顶部通栏 38 − 控制条当前行高，tab 条/活动条占位一并扣除），所以窗口越高手机形态越宽，面板页面永远是块标准手机屏。夹取三保险：绝对兜底 320（矮窗可用性）、封顶 560（超高窗不过宽）、可用宽 = 60% 上限 − 沟槽（窄窗让位业务区）；磁吸点与顶栏预设共用同一 clamp，拖近 ±14px 自动吸上。真机核验：344×745 = 0.4617 ≈ 0.4613。

## 6. 控制条（browser-control-strip.html）

自上而下四行（后三行按需出现）：

1. **tab 条**（多 tab 时）：白卡激活态、hover 出关闭、站点 favicon。
2. **工具行**：后退/前进/刷新（禁用态 30% 透明）、地址栏、状态文字（就绪绿/加载中琥珀/需接管红）、**接管/交还**（仅 AI 代操作开启时露面：系统控制态=幽灵「接管」，接管中=主色实底「交还」）、AI 代操作（开启=主色实底）、收起 ×。
3. **快捷区**（地址栏聚焦时展开，高度按内容 30~160px 上送主进程）：
   - 建议下拉：从活动流 nav 记录推导最近访问（按 host 去重、留 6 条），实时过滤前 4 条；无匹配给「搜索「词条」」百度兜底；点建议行 `pointerdown` 拦截防失焦竞态；回车优先采纳首条建议。
   - 快捷打开行：**只放系统有获客功能的四个平台**——抖音 / 小红书 / 视频号 / 快手（与评论获客的 `AcquisitionPlatform` 一一对应；每个 chip tooltip 注明联动去向）。公众号/百度这类没有系统功能承接的通用站不在快捷行（搜索兜底仍在地址栏建议下拉里）。
   - **左侧系统联动**：手动导航（快捷 chip / 地址栏回车）命中平台站时，主进程广播
     `browser-panel:platform-focus` 给业务视图，前端桥组件把左侧切到该平台的评论获客页
     （`/engagement/comment-acquisition?platform=…`，已停在该页则原地切平台不同步刷页）——
     点小红书，左边就是小红书获客。AI 自动导航走 broker 不经此路，不劫持用户左侧页面；
     公众号/百度无对应获客页 → 不联动。
4. **活动条**（有记录时常驻，TraeWork「控制台日志」语义）：
   - 收起态一行：色点（approve 绿 / reject 红 / nav·open 紫 / tab·hide 灰）+ 最近一条 + 时间 + 计数 + 清除；
   - 点击展开最近 8 条完整事件流；
   - 数据源：主进程环形日志（30 条容量、下发 15 条、最新在前），记录点覆盖 open/hide/show/navigate/back/forward/reload/切关 tab/批准/拒绝；内部操作 silent 记录避免重复广播。

**加载进度线**：控制条顶部 2px 渐变线（`#722ed1→#b37feb`），`starting` 流动、`ready` 补满淡出——补上"页面加载无反馈"的洞。

## 7. favicon 策略

内置 host 正则 → 品牌色底 + 字母块（中文字形渲染最稳，不依赖网络图标）：

| 站点 | 底色 | 字 |
|---|---|---|
| 抖音 | `#161823` | 抖 |
| 小红书 | `#ff2442` | 红 |
| 微信/公众号 | `#07c160` | 微/公 |
| 视频号 | `#07c160` | 视 |
| 百度 | `#2932e1` | 百 |
| B站 | `#fb7299` | B |
| 淘宝/京东/知乎/GitHub | `#ff5000`/`#e1251b`/`#0084ff`/`#24292f` | 淘/京/知/G |
| 未知站点 | `#b9bec6` 灰 | 域名首字母 |

尺寸：顶栏 chip 14×14（圆角 4），控制条 tab 13×13（圆角 3.5）。

## 8. 控制权模型与审批浮层（browser-approval-overlay.html）

**控制权模型（TraeWork 式）**——AI 代操作开启后，默认就是「系统控制」：

- **系统控制（`control='system'`，默认）**：AI 写的每一步（导航/点击/输入/按键/标签页）在签确认单的那一刻即经面板 owner 通道**自动批准**（审计标 `via: auto-system-control`），后端 executor 拿到 `autoApproved` 回执直接执行——面板上能看到 AI 在动，但不弹任何审批卡。
- **接管（点控制条「接管」）**：`control='user'`，AI 新签单恢复排队（浮层卡片 + 琥珀横幅「你已接管页面，AI 暂停中」），用户可自由手动操作页面，也可对排队单逐条批准/拒绝（细粒度旁路保留）。
- **交还（点「交还」）**：`control='system'`，排队中的单全部自动放行（`via: auto-release-control`），AI 从断点继续；活动条记录「已放行 N 个排队操作」。
- 边界（有意为之）：接管瞬间在途的已批准单会执行完（亚秒级），不追回；自动批准失败一律回落人工审批路径（fail-safe，绝不升权）。

**审批浮层**（接管态下即「排队队列」）：

- 位置：面板右下角，距边 8px，最大高 220px；仅待批动作 >0 时可见。
- 卡片：白卡 + 主色「批准」实底 + 幽灵「拒绝」；动作类型加粗、URL 灰字截断；头部橙点 + 计数徽章；接管态顶部加横幅说明卡的去处。
- 安全姿态：批准权仍在 owner 通道（自动批准走主进程 `approveOwnerSide`，Agent 侧 API 依旧不得自批）；批准/拒绝 IPC `approvalOnly`（只有浮层自身 webContents 可发），第三方页面无法伪造；接管/交还通道 `stripOnly`（只有本地控制条按钮能替用户交权）。

## 9. 工程与安全要点

- **视图分层**：子视图按加入顺序绘制——控制条先加（在下层），审批浮层最后加（置顶）；沟槽视图与控制条共用 strip preload 的白名单 IPC（`isStripSender` 认两者为本地受信 chrome）。
- **面板页零特权**：第三方面板 webContents 无 preload、sandbox、contextIsolation；导航类通道 `stripOnly`，开面板/查状态 `strip ∨ trusted`。
- **通道清单**：`set-width`（拖拽/双击复位）、`begin-resize`/`end-resize`（拖拽会话）、`expand-strip`（快捷区高度）、`clear-activity`（活动清除）、`tab-strip:browser-show/hide/open-current/new-tab/width`（顶栏入口五件套）、`take-control`/`release-control`（控制权接管/交还，stripOnly）。`publicState.control` 随状态流下发（控制条按钮/浮层横幅共同消费）。
- **打包白名单修复**：browser-panel 整套运行时文件（14 个，含沟槽视图）此前不在 `build.files`，已补齐——否则打包版缺模块。

## 10. 验证与质量

- 回归：面板管理 spec **49 项全绿**（含控制权模型三例：默认系统控制、接管/交还幂等、浮层 control 重推）；wiring spec **19 项全绿**（新增系统控制自动批准、接管排队、交还批量放行、无 getControl fail-safe 四例）；审批/桥接/broker/registry 等桌面 spec 全过；后端 bridge/executor/loop 合并 spec **124 项全绿**（含 autoApproved 透传与 goto/click 直接执行两例）。
- 真机端到端（直击运行中的桌面桥 + CDP 点控制条）：系统控制签单 `autoApproved=true` 且单即 `approved`、带票执行后面板真实换页（example.com→example.org，零人工干预）；点「接管」→ 签单 `pending` 排队、浮层 1 卡 + 横幅；点「交还」→ 排队单转 `approved`、队列清零、活动条「已放行 1 个排队操作」。
- 视觉：无头 Chromium 按 `colorScheme: light/dark` 对顶栏、控制条、沟槽、审批浮层逐视图截图核对成套；总览图即其产物。
- 预览入口（同源带登录态）：`/shell-overview.html`、`/shell-preview.html`、`/browser-panel-preview.html`、`/tab-strip.html`、`/browser-control-strip.html`。

## 11. 已知边界与后续可选

- 全局快捷键（⌘⇧B 开合面板）在方案阶段提出，本轮未实现——顶栏 chip 已承担常驻入口。
- favicon 为品牌映射块，非真实站点图标文件；接入 `webContents` 的 favicon 拉取可作后续。
- 活动日志为内存环形，重启清空；如需跨会话审计应落后端事件表。
- 深色壳跟随系统自动切换，暂无应用内手动开关。
