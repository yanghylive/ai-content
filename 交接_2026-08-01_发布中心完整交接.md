# JIUZHANG AI 多平台发布中心 — 完整交接文档

> 日期：2026-08-01
> 分支：`codex/content-workspace-20260721`
> 项目路径：`/Users/yanghy/Documents/New project/ai-content`
> 当前 HEAD：`e778adf`

---

## 零、完整访问路径

### 用户导航路径（前端 3010）

```
登录
└── 首页 (/)
    ├── 左侧导航 → 今天
    │   └── /today
    ├── 左侧导航 → 助手
    │   └── /agent
    ├── 左侧导航 → 客户
    │   └── /customer
    ├── 左侧导航 → 内容  ← 文章反抓入口在这里
    │   ├── /content          （选题/内容生成/视频工坊/素材库/文章反抓/模板与风格/发布）
    │   ├── /content/topics   （选题）
    │   ├── /content/articles （内容生成）
    │   ├── /content/video    （视频工坊）
    │   ├── /materials        （素材库）
    │   ├── /distribution-v2/scrape  （文章反抓：输入 URL 提取内容）
    │   ├── /content/templates       （模板与风格）
    │   └── /distribution     （发布）
    ├── 左侧导航 → 发布中心
    │   ├── /distribution              （发布看板 + Bridge 状态条 + 统计）
    │   ├── /distribution-v2/publish-video   （发布视频：5 步向导）
    │   ├── /distribution-v2/publish-article （发布图文：5 步向导）
    │   ├── /distribution-v2/tasks     （任务列表：5 秒轮询）
    │   ├── /distribution-v2/articles  （图文库）
    │   └── /distribution/compliance   （合规检查）
    ├── 左侧导航 → 发布 → 发布账号
    │   ├── /platforms         （平台账号管理：9 平台添加/删除/扫码登录）
    │   ├── /platforms/new     （添加账号：选择平台类型 1-9）
    │   └── /platforms-v2      （平台中心）
    └── 左侧导航 → 我的
        └── /account-v2        （登录/账户设置）
```

### 直达链接

| 功能 | URL |
|------|-----|
| 登录 | http://127.0.0.1:3010/account-v2 |
| 发布中心 | http://127.0.0.1:3010/distribution |
| 发布视频 | http://127.0.0.1:3010/distribution-v2/publish-video |
| 发布图文 | http://127.0.0.1:3010/distribution-v2/publish-article |
| 任务列表 | http://127.0.0.1:3010/distribution-v2/tasks |
| 文章反抓 | http://127.0.0.1:3010/distribution-v2/scrape |
| 平台管理 | http://127.0.0.1:3010/platforms |
| 添加账号 | http://127.0.0.1:3010/platforms/new |
| 平台中心 | http://127.0.0.1:3010/platforms-v2 |
| 内容首页 | http://127.0.0.1:3010/content |

### 后端 API（3011）

| 端点 | 方法 | 路径 | 是否公开 |
|------|------|------|---------|
| 健康检查 | GET | /api/health | ✅ |
| Bridge 状态 | GET | /api/local-bridge/status | ✅ @Public |
| 平台能力 | GET | /api/local-bridge/capabilities | ✅ @Public |
| 账号列表 | GET | /api/local-bridge/accounts | ❌ |
| 执行发布 | POST | /api/local-bridge/publish | ❌ |
| 任务状态 | GET | /api/local-bridge/tasks/:id | ❌ |
| 取消任务 | POST | /api/local-bridge/tasks/:id/cancel | ❌ |
| 发布历史 | GET | /api/local-bridge/history | ❌ |
| 重试发布 | POST | /api/local-bridge/retry | ❌ |
| 删除记录 | POST | /api/local-bridge/delete | ❌ |
| 文章反抓 | POST | /api/local-bridge/scrape | ❌ |
| 打开账号 | POST | /api/local-bridge/accounts/open | ❌ |
| 刷新账号 | POST | /api/local-bridge/accounts/refresh | ❌ |

### 后端调试命令

```bash
# 健康检查
curl -s http://127.0.0.1:3011/api/health | python3 -m json.tool

# Bridge 状态（需要 trace header）
curl -s -H 'x-jiuzhang-trace-id: test' http://127.0.0.1:3011/api/local-bridge/status | python3 -m json.tool

# 9 平台能力
curl -s -H 'x-jiuzhang-trace-id: test' http://127.0.0.1:3011/api/local-bridge/capabilities | python3 -m json.tool

# 账号列表（需要登录 Cookie）
curl -s -b 'session=xxx' http://127.0.0.1:3011/api/auto-upload/accounts | python3 -m json.tool

# 任务列表（需要登录 Cookie）
curl -s -b 'session=xxx' http://127.0.0.1:3011/api/auto-upload/tasks?page=1\&pageSize=10 | python3 -m json.tool

# 文章反抓（需要登录 Cookie）
curl -s -X POST -b 'session=xxx' -H 'Content-Type: application/json' \
  -d '{"url":"https://example.com/article"}' \
  http://127.0.0.1:3011/api/auto-upload/scrape-article | python3 -m json.tool
```

### 数据库直接查询

```bash
# durable 任务状态
sqlite3 backend/prisma/kaypal-ai.sqlite \
  "SELECT relatedId, status, reasonCode, attemptCount, userMessage FROM runtime_executions WHERE taskType='auto-upload-publish-record-v1' ORDER BY createdAt DESC LIMIT 10;"

# 平台账号
sqlite3 backend/prisma/kaypal-ai.sqlite \
  "SELECT id, type, name, status FROM accounts WHERE deletedAt IS NULL ORDER BY createdAt DESC LIMIT 10;"
```

---

## 一、项目一句话

JIUZHANG AI 的多平台内容发布中心：从 Web 页面发起 → 通过 Local Bridge 或直连后端 → 调度本地浏览器（Playwright/CDP）→ 发布到 9 个平台。

---

## 二、启动方法

### 后端（3011）

```bash
cd /Users/yanghy/Documents/New\ project/ai-content/backend

# 首次或 schema 变更后
npm run db:prepare:sqlite       # 生成 SQLite schema
npx prisma generate --schema prisma/schema.sqlite.prisma
npm run build

# 启动
env -u HTTP_PROXY -u HTTPS_PROXY -u http_proxy -u https_proxy \
  KAYPAL_CREDENTIAL_MASTER_KEY="$(cat ~/Library/Application\ Support/ai-content-desktop/credential-master-key)" \
  node dist/main.js
```

### 前端（3010）

```bash
cd /Users/yanghy/Documents/New\ project/ai-content/frontend
env -u NODE_OPTIONS -u HTTP_PROXY -u HTTPS_PROXY -u http_proxy -u https_proxy npm run dev
```

### 验证

```bash
# 后端
curl -s http://127.0.0.1:3011/api/health | python3 -c "import sys,json; d=json.load(sys.stdin); print(d['data']['checks']['database'])"
# → {'ok': True, 'status': 'connected'}

# Local Bridge（需要 trace header）
curl -s -H 'x-jiuzhang-trace-id: test' http://127.0.0.1:3011/api/local-bridge/status
# → online=True, actions=12

curl -s -H 'x-jiuzhang-trace-id: test' http://127.0.0.1:3011/api/local-bridge/capabilities
# → 9 个平台
```

---

## 三、今天做了什么（2026-08-01，24 个提交）

### Phase 1：只读 Local Bridge（3 个提交）
- Electron main 读 httpOnly Cookie 转发到后端
- 3 项只读 action：CHECK_STATUS / LIST_CAPABILITIES / LIST_ACCOUNTS
- nonce/traceId 防重放，origin 白名单

### Phase 2：平台注册表 + 5 平台 adapter 化（7 个提交）
- `platform-publish.service.ts` 从 2325 行拆成 5 个独立 adapter
- `PlatformAdapterRegistry` 统一管理 capability + publish factory
- 新平台 = 加一个文件注册一行，不动主干

### Phase 3A：写操作全链路（5 个提交）
- 3A-1：安全契约（3 项写 action + fail-closed 503）
- 3A-2：数据库基础（幂等唯一约束 + worker CAS 原语 + P2002 精确分类）
- 3A-3：confirmation CAS 与 task create 同事务
- 3A-4：durable worker 5s 轮询 + heartbeat + completion CAS + 真实浏览器发布
- 3A-5：Desktop/frontend 同步 3 项写 action

### 功能差距补齐（6 个提交）
- P5 图片预处理：远程图→data URL/本地文件（防盗链）
- P4 文章反抓：URL→结构化内容（cheerio 解析）
- Worker 重试策略：max attempt=3 + dead letter
- P2 公众号订阅号：mp.weixin.qq.com 浏览器发布
- P3 加平台：微博/知乎/头条（5→9 平台）
- 补齐剩余 4 项差距（action 6→12、发布中枢 hook、文章反抓升级、图片预处理升级）

### 前端交互接入（7 个提交）
- 平台列表 5→9（publish-flow + platforms-center）
- 任务状态 5s 轮询（distribution-tasks + publish-center）
- 文章反抓页面（/distribution-v2/scrape）
- Bridge 状态条增强（平台数显示 + API 降级）
- 平台类型 6-9 前后端全量打通
- 文章反抓入口在内容页面
- Local Bridge status/capabilities @Public

### Bug 修复（2 个提交）
- SQLite ALTER TABLE 不支持 CURRENT_TIMESTAMP 默认值
- typed-client require() ESLint 错误

---

## 四、架构图

```
┌─────────────────────────────────────────────────────────┐
│  Web 前端 (3010, Next.js)                                │
│  ├─ 发布中心 /distribution                                │
│  ├─ 发布视频 /distribution-v2/publish-video               │
│  ├─ 发布文章 /distribution-v2/publish-article             │
│  ├─ 任务列表 /distribution-v2/tasks                       │
│  ├─ 文章反抓 /distribution-v2/scrape                      │
│  ├─ 平台管理 /platforms                                   │
│  └─ 内容 /content                                        │
└──────────┬──────────────────────────────────────────────┘
           │ fetch (业务 API)        window.postMessage (Local Bridge)
           ▼                         ▼
┌──────────────────────┐    ┌──────────────────────────────┐
│  NestJS 后端 (3011)   │    │  Electron 桌面端              │
│  ├─ Local Bridge      │◄───┤  ├─ preload.js (postMessage) │
│  │  ├─ status         │    │  ├─ main.js (IPC + Cookie)   │
│  │  ├─ capabilities   │    │  └─ local-bridge.js (转发)    │
│  │  ├─ accounts       │    └──────────────────────────────┘
│  │  ├─ publish        │
│  │  ├─ tasks/:id      │
│  │  ├─ tasks/:id/cancel
│  │  └─ ... 6 more     │
│  ├─ AutoUploadService │
│  │  ├─ publishBatch() │
│  │  ├─ executeClaimedDurableTask()
│  │  └─ resolvePlatformName() (9 平台)
│  ├─ DurablePublishCommandCoordinator
│  │  ├─ claimOrLoad()          (幂等 claim)
│  │  └─ executeDurablePublish() (confirmation+task 同事务)
│  ├─ DurablePublishWorker
│  │  ├─ 5s 轮询 claimNextQueued
│  │  ├─ 30s heartbeat 续期
│  │  ├─ completion CAS
│  │  └─ max attempt=3 + dead letter
│  ├─ PublishRecordStore
│  │  ├─ createClaim / claimNextQueued / completeClaimedTask
│  │  ├─ renewLease / reclaimStaleClaims
│  │  └─ resolveOwnerScope (fail-closed)
│  ├─ PlatformAdapterRegistry
│  │  └─ 9 adapter 注册 (capability + factory)
│  ├─ RemoteImagePreprocessor
│  │  └─ 远程图→data URL/本地文件
│  └─ ArticleScraperService
│     └─ URL→结构化内容
├────────────────────────┤
│  Prisma (SQLite)       │
│  └─ runtime_executions │
│     ├─ idempotencyKey  │
│     ├─ requestHash     │
│     ├─ confirmationId  │
│     ├─ authSessionId   │
│     ├─ claimToken      │
│     ├─ claimedAt       │
│     ├─ leaseExpiresAt  │
│     └─ attemptCount    │
└────────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│  Playwright/CDP 浏览器引擎        │
│  └─ 9 平台 adapter                │
│     ├─ xiaohongshu (小红书)       │
│     ├─ wechat-channel (视频号)    │
│     ├─ wechat-official (公众号)   │
│     ├─ douyin (抖音)              │
│     ├─ kuaishou (快手)            │
│     ├─ bilibili (B站)             │
│     ├─ weibo (微博)               │
│     ├─ zhihu (知乎)               │
│     └─ toutiao (头条)             │
└──────────────────────────────────┘
```

---

## 五、9 个平台

| 类型 | 平台键 | 中文名 | 内容类型 | 发布方式 |
|------|--------|--------|---------|---------|
| 1 | xiaohongshu | 小红书 | 图文+视频 | CDP 浏览器 |
| 2 | wechat-channel | 视频号 | 图文+视频 | CDP 浏览器 |
| 3 | douyin | 抖音 | 图文+视频 | CDP 浏览器 |
| 4 | kuaishou | 快手 | 图文+视频 | CDP 浏览器 |
| 5 | bilibili | B站 | 视频 | CDP 浏览器 |
| 6 | weibo | 微博 | 图文+视频 | CDP 浏览器 |
| 7 | zhihu | 知乎 | 图文 | CDP 浏览器 |
| 8 | toutiao | 今日头条 | 图文 | CDP 浏览器 |
| 9 | wechat-official | 微信公众号 | 图文 | CDP 浏览器 + 官方 API |

---

## 六、Local Bridge 12 项 Action

| Action | 方法 | 路径 | 说明 | 是否公开 |
|--------|------|------|------|---------|
| CHECK_STATUS | GET | /api/local-bridge/status | 执行器状态 | ✅ @Public |
| LIST_CAPABILITIES | GET | /api/local-bridge/capabilities | 9 平台能力 | ✅ @Public |
| LIST_ACCOUNTS | GET | /api/local-bridge/accounts | 平台账号列表 | ❌ 需登录 |
| OPEN_ACCOUNTS | POST | /api/local-bridge/accounts/open | 打开账号登录页 | ❌ 需登录 |
| REFRESH_ACCOUNTS | POST | /api/local-bridge/accounts/refresh | 刷新登录态 | ❌ 需登录 |
| EXECUTE_PUBLISH | POST | /api/local-bridge/publish | 创建发布任务 | ❌ 需登录 |
| GET_TASK_STATUS | GET | /api/local-bridge/tasks/:id | 任务状态 | ❌ 需登录 |
| CANCEL_TASK | POST | /api/local-bridge/tasks/:id/cancel | 取消任务 | ❌ 需登录 |
| LIST_PUBLISH_HISTORY | GET | /api/local-bridge/history | 发布历史 | ❌ 需登录 |
| RETRY_PUBLISH | POST | /api/local-bridge/retry | 重试失败任务 | ❌ 需登录 |
| DELETE_PUBLISH_RECORD | POST | /api/local-bridge/delete | 删除记录 | ❌ 需登录 |
| SCRAPE_ARTICLE | POST | /api/local-bridge/scrape | 文章反抓 | ❌ 需登录 |

---

## 七、发布链路（完整流程）

```
用户点"发布"
→ publish-flow.tsx 提交 payloads 到 POST /auto-upload/publish
→ AutoUploadService.publishBatch()
  ├─ resolvePublishApprovalScope() (confirmation 验证)
  ├─ consumeHighRiskApproval() (确认 CAS，单次消费)
  └─ executeDurablePublishRecord()
     ├─ preprocessImages() (远程图→data URL)
     ├─ createDurablePublishRecord() (durable 任务)
     └─ publishBatchWithTracking() → autoUploadClient.publishBatch()
        → RuntimeOrchestrator → ExecutorRouter → PlatformPublishService
        → adapter.buildVideoPublishPlan() / buildImageTextPublishPlan()
        → Playwright 浏览器操作 → 平台发布
     └─ publishRecordStore.updateResult() (更新结果)

或走 Local Bridge：
用户通过 Electron 桌面端发起
→ JZ_BRIDGE_EXECUTE_PUBLISH
→ LocalBridgeService.executePublish()
→ DurablePublishCommandCoordinator.executeDurablePublish()
  ├─ prisma.$transaction:
  │  ├─ consumeHighRiskApproval(tx) (confirmation CAS)
  │  └─ createClaim(tx) (queued 任务)
  └─ 返回 { accepted, taskId, status: 'waiting', idempotencyKey }

DurablePublishWorker (5s 轮询):
→ claimNextQueued() (CAS 抢占)
→ AuthRequestContextService.run(restoreScope)
→ executeClaimedDurableTask()
→ publishBatchWithTracking() → 浏览器发布
→ updateResult()
→ completeClaimedTask() (completion CAS)
```

---

## 八、关键文件清单

### 后端核心

| 文件 | 说明 |
|------|------|
| `src/modules/local-bridge/local-bridge.service.ts` | Local Bridge 业务逻辑，executePublish 接 coordinator |
| `src/modules/local-bridge/local-bridge.controller.ts` | 12 项路由，status/capabilities @Public |
| `src/modules/local-bridge/local-bridge.contract.ts` | 12 项 action 定义 + DTO |
| `src/modules/auto-upload/auto-upload.service.ts` | 发布编排，executeClaimedDurableTask + resolvePlatformName(9) |
| `src/modules/auto-upload/auto-upload.client.ts` | 4600 行，账号/物料/凭证/浏览器自动化 |
| `src/modules/auto-upload/durable-publish-command.coordinator.ts` | 幂等 claim + confirmation 同事务 |
| `src/modules/auto-upload/durable-publish.worker.ts` | 5s 轮询 worker + heartbeat + dead letter |
| `src/modules/auto-upload/publish-record.store.ts` | durable 持久化 + CAS 原语 + fail-closed scope |
| `src/modules/auto-upload/remote-image-preprocessor.ts` | 远程图→data URL/文件 |
| `src/modules/auto-upload/article-scraper.service.ts` | URL→结构化内容 |
| `src/modules/platform-registry/platform-adapter.interface.ts` | 接口定义（3 种 adapter 能力） |
| `src/modules/platform-registry/platform-registry.module.ts` | 9 adapter 注册 |
| `src/modules/runtime/platforms/publishing/*.adapter.ts` | 9 个平台 adapter（含选择器） |
| `src/modules/auth/risk-policy.service.ts` | confirmation CAS（支持 tx 参数） |
| `src/prisma/prisma.service.ts` | SQLite 建表/升级 + ensureSqliteColumn |
| `prisma/schema.prisma` | RuntimeExecution durable 字段 + 幂等约束 |

### 前端核心

| 文件 | 说明 |
|------|------|
| `src/lib/local-bridge/actions.ts` | 12 项 action 常量 |
| `src/lib/local-bridge/client.ts` | postMessage 通信客户端 |
| `src/lib/local-bridge/typed-client.ts` | typed 便捷方法封装 |
| `src/lib/local-bridge/use-local-bridge.ts` | 状态 hook（API 降级） |
| `src/lib/local-bridge/hooks/use-publish-hub.ts` | 多平台并发发布 hook |
| `src/app/(dashboard)/distribution/publish-flow.tsx` | 发布流程（9 平台映射） |
| `src/app/(dashboard)/distribution/distribution-tasks.tsx` | 任务列表（5s 轮询） |
| `src/app/(dashboard)/distribution/publish-center.tsx` | 发布中心首页（轮询） |
| `src/app/(dashboard)/distribution/local-bridge-status.tsx` | Bridge 状态条 |
| `src/app/(dashboard)/distribution-v2/scrape/page.tsx` | 文章反抓页面 |
| `src/app/(dashboard)/platforms/platform-accounts.tsx` | 平台账号管理（9 平台） |
| `src/app/(dashboard)/platforms/platforms-center.tsx` | 平台中心（9 平台标签） |
| `src/app/(dashboard)/content/page.tsx` | 内容页面（含反抓入口） |

### 桌面端

| 文件 | 说明 |
|------|------|
| `desktop/local-bridge.js` | Electron main 桥接（POST + 动态路径 + sanitizer） |
| `desktop/preload.js` | Electron preload（postMessage 转发） |
| `desktop/test/local-bridge.test.cjs` | 桌面端测试（11/11） |

---

## 九、测试

### 后端

```bash
cd backend
npx jest --runInBand modules/auto-upload modules/local-bridge modules/platform-registry modules/runtime/platforms/publishing common/interceptors/transform.interceptor.spec.ts
# 21 suites / 226 tests 全绿

npm run lint -- --max-warnings=0
# 0 错

node_modules/.bin/tsc --project tsconfig.json --noEmit
# 0 错
```

### 桌面端

```bash
cd desktop
npm run test:local-bridge
# 11/11 全绿
```

### 前端

```bash
cd frontend
env -u NODE_OPTIONS npx tsc --noEmit
# 0 错
```

### CI

云端 Quality Gates 每次推送自动跑 8 门：
1. Demo 舱隔离规范
2. 新版 UI 锚点
3. 第三方许可证
4. 前端 ESLint
5. 前端 tsc
6. 后端 ESLint
7. 后端 tsc
8. 桌面只读桥单测

---

## 十、已知限制 / 待办

### 需要登录才能用的功能
- 账号管理（/platforms）— 添加/删除/扫码登录平台账号
- 发布视频/文章 — 提交发布请求
- 任务列表 — 查看发布任务状态
- 文章反抓 — 提取文章内容
- Local Bridge 大部分 action（除了 status/capabilities）

### 需要 Electron 桌面端才能用的功能
- Local Bridge postMessage 通信（普通浏览器没有 preload 脚本）
- 打开平台登录页（需要 Electron 打开新窗口）
- 二维码扫码登录（需要 Electron 显示二维码）

### 待做
- **协作式取消**：CANCEL_TASK 现在返回 409 CANCELLATION_UNSUPPORTED，需要真正中断浏览器操作
- **Worker 单测**：DurablePublishWorker 本身没有单元测试（store 原语有）
- **真 Electron E2E**：集成测试过了但没起真 Electron 跑过一次
- **发布中枢 UI**：usePublishHub hook 写了但没接入任何页面
- **auto-upload.client.ts 拆分**：4600 行还是太大，平台逻辑已拆走但账号/物料/凭证部分还在

### 选择器注意事项
- 微博/知乎/头条/公众号的 DOM 选择器基于 2026 年界面结构，平台改版后需要更新
- 微信公众号（wechat-official）的 UEditor 编辑器通过 iframe 操作，需要实际测试验证
- 知乎的 Draft.js 编辑器必须用 `document.execCommand('insertText')` 写入，不能直接赋值 innerHTML

---

## 十一、常用调试命令

```bash
# 查看后端日志（screen）
screen -r ai-content-frontend

# 查看 Local Bridge 状态
curl -s -H 'x-jiuzhang-trace-id: test' http://127.0.0.1:3011/api/local-bridge/status | python3 -m json.tool

# 查看平台能力
curl -s -H 'x-jiuzhang-trace-id: test' http://127.0.0.1:3011/api/local-bridge/capabilities | python3 -m json.tool

# 查看数据库
sqlite3 backend/prisma/kaypal-ai.sqlite "SELECT id, status, reasonCode, attemptCount FROM runtime_executions WHERE taskType='auto-upload-publish-record-v1' ORDER BY createdAt DESC LIMIT 10;"

# 查看 git 提交历史
git log --oneline codex/content-workspace-20260721 --since="2026-08-01T00:00:00"
```

---

## 十二、提交历史（24 个，2026-08-01）

```
e778adf feat(publishing): 平台类型 6-9 前后端全量打通
a0b1706 fix(frontend): fetchPlatformCount 改用 fetch 带 trace-id header
03728bd fix(frontend): 反抓卡片 tint 从不存在的 kx-t-teal 改为 kx-t-slate
4901a02 fix(frontend): 文章反抓图标对齐其他卡片风格
4875024 fix(frontend): 文章反抓入口从发布中心移到内容页面
622518f fix(frontend): Bridge 状态条 API 降级 + 反抓页面入口
4ff78a1 fix(bridge): Local Bridge status/capabilities 路由加 @Public
9921a3a feat(frontend): 4 项前端交互接入
6b6b7e2 fix(prisma): SQLite ALTER TABLE 不支持 CURRENT_TIMESTAMP 默认值
ae13c31 feat(bridge): 补齐 MultiPost 剩余 4 项差距
8475743 feat(publishing): P3 加平台 微博/知乎/头条（5→9 平台）
1f2abdd feat(publishing): P2 公众号订阅号浏览器发布
ff6347a fix(frontend): 修复 typed-client require() ESLint 错误
c2f0dff feat(publishing): P4 文章页内反抓（URL→结构化内容）
1acac6b feat(frontend): Local Bridge typed client 封装
2aa84d9 feat(publishing): worker 重试策略 + max attempt + dead letter
22e0c2c feat(publishing): P5 图片预处理（远程图→data URL 防盗链）
97fc832 test(bridge): Phase 3A 全链路集成测试
1b3c706 feat(bridge): 同步 Desktop/frontend 三项 Local Bridge 写操作
f298228 feat(publishing): durable worker 轮询循环 + completion CAS
35b0adb feat(publishing): confirmation CAS 与 task create 同事务
159d8eb feat(publishing): durable 幂等任务数据库基础与 worker CAS
cffdf8e feat(local-bridge): Phase 3A 安全写操作契约
dd38355 refactor(publishing): registry factory 装配发布适配器
```
