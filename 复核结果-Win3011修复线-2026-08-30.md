# 完整复核结果：Win 3011 修复线（2026-08-30）

复核项目根目录：`/Users/yanghy/Documents/New project/ai-content`

结论先行：**需修复，当前 Windows v1.1.101 不可宣称为可发布安装包。** 代码层面的 SQLite 自愈和云端兜底上报链路有真实证据，但当前发布的 Windows NSIS 包混入了 macOS arm64 的 Node/Chromium 资源，干净 Windows 用户无法依靠包内运行时启动后端。这是 P0 发布阻断，优先级高于“15/15 门禁通过”。

## 1. 复核范围与仓库状态

- 目标提交存在且顺序符合清单：`818134c9`（v1.1.101）、`c526f46d`、`a6ef57ad`（v1.1.100）、`7808a14f`（v1.1.99）。
- 目标修复文件没有发现未提交改动；仓库整体仍有用户已有的前端、文档和验收证据改动，未做回滚。
- `node --check desktop/cloud-api.js desktop/main.js desktop/auto-updater.js` 通过；相关 diff `git diff --check` 通过。

## 2. P0：Windows 安装包资源错误，门禁出现假绿

实测：

```text
BUILD_PLATFORM=win-x64 node desktop/scripts/check-full-installer-assets.js
- packaged resources missing bundled Node runtime: runtime/node/bin/node.exe
- packaged resources missing bundled Playwright Chromium executable under playwright-browsers
- ... missing Playwright Chromium executable for win-x64 ...
Full installer asset check failed (post, target=win-x64).
```

解包生产/本地同一份 `JIUZHANG AI 内容创作平台 Setup 1.1.101.exe` 的 `$PLUGINSDIR/app-64.7z` 后确认：

- `resources/runtime/node/bin/node.exe` 数量为 0；只有 macOS 路径 `resources/runtime/node/bin/node`。
- `resources/playwright-browsers` 只有 `chromium/chrome-mac-arm64`，没有 `chrome.exe`。
- `desktop/dist/win-arm64-unpacked` 虽有 Windows 资源，但远端 `latest.yml` 指向的是 Windows x64 NSIS 包，不能替代该包。
- `desktop/main.js:873-925` 在打包 Windows 上只接受 `runtime/node/bin/node.exe`；缺失时明确返回 `null`，后端不会启动。
- `desktop/main.js` 的浏览器运行时也按 Windows 可执行文件查找，当前包无法满足。

原因是 `scripts/pre-release-full.mjs:116-120` 的 L4 只调用较弱的 `check-package-contents.js`，而该脚本 `:156-159` 只检查 Chromium 目录存在，不检查平台可执行文件，也不检查 bundled Node。严格检查只在 `desktop/scripts/build-win-full.js:101-113` 的完整构建链中执行；本次 `15/15` 总门禁没有覆盖它。因此“15/15 全通过，可以发版”被实际包内容推翻。

发布前必须在 Windows 真机正确构建，或在明确准备好 win-x64 Node、Chromium、Prisma/原生依赖后交叉构建；随后重新执行严格资产检查、解包检查和 Windows 启动 smoke，不能继续使用当前远端包。

## 3. P1/P2 代码与云端问题

### 3.1 自愈逻辑

- ✅ `desktop/main.js:1596-1624` 的单链路为 `close(code!=0) -> report -> heal -> scheduleRestart`，未发现主动 restart 与 close restart 双启动。
- ⚠️ **P1：首次自愈失败后永久禁用重试。** `:1649-1653` 在确认数据库路径存在、rename 成功前就设置 `backendDbHealUsed=true`；锁占用、路径异常或临时 I/O 失败会消耗唯一机会。
- ⚠️ **P1：sidecar rename 失败被静默吞掉。** `:1658-1663` 对 `-wal/-shm` 的 rename 使用空 `catch`；旧 WAL 残留可能再次污染重建库，且日志没有明确记录。
- ⚠️ **P1：损坏特征正则偏宽且可能误伤。** `:1645-1647` 只要 stderr 出现 `malformed`、`unable to open database` 等词就执行破坏性换库；建议增加错误上下文/重试门槛，并在 UI 明确告知“旧库已备份并重建”。
- ⚠️ **P2：特征匹配只看最后 4096 字节。** `:1588-1593` 的 `stderrTail` 可能把早期 Prisma 损坏信息挤出窗口；虽然 `:1678-1695` 有文件尾读取函数，但自愈判断没有使用更大的 `backend-stderr.log` 尾部。
- ✅ `MAX_RESTARTS=3` 和单次会话一次自愈的计数关系已核对，当前实现不会产生第二条 restart 链，但自愈成功会消耗一次重启额度。

### 3.2 兜底上报

- ✅ `desktop/main.js:1606-1613,1697-1726` 覆盖崩溃和就绪超时，具有每会话 3 次、指纹去重和字段截断。
- ✅ 生产 `POST https://kaypal.cn/cloud-api/api/v1/client-error` 返回 `204`，随后 OSS `error-reports/2026-08-30/` 出现对象；现存真实 `backend-startup-crash` 报告包含 `P2010/database disk image is malformed`、退出码 1 和 AutoHeal 时间线，证明云端落 OSS 链路有效。
- ⚠️ **P2：204 空响应被客户端判为失败。** `desktop/cloud-api.js:80-104` 对所有 2xx 无条件 `JSON.parse(data)`；`reportClientError()` `:251-265` 因生产端 204 空 body 返回 `false` 并打印“上报失败”，但 OSS 实际已写入。应把空 2xx 视为成功。
- ⚠️ **P1：IP 限流信任客户端 X-Forwarded-For。** `cloud-api-server/server.js:131-137` 直接取请求头第一个地址；当前 Nginx `$proxy_add_x_forwarded_for` 会保留攻击者自带的首地址，攻击者可轮换 XFF 绕过 20/min/IP 限流。
- ⚠️ **P2：匿名端点仍可被分布式刷写 OSS。** `:28` 使用 Express 默认约 100KB body 上限，业务字段虽截断到 8KB，仍没有全局鉴权或分布式限流；建议可信代理覆盖真实客户端 IP，并增加网关级限速/配额。
- ✅ `grep` 检查 `cloud-api-server/server.js` 未发现硬编码 OSS AccessKey/Secret，代码从环境变量读取 `:154-160`。

### 3.3 CloudAPI/auth 合同

- ✅ `desktop/cloud-api.js:25-39` 的 endpoint 路径拼接修复实测得到 `https://kaypal.cn/cloud-api/api/v1/client-error`，未发现旧调用方依赖错误路径。
- ⚠️ **P2：CloudAPI auth 方法是死/过期合同。** `CloudAPI.login()` 等方法调用 `/api/v1/auth/*`，但 `cloud-api-server/server.js` 只有 health、generate-reply、check-content、check-dedup、mark-sent 和 client-error；实际桌面登录页走本地 `frontend/src/lib/api/auth.ts:48-53` 的 `/api/auth/login`，所以当前登录未被该缺口影响。后续启用 CloudAPI auth 前需补服务端路由或删除过期接口。
- ⚠️ **P1：生产鉴权业务接口不可用。** 远端 `/cloud-api/api/v1/generate-reply` 返回 `503 服务端未配置 API_KEY，拒绝访问`；对应代码在 `cloud-api-server/server.js:45-52`。远程容器环境只有 OSS/HOST 相关变量，没有 `API_KEY`。
- ⚠️ **P1 安全项：远程 compose 文件含硬编码形式的 `AI_API_KEY`。** 已确认存在于 `/opt/ai-content-cloud-api/docker-compose.yml`，未在本报告复制密钥值。应立即轮换该密钥，改用受保护的 `.env`/secret 注入，并从 compose 历史中清理。

## 4. 版本、门禁和发布通道

- ✅ `node desktop/scripts/check-version-sync.js`：`Version sync passed: v1.1.101; updater notes=release-notes.md`。
- ✅ `package.json`、`package-lock.json`、`packager.json`、`DESKTOP_APP_VERSION`、release notes 页面均为 `1.1.101`。
- ✅ `node scripts/pre-release-full.mjs` 实际 `15/15` 通过（tsc、Jest、Vitest、构建、回归、路由扫描、L4 目录检查）。路由扫描证据为 149/203，排除 54，失败 0，console errors 0，request failures 0。
- ✅ 生产 `latest.yml`、`latest-mac.yml` 均为 `1.1.101`；本地 Windows exe（530,738,644 bytes）和 Mac zip（654,517,550 bytes）的 SHA512 与 feed 一致；两者 Range GET 均为 206。
- ❌ **P1：两个远程 blockmap 均 HTTP 404。** `desktop/scripts/verify-oss-release.js:400-403` 明确要求 blockmap；remote 验证因此失败。`--local-only` 另因本地缺少 `wechat-db-helper` 资源失败。当前自动更新缺少差分元数据，完整更新链路未闭环。应上传 Win/Mac blockmap 后重新验证。
- ⚠️ **P2：版本同步脚本覆盖不足。** `desktop/scripts/check-version-sync.js:20-75` 检查版本常量和 release notes 非空，但不检查 release notes 是否包含当前版本，也不检查 feed/blockmap/远端 artifact 元数据。
- ⚠️ **P2：autoUpdater 没有单元/E2E 测试。** `desktop/auto-updater.js:42-78` 已设置 `autoDownload=true`、`autoInstallOnAppQuit=true` 并在 update-available 触发下载，但仓库未找到 updater 专项测试；在 blockmap 404 前提下不能宣称更新可靠。
- ⚠️ 验证脚本之间存在合同冲突：`verify-oss-release.js` 将 `wechat-db-helper` 设计为 OSS 按需资源，而 `windows-commercial-release-gate.js` 又把未打包该目录列为 blocker。需要统一一个发布 SSOT，避免一套门禁否定另一套门禁的设计决策。

## 5. 云端部署与 E2E

- ✅ SSH 复核 `root@118.178.108.44`：cloud-api 容器运行中，compose 设置 `HOST=0.0.0.0`，`.env` 权限为 `600`，Nginx `/cloud-api/` 反代存在且有变更前备份。
- ⚠️ `api.kaypal.cn` DNS 仍指向另一台测试 ECS（101.37.128.23），该域名 `/health` 返回 404；本次实际链路使用 `kaypal.cn/cloud-api`，不影响已验证链路，但属于域名配置遗留。
- ✅ OSS `updates-test/diag/accept-0828-e2eB.txt` 证明在已有 v1.1.99 安装上替换 app.asar、人工损坏 SQLite 后，出现 P2010 malformed，随后 AutoHeal 备份/重建，重启后 `PORT3011=True PORT3010=True`，时间线在 150 秒内。
- ⚠️ 该 E2E **不是**真实 `1.1.99 -> 1.1.101` 安装包升级：它是现有安装上的 app.asar 替换；探测的 `/api/v1/system/health` 返回 404，端口恢复为真但健康端点 200 未在云电脑上验证。原始用户 15827 的根因也只能说与同一错误特征一致，不能声称已由原机证据完全证明。
- ⚠️ 清单中的 ECD `jz-win11-accept-0828` 当前状态未通过 ECD 专用 API 重新确认，也未执行 SYSTEM 会话 `/S` 安装挂死复测；`ecctl ecs instance list` 只能确认主机 `118.178.108.44` 对应 ECS 为 `Running`。

## 6. 清单 §8 逐项结论

| # | 清单遗留 | 复核结论 |
|---|---|---|
| 1 | SYSTEM 会话 `/S` 挂死 | ⚠️ 未复测；oneClick/per-user 配置存在，但无人值守安装仍无真实证据 |
| 2 | 真实旧版升级 E2E | ❌ 未完成；现有证据是 app.asar 替换，不是安装器升级 |
| 3 | `/api/ai` 改 `/api/v1` | ⚠️ 代码/测试口径一致，但生产网关未做带鉴权 live contract 验证 |
| 4 | 门禁脚本排除路由 | ✅ 8/24 路由变更与 `/case-admin` 403 依据可解释；仍应合并成单一 SSOT |
| 5 | `autoDownload=true` | ⚠️ 代码已生效；blockmap 404、无 updater E2E，体验/可靠性风险未闭环 |
| 6 | 自愈误伤面 | ❌ 存在宽正则、一次失败永久禁用、sidecar 错误吞掉三项风险 |
| 7 | 生产 `API_KEY` 未配置 | ❌ 已现场确认，鉴权业务接口返回 503 |
| 8 | 云电脑保持 Running | ⚠️ ECD 状态未确认；主 ECS 仍 Running，应按资源管理流程释放/确认 |

## 7. 发布判定与整改顺序

当前判定：**需修复，不可发布 Windows v1.1.101。**

整改顺序：

1. 重建正确的 win-x64 安装包，确保包内 `node.exe`、`chrome.exe`、Prisma 原生引擎与其他 Windows 依赖匹配；将严格 `check-full-installer-assets --phase=post` 纳入总门禁。
2. 上传并验证 Win/Mac blockmap，重新跑 `verify-oss-release.js`，再做真实 Windows 安装、启动和更新 E2E（含 SYSTEM `/S` 与旧版升级）。
3. 配置生产 `API_KEY`，轮换并移除 compose 中硬编码 `AI_API_KEY`。
4. 修正 204 响应解析、真实客户端 IP 获取、分布式限流；收紧自愈触发条件，记录 sidecar 处理失败，允许失败后的可控重试。
5. 补齐云端鉴权接口合同验证和 updater 测试，并统一各发布门禁的资源/依赖政策。
