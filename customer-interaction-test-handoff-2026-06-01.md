# 客户互动真实商用测试交接

## 当前目标

只做客户互动四个前台页面的真实商用测试：从 `http://localhost:3010` 系统页面操作，验证系统自己能否读取真实客户内容、AI 相关回复、自动发送、平台回读成功。

## 测试范围

- `/workbench/douyin-comments/`：抖音评论回复
- `/workbench/douyin-messages/`：抖音私信回复
- `/workbench/channel-comments/`：视频号评论回复
- `/workbench/channel-messages/`：视频号私信回复

## 成功标准

- 必须从系统前台点按钮，不绕过系统直接去平台手工发。
- 必须读到真实客户评论/私信，不是系统通知、按钮文案或假数据。
- AI 回复必须和客户内容相关，不是固定模板硬套。
- 自动发送模式下必须直接发送，不进确认队列。
- 成功只认平台真实发出，并且系统回读确认页面已出现回复。
- 只写任务、只写日志、只显示“已处理”都不算成功。

## 当前服务状态

- 前端：`http://localhost:3010`
- 后端：`http://localhost:3011`
- auto-upload：`http://127.0.0.1:5409`
- 抖音 CDP 持久浏览器：`127.0.0.1:9290`
- 视频号 CDP 持久浏览器：`127.0.0.1:9253`

## 已知状态

- 抖音、视频号账号曾校验为 cookie 有效。
- 之前测到视频号评论能真实发送并回读，但回复内容偏模板。
- 之前测到抖音评论能读到评论，但曾卡在找不到回复框。
- 之前测到视频号私信能扫描候选，但未形成可回复对象。
- 最近一次页面观察出现过异常：`/workbench/douyin-comments/` 地址栏下渲染成“视频号评论回复”；测试前必须确认页面标题和 URL 匹配。

## 下一步

1. 打开当前 in-app browser 的 `http://localhost:3010/workbench/channel-comments/` 或四个页面之一。
2. 确认页面标题和 URL 匹配；不匹配先记录“前台入口错位”，不要当业务测试结果。
3. 点击页面里的“开始回评论”或“开始清私信”。
4. 等系统执行完成，记录：客户原文、AI 回复、发送状态、回读状态、失败原因。
5. 四个页面按同一标准给出“能商用/不能商用”的结论。

## 不能做

- 不写代码。
- 不改配置。
- 不改账号。
- 不做文档扩展。
- 不解释浏览器控制层。
- 不把工具卡顿当业务失败。
- 不用系统以外的能力手工去平台回复。
