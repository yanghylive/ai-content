# AI Content Backend

This directory contains the NestJS backend for the full-featured JIUZHANG AI platform.

Use the root guide for setup:

- `../README.md`

Common commands:

```bash
npm install
npm run db:init
npm run db:bootstrap-admin
npm run start:dev
```

Swagger will be available at `http://localhost:3001/api/docs`.
