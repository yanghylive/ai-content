# dependencies
**/node_modules/
.pnp
.pnp.js
.yarn/install-state.gz

# testing
**/coverage/

# next.js (frontend)
frontend/.next/
frontend/out/
frontend/out-mobile/
frontend/out-desktop/

# production
**/build/
**/dist/

# misc
.DS_Store
*.pem

# debug
*.log*
.pnpm-debug.log*

# local env files
.env*.local
backend/.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
Thumbs.db
Desktop.ini

# Persistence
backend/data/*.db
backend/data/*.db-journal

# tsc stale build artifacts (regenerated on build)
backend/test_*.js
backend/test_*.js.map
backend/test_*.d.ts
backend/scripts/*.js
backend/scripts/*.js.map

# electron-updater dev mode
desktop/dev-app-update.yml

# Local runtime state
frontend/.local-geo-bridge-tasks.json

# Python wheelhouse (build artifact, regenerated by prepare-wheelhouse.js)
desktop/installer/wheelhouse/*/
!desktop/installer/wheelhouse/.gitkeep

# Local runtime logs (not source)
.local-logs/

# Backend dist-bundle (build output, regenerated by nest build:bundle)
backend/dist-bundle/
backend/dist-bundle-sqlite/
backend/dev.db
backend/ai-content-dev.db

# Desktop packaged runtime/output snapshots
desktop/dist.bak-*/
desktop/runtime/
# Un-ignore tracked source files under desktop/runtime/
!desktop/runtime/wechat-db-helper/
!desktop/runtime/wechat-db-helper/**
!desktop/runtime/wechat-native-runtime/
!desktop/runtime/wechat-native-runtime/**
!desktop/runtime/remote-assets/
!desktop/runtime/remote-assets/**

# Python cache artifacts
__pycache__/
*.py[cod]

# Local backup files
*.bak
.local-backups/

# Local desktop/browser runtime state
data/
.kaypal-runcheck-*.probe
backend/.facefusion-venv/
backend/*.sqlite
backend/prisma/*.sqlite
backend/prisma/*.db
backend/prisma/*.db-journal
skillhub-skills/
*.sqlite
*.sqlite-journal

# tsc stale build artifacts (regenerated on build)
**/dist.stale-*/
**/dist-bundle-sqlite.bak-*/
frontend/.tmp-*.png
frontend/.tmp-*.jpg

# Windows packaging output (regenerated by electron-builder)
desktop/dist-windows-*/

# Cloud-api server (separate subproject, not in scope of this repo)
cloud-api-server/node_modules/
cloud-api-server/cloud-api.db
cloud-api-server/cloud-api.db-shm
cloud-api-server/cloud-api.db-wal
cloud-api-server/.env
cloud-api-server/.env.local
cloud-api-server/data/

# Env files (anywhere, but not .env.example which is template)
.env
.env.local
frontend/.env
frontend/.env.local
cloud-api-server/.env.example
!cloud-api-server/.env.example
!backend/.env.example
!frontend/.env.example

# 桌面端真实环境（含第三方密钥，不入库；模板见 desktop/backend.env.example）
desktop/backend.env
!desktop/backend.env.example

# AI top-level file (tooling artifact)
AI

# Desktop installer downloaded deps (large binaries, fetched at install time)
desktop/installer/deps/

# Desktop installer-helper binaries
desktop/installer-helper/node_modules/
desktop/installer-helper/dist/
desktop/installer-helper/resources/*.exe
desktop/installer-helper/resources/*.dmg
desktop/installer-helper/resources/*.zip
# playwright-mcp 临时文件
backend/.playwright-mcp/
# local-engine profile 临时数据
backend/data/
backend/index.js

# 数据库备份归档（不提交）
backups/

# 本地运行时/工具产物（不入库）
*.pid
.hallmark/
.workbuddy-commercial-backup/

# 验收证据里的安装包解压产物（Electron 安装包 178MB+ / chromium 269MB+，超 GitHub 100MB 上限）
docs/acceptance-evidence-*/**/windows-installer-extract/

# 交叉构建产物与缓存（413MB+ chromium，不入库）
desktop/pw-win/
pw-win/
desktop/backend-bundle/
desktop/dist/win-unpacked/
desktop/dist/*.blockmap

# 前端构建缓存备份（4G+，不入库）
frontend/.next.bak-*/
frontend/out.bak-*/

# 临时目录
tmp/

# 根目录误放的旧 package.json（与 desktop/package.json 重复，1.1.30 过时）
/package.json
desktop/.env

# ===== 凭据/登录态显式隔离（2026-08-18 事故审查加固）=====
# 第三方平台登录态与凭据。父目录规则（data/、*.sqlite）已覆盖，
# 此处显式声明防止未来规则调整后敏感文件裸奔入库。
**/.login-cookies.json
**/storageState*.json
**/cookiesFile/
**/local-mcp-auth.json
**/boss-storage-states/
# Kaypal OAuth token 运行库（SQLite，含明文 refresh_token）
backend/prisma/data/
**/kaypal-ai.sqlite*
# launchd 运行副本（含密钥 env 的 plist 派生文件，不入库）
.ai-content-frontend/
.ai-content-backend/

# Python venv（sidecar 执行器）
desktop/sidecars/agent-s-executor/venv-s3/

# 构建临时残留（next/out 改名产物）
frontend/.next-stale-*/
frontend/.next-t*-*/
frontend/out-stale-*/
frontend/out-stale*/
