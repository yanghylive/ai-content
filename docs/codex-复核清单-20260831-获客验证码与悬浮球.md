# Codex 复核清单 — 获客验证码误判修复 + 悬浮球存量清理

> 复核时间：2026-08-31 深夜
> 待复核 commit：**`e4a35d61`**（backend+frontend）、**`df4f71ed`**（desktop/main.js）
> 基线：origin/main `6d6e19bf`
> 复核方式：按本清单逐项核实 **file:line + 可验证命令 + 预期结果**，允许并鼓励跑负向用例；按 P0/P1/P2 打回，不许只给"看起来没问题"。

---

## 〇、背景（30 秒版）

1. **获客任务误判**：2026-08-31 20:05:21 线上 run（本机库 `growth_acquisition_runs`，id `run-1788177921480-7a5e88`）失败，failure_reason=`unknown`、消息"页面未出现搜索结果信号"。现场截图实为**抖音滑块验证码**（文案「请完成下列验证后继续」），旧正则 `验证码|安全验证|滑块|行为验证|验证通过` 一个都不匹配 → 落入 `platform_changed` → `mapReasonCode` 映射为 `unknown`。历史同款误报 16 次（8/20 起），全 50 次 run 仅 2 次 success。
2. **悬浮球残留**：大王报"悬浮球不是不要了吗"。实锤本机 Mac 装的是 **1.1.104 旧包**（Info.plist），旧逻辑 `!== false` 默认开启；且 ≤1.1.105 时代 config 持久化过 `hoverBallEnabled=true` 的机器升级后仍出球（7d251a64 的默认关只管"无键"用户）。

---

## 一、复核项 A：验证码正则补变体（commit e4a35d61 · backend）

**文件**：`backend/src/modules/runtime/platforms/douyin/exposure-collector.service.ts`

| # | 位置 | 改动 | 预期 |
|---|---|---|---|
| A1 | :1219（`diagnose`，:1194 起，评论区路径） | 验证码正则追加 `请完成.{0,8}验证\|验证后继续\|完成拼图` | 命中返回 `captcha_required` + 可行动文案 |
| A2 | :1262（`diagnoseSearch`，:1244 起，搜索页路径） | 同上 | 同上 |
| A3 | :1305（`diagnoseIdentityGate`，:1287 起，目标主页路径） | 同上（文案无"拉大间隔"句） | 同上 |

**验证命令**：

```bash
cd backend && npx jest src/modules/runtime/platforms/douyin/exposure-collector.service.spec.ts
# 预期：32 passed（含新增 2 例）

cd backend && npx jest src/modules/runtime/platforms/douyin
# 预期：3 suites / 67 passed

cd backend && npx tsc --noEmit
# 预期：exit 0
```

**复核要点 / 负向用例（Codex 主动攻击）**：

- [ ] **A-N1 正则误伤面**：这组判定只接收页面纯文本，命中任一验证码短语就会返回 `captcha_required`，无法从文本本身区分“平台风控文案”和用户评论里恰好出现的同词。因此负向用例应使用**不含这些短语**的正常评论/搜索页文本，确认仍按正常信号返回；不要声称“含验证码短语的正常页不触发”，那与实现矛盾。已知设计取舍：`.{0,8}` 不跨行（无 `s` 标志），`请完成下列验证后继续` 能命中；「请完成下面三步验证」这类空格/标点变体在 8 字内也命中。
- [ ] **A-N2 判定顺序**：`diagnoseSearch` 登录判定（:1251-1254）在验证码判定**之前**，且登录正则含裸 `登录|扫码`。验证码页若同时含"登录"字样会被判 `account_not_logged_in` 而非 `captcha_required`。**这是已知行为（交底），不算本轮回归**；若你认为该提级修复，按 P2 提改进项，不要改本轮语义。
- [ ] **A-N3 三处一致性**：A1/A2/A3 三条正则必须逐字一致（本次事故根因就是"同款正则改了一处漏两处"的可能性），grep 确认无第 4 处漏网：
  ```bash
  grep -rn "请完成.{0,8}验证" backend/src --include="*.ts" | grep -v spec
  # 预期：恰好 3 行 = exposure-collector.service.ts 的 1219/1262/1305
  grep -rn "验证码|安全验证|滑块" backend/src --include="*.ts" | grep -v spec
  # 预期：4 行 = 1216(注释引用旧正则) + 1219/1262/1305(三条新正则)；出现其他 .ts 文件即为漏网
  ```
- [ ] **A-N4 下游映射**：`captcha_required` 状态经 `growth.service.ts` `mapReasonCode`（:6127-6140）`/captcha/` 分支 → failure_reason=`captcha_required`，不再落 `unknown`。验证：`grep -n "captcha|verification" backend/src/modules/growth/growth.service.ts`。
- [ ] **A-N5 视觉兜底不越权**：`visionFallbackSearchCandidates`（注释 :2636，函数 :2642 起）不执行验证码求解；prompt 要求验证码、登录页或无内容时输出空数组，但在验证码/反爬遮罩之外仍可见候选时允许视觉模型提取这些候选。确认 `aiClient` 为空（:2667-2669）或截图缺失（:2671-2673）时返回 null、不抛异常（spec 无 aiClient 注入即覆盖此路径）。

**新增 spec 用例**（`exposure-collector.service.spec.ts`）：

| 位置 | 用例 | 预期 |
|---|---|---|
| :377 | hot-video 搜索页命中现场文案「请完成下列验证后继续\n按住左边按钮拖动完成上方拼图\n刷新\n反馈」 | `ok=false`、`status='captcha_required'`、message 含「手动完成验证」 |
| :399 | 评论页同款文案（`collectFromLinks`） | `status='captcha_required'` |

---

## 二、复核项 B：执行记录露出失败原因（commit e4a35d61 · frontend）

**文件**：`frontend/src/app/(dashboard)/growth/growth-acquisition-tasks.tsx:446-452`

改动：执行记录行（原 :407-447）在 `run.status !== "success"` 时渲染 `run.message`。此前页面只显示"失败"徽章，后端 message（含风控指引）完全不露出，用户无从对症处理。

**验证命令**：

```bash
cd frontend && npx tsc --noEmit
# 预期：exit 0
```

**复核要点 / 负向用例**：

- [ ] **B-N1 空串不渲染**：`run.message` 为空字符串/undefined 时不产生空 `<p>`（条件 `run.message &&` 已挡，确认无 falsy 漏网）。
- [ ] **B-N2 非失败态文案可读性**：`partial`/`skipped` 的 message 也会露出——**设计如此**（部分完成也要让人知道为什么），确认文案无敏感信息（token/密钥/内部路径）。
- [ ] **B-N3 XSS 面**：message 为后端纯文本、React 文本节点渲染，无 `dangerouslySetInnerHTML`；确认该文件无其他注入点。
- [ ] **B-N4 与 fallback 提示共存**：:438-445 的 RPA 回退提示与 message 行同时存在时排版不叠（两个独立 `<p>`）。

---

## 三、复核项 C：悬浮球存量配置启动清理（commit df4f71ed · desktop）

**文件**：`desktop/main.js:2463-2476`

改动：app ready 分支里，在悬浮球创建判断（:2475 `=== true`）**之前**，`store.has('hoverBallEnabled')` → `store.delete('hoverBallEnabled')`。目的：≤1.1.105 时代持久化的存量 `true` 键在升级后必然被清除，悬浮球在任何机器不再出现。

**验证命令**：

```bash
cd desktop && node -e "new Function(require('fs').readFileSync('main.js','utf8'))"
# 预期：无语法错误

cd desktop && node --test scripts/release-guards.test.js
# 预期：# pass 25 / # fail 0
```

**复核要点 / 负向用例**：

- [ ] **C-N1 删除先于创建**：`store.delete` 必须在 ready 分支的 `createHoverBall()` 判断（:2474）之前执行（当前顺序正确）。`createHoverBall` 调用点共 2 处：
  ```bash
  grep -n "createHoverBall" desktop/main.js
  # 预期：:1948(定义) + :1987(toggleHoverBall 内,见下) + :2474(ready 分支)
  grep -n "toggleHoverBall" desktop/main.js
  # 预期：仅 :1982 定义、无任何调用方 = 死代码（托盘开关已移除的历史残留）
  ```
  活路径仅 :2474 一处；`toggleHoverBall` 为死代码（P2 可提清理项，不阻塞）。
- [ ] **C-N2 渲染进程再写入口**：`config:set` 白名单（:2286-2291）仍含 `hoverBallEnabled`。渲染进程可在运行期写 true，但创建时机只在 app ready 且下一轮启动会再删 → **球永不出现**。复核：确认除 ready 分支外无第二个活创建路径（toggleHoverBall 死代码除外）。
- [ ] **C-N3 electron-store API**：确认所用 electron-store 版本支持 `.has()`/`.delete()`。已实测：`desktop/package.json` 为 `^8.2.0`、`node_modules/electron-store/package.json` = **8.2.0**（`.has()`/`.delete()` 均支持）；若复核时版本变化需重验。
- [ ] **C-N4 托盘无开关入口**：确认托盘菜单无悬浮球开关（`grep -n "hoverBall" desktop/main.js | grep -i tray` 应为空），避免"删了又被托盘写回"。
- [ ] **C-N5 包检查兼容**：悬浮球文件仍在包里（`hover-ball.html`/`preload-hoverball.js`），`check-full-installer-assets.js:732-733` 的 hover ball 检查不应红。确认本轮未动打包配置。

---

## 四、门禁汇总（复核完必跑）

```bash
# 后端
cd backend && npx jest src/modules/runtime/platforms/douyin && npx tsc --noEmit
# 前端
cd frontend && npx tsc --noEmit
# 桌面
cd desktop && node --test scripts/release-guards.test.js
# 提交核对
git show --stat e4a35d61   # 3 files changed, 74 insertions(+), 6 deletions(-)
git show --stat df4f71ed   # 1 file changed, 6 insertions(+)
```

全部预期：exit 0 / 全绿。任一不符 → 按下表定级打回。

## 五、P0/P1/P2 判定标准

| 级 | 定义 | 本轮示例（ hypothetical） |
|---|---|---|
| P0 | 功能错误 / 回归：正则三处不一致、删除顺序颠倒导致球复现、测试红 | — |
| P1 | 边界缺陷：有证据证明明确非风控页面被误判 `captcha_required`、空 message 渲染空节点 | — |
| P2 | 改进项：登录判定先于验证码判定的顺序问题、A-N4 映射硬编码正则的可维护性 | — |

## 六、已知局限交底（不算本轮 bug，防误打回）

1. **验证码不可自动绕过**：本轮修的是"识别准 + 提示清"，验证码仍需用户在内置浏览器人工完成；视觉兜底不会执行验证码求解，若遮罩外仍有可见候选，模型可能只提取这些候选，否则按 prompt 输出空数组并由解析结果返回 null。
2. **修复生效范围**：源码已合入 main，随 **1.1.111**（CI 全自动发版，run 33400106127 链路已验证全绿）下发；`df4f71ed` 位于 1.1.110 发布之后，本机 Mac 装的 1.1.104 旧包必须升级到包含该 commit 的 1.1.111 才能清掉存量 `hoverBallEnabled`。
3. **移动端 AI 助手悬浮球**（`app-shell.tsx:367` 挂 `AiAssistant` 非内嵌，右下角紫钮）是另一个组件，Q3 决策"保留"，**不在本轮范围**；若大王决策一并去掉，另开任务。
4. 20:05:21 run 的 `started_at == ended_at`（同毫秒）是记录写入时序问题（终态一次性落库），不影响数据正确性，未在本轮修。
