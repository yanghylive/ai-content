# Codex 复核清单——今日增长首页精修与双首页合并

> 生成：2026-09-03 17:05 · 复核范围：`/today` 首页精修、`/growth` 控制台退役并入、设计 token 补齐（未提交，工作区改动）
> 背景：双首页（`/today` 今日增长 + `/growth` 获客中心）同源数据两套呈现，本轮按大王决策「今日增长接管」合并；同时完成 /today 视觉体检（宽度/间距/字号）、/growth 评审批次 ①-④、全局 token 缺失补齐。清单供逐项复核闭环。
> 自包含要求：每项含 文件:行号 + 可验证命令 + 预期结果；行号为当前工作区实测（非推算）。

---

## 一、复核范围与基准

| 项 | 内容 |
|---|---|
| 主改动区 | `frontend/src/app/(dashboard)/today/today-center.tsx`（新增 AI 简报 / 价值账单 / 增长功能矩阵，overview 并入） |
| 路由改造 | `frontend/src/app/(dashboard)/growth/page.tsx`（整文件改服务端 redirect） |
| 导航收口 | `app-shell.tsx` / `command-palette.tsx` / `route-config.ts` / `home-center.tsx` / 情报 2 处 / 6 个子页返回链 |
| 退役删除 | `growth-center.tsx`、`growth-page-shell.tsx`、`growth-console-entry.tsx`、`redirect-growth-view.ts`、`components/growth/growth-console.tsx`（9446 行旧控制台） |
| Token 补齐 | `frontend/src/app/globals.css`（:root 明色 + .dark 暗色双套 17 token） |
| 已跑门禁 | frontend `tsc --noEmit` 0；`next build` 通过并同步 `~/.workbuddy/ai-content-frontend/out` |

**核心审查命题**：
1. `/growth` 主链（含 `?view=` 旧子页体系）彻底退役后无残留引用/死链，所有返回入口指向 `/today`；
2. `/today` 数据加载 = `home + overview` 并行、任一失败都有兜底与报错上屏；
3. 全站 `--kaypal-v3-*` 不存在「被使用但未定义」；
4. 首页间距/字号遵循文件头版式规范注释（today-center.tsx 顶部）。

---

## 二、门禁命令（先跑，预期全绿）

```bash
cd frontend
./node_modules/.bin/tsc --noEmit            # 预期：无输出，exit 0
npm run build 2>&1 | tail -3                 # 预期：postbuild-remove-demo + sync-runtime-out 两行成功
```

删除残留扫描（合并是否清干净）：

```bash
# 1) 不应再有对已删文件的 import（预期无输出）
grep -rn "growth-console-entry\|growth-page-shell\|redirect-growth-view\|components/growth/growth-console\"" \
  frontend/src --include="*.tsx" --include="*.ts" || true

# 2) 不应再有指向 /growth 主页（不含子路由）的整页入口（预期仅剩重定向本身，或注释说明）
grep -rn 'href="/growth"\|push("/growth")\|href="/growth?"' frontend/src --include="*.tsx" --include="*.ts" || true

# 3) token 定义完备性（产物级，预期 MISSING=0）
cd ~/.workbuddy/ai-content-frontend/out/_next/static/chunks && node -e '
const fs=require("fs");const files=fs.readdirSync(".").filter(f=>f.endsWith(".css"));
const defined=new Set();for(const f of files){const s=fs.readFileSync(f,"utf8");for(const m of s.matchAll(/--(kaypal-v3-[\w-]+)\s*:/g))defined.add(m[1]);}
const used=new Set();for(const f of files){const s=fs.readFileSync(f,"utf8");for(const m of s.matchAll(/var\(--(kaypal-v3-[\w-]+)/g))used.add(m[1]);}
const miss=[...used].filter(u=>!defined.has(u));console.log("MISSING("+miss.length+"):"+miss.join(","));'
# 预期：MISSING(0):
```

---

## 三、逐项复核（A-E）

### A. [P0] 双首页合并后的路由闭环

| 检查点 | 位置 | 预期 |
|---|---|---|
| A1 `/growth` 主链重定向 | `growth/page.tsx`（整文件） | 服务端 `redirect("/today")`；浏览器访问 `/growth` 终态 URL = `/today` |
| A2 `?view=` 旧体系收口 | 同上 + 已删 `redirect-growth-view.ts` | 无任何代码再跳 `/growth?view=*`（grep 二.2） |
| A3 桌面侧栏唯一化 | `components/shell/app-shell.tsx:34-35` | SCENES 仅剩 `growth-home`(今日增长) 一项 growth 项；`sceneOfPath` 中 `/growth*`、`/intelligence*`、`/effects*` → `growth-home`（不再 return "growth"） |
| A4 命令面板去重 | `command-palette.tsx` | 已删 `/growth` 主入口条目；`SCENE_NAME["/growth"]` 与 growth 前缀归类均标「今日增长」 |
| A5 子功能返回链 | growth 下 6 子页（acquisition-tasks/leads-pool/strategies/reports/account-health/workflows） | 「返回增长控制台」文案 →「返回今日增长」且 `push("/today")` |
| A6 其它落点 | `route-config.ts:42`、`home-center.tsx:101`、`intelligence/data.ts:807`、`redfox-skills-client.tsx:128` | 全部指向 `/today` |

### B. [P0] 合并后 `/today` 数据与能力完整性

| 检查点 | 位置 | 预期 |
|---|---|---|
| B1 双接口并行 | `today-center.tsx` `loadHome`（约 :1660 前） | `Promise.all([getGrowthHome, overview])`；任一 rejected → `toPublicError` 上屏，另一接口数据仍渲染 |
| B2 渲染区块 | `TodayCenter` return（约 :1670） | 顺序：页头(统计+趋势) → 需要关注 → AI 简报 → 漏斗 → 最近运行 → AI 价值账单 → 增长功能 |
| B3 新失败举手迁移 | `announceNewFailedRuns`（约 :470 前 helper） | 首批失败静默入 `sessionStorage` 去重集合（`growth-failure-toast-announced`），仅后续轮询弹**新增**失败；30s 节拍（原 /growth 15s 轮询已删，无独立轮询残留） |
| B4 旧区块清理 | 全文件 | 无「下一步建议」/ `GrowthHomeNextAction` 引用（grep 应 0） |

### C. [P1] 首页版式规范自洽（对照 today-center.tsx 头部注释）

| 检查点 | 预期值 |
|---|---|
| C1 区块间上下间距 | `.kx-view` gap-6 → 24 |
| C2 面板与横幅内边距 | 趋势/漏斗/最近运行/增长功能/AI 简报/AI 价值账单 = `p-6` 24 |
| C3 head→内容首间距 | 各区块统一 16（`mt-4`/`mb-4`，无 `mt-3` 遗漏） |
| C4 横向大网格 | 统计卡/价值账单格/增长功能入口 = `gap-4` 16 |
| C5 统计卡主数字 | 内联 `fontSize: var(--kaypal-v3-font-display, 32px)`；**不得**回退 `text-[var(...32px)]` 类写法（Tailwind3 arbitrary 带逗号不生成） |
| C6 区块标题 H2 | 代码 `text-base`（真实渲染 15px 由 `.kx-view h2` 全局决定，勿叠 `text-sm/lg` 制造虚假声明） |

验证：浏览器 1440 视口打开 `/today`，逐卡 `getComputedStyle().paddingTop / gap` 对照上表。

### D. [P1] /growth 控制台视觉 Bug 修复是否留存

| 检查点 | 位置 | 预期 |
|---|---|---|
| D1 状态徽章 | 原 growth-center 六态 → 已随文件删除 | 合并后首页/子页凡状态徽章均用 `kx-tag kx-t-*`（已定义类），无 `--kaypal-v3-*-ink` 直接引用残留 |
| D2 漏斗 0 值表现 | 首页漏斗（`FunnelSection`） | 0/null 段渲染灰槽（border + 纸底、灰字），不随 opacity 递减到不可见 |

### E. [P1] Token 补齐正确性（globals.css）

| 检查点 | 预期 |
|---|---|
| E1 明色 + 暗色双套 | 新增 17 token 均在 `:root` 与 `.dark` 各定义一次（共 34 处）；语义值随 `.dark` 切换（如 danger-ink 明深红 / 暗亮红） |
| E2 别名指向已定义主色 | `primary=accent`、`warning=amber`、`bg=canvas`、`panel/surface=paper`、`surface-2/surface-hover=paper-soft` |
| E3 无覆盖误伤 | 新增行不改变既有 token 值（补丁为纯新增行，锚点后插入） |

---

## 四、负向审查（读代码验证）

- N1：若 `overview` 拉取失败（`growthApi.overview()` rejected）→ AI 简报/价值账单/增长功能角标整组不渲染（`{overview ? ... : null}`），**home 主数据仍完整**——复核是否可接受「功能矩阵随 overview 一起消失」（设计取「无 overview 不显示依赖它角标的区块」，若 Codex 认为应降级为无角标渲染请列 P2 建议）。
- N2：`/growth` 页已重定向，但**静态产物**中 `/growth` HTML 仍在（Next 静态导出 + redirect 运行时跳转）——SEO/直达无害，直连 `/growth` 实测跳 `/today` 即可；未做 301 静态重写属可接受范围（如需请列 P2）。
- N3：首页 30s 自动刷新现在额外携带 `overview()` 请求，轮询开销翻倍；两接口同源可合并成一个聚合端点（P2 优化建议，不判 P0）。
- N4：`growth-mobile-console.tsx` 内联了 `GrowthView` 联合类型替代 `export type from "./growth-console"`，仅桌面端旧控制台依赖已删；移动端子页继续可用——实测 `/growth/leads` 移动视口应正常。

---

## 五、浏览器复核清单（建议逐条过）

1. `/growth` 直达 → 跳 `/today`（截图终态 URL）
2. `/today` 首屏含：统计卡×5、近 7 日趋势主图、AI 简报、漏斗、最近运行、AI 价值账单、增长功能×7
3. 侧栏无「获客中心」；`/growth/leads` 页侧栏高亮「今日增长」
4. 增长子页头部返回按钮文案「返回今日增长」且点击回 `/today`
5. 明/暗主题切换，AI 简报、徽章、阻断卡颜色正常、无控制台 `console.error`
6. 1440px 下逐卡间距对照 C 表；375px 移动视口无横向溢出（网格 1-2 列降级）
