# Codex 复核回应——C2 单词级隔离 / D4 统计写入修复（2026-09-03）

> 回应 2026-09-03 codex 复核结论（P1: C2 / D4；P2: A1/A3/B3）。已修复 + 已补测试 + 运行态已恢复并验证。行号为修复后工作区实测。

---

## 一、P1 修复与证据

### C2 [已修复] 多行业词轮询被单词异常中断

- 位置：`backend/src/modules/growth/growth.service.ts` tryFetchDouyinAccountJourney 第一段 for 循环（单词级 try/catch）
- 修复：单行业词 `driver.execute` 包 try/catch——抛错记录 `logger.warn` 后 `continue` 下一个词；`ConflictException`（账号忙）保持传播由调用方转 409
- 测试：`growth.service.spec.ts`「C2：第一个行业词搜索抛错 → 不中断，继续第二个词并成功」——driver mock 第一词 throw、第二词成功，断言 `discover-account-search` 调用 2 次 + `ok:true`
- 验证：修复前该测试会因单词异常中断而失败（execute 仅 1 次），修复后绿

### D4 [已修复] 反馈统计竞态 / 提前返回 / 未隔离

- 位置：`growth.service.ts` recordKeywordFeedback / keywordStats（整段重构）
- 修复三件事：
  1. **串行 + 原子写**：进程内写链 `keywordStatsWriteChain`（互斥防读改写互相覆盖），写临时文件 + `rename` 原子替换；首次写入自动 `mkdir`（全新数据目录不 ENOENT）
  2. **响应前等待落盘**：executeConfig 调用由 `void record…` 改为 `await`；写失败经链回传调用方并 `logger.warn` 留痕（不静默吞），不阻塞获客主流程
  3. **scope 隔离**：文件结构升 v2（`{version, updatedAt, scopes: { [tenantId||userId]: {updatedAt, industries} }}`）；controller `GET keyword-stats` 改为传当前登录用户（`this.getUserId(request)`），service 按 `resolveGrowthTenantId(userId) || userId` 返回对应段；旧全局平铺格式读入时自动归入 `scopes.legacy` 不丢数据
- 测试（4 条新增全绿）：等待落盘+readback 一致；并发两写累计不覆盖（=2）；user-2 读不到 user-1；C2 见上
- 运行态验证（服务已重启）：
  - `GET /api/agent-s/health` → 200
  - `GET /api/growth/keyword-stats`（登录态）→ 200 返回 `{updatedAt:null, industries:{}, message:'暂无命中统计…'}`（scope 空段）
  - 同端点无 cookie → 401（隔离生效）
  - bundle 标记命中：`keywordStatsWriteChain`×3、`跳过并继续下一词`×1、controller `keywordStats(this.getUserId`×1

---

## 二、P2 处理与说明

### B3 [已补齐] playbook keywords 类型缺字段

- 位置：`frontend/src/lib/api/growth.ts` listWorkflowPlaybooks keywords 类型
- 补齐：`demandSynonymGroups?: string[][] | null`、`isLocal?: boolean | null`（与后端 IndustryKeywordPreset 对齐）
- 验证：frontend tsc 0

### A3 [说明] 抖音评论无 /user/ 链接时按文本去重

- 属真实 DOM 行为（discovery-browser-runner.ts 提取层），负向覆盖需要真实页面（无作者主页链接的匿名评论同文本场景）；本机无真实登录态 DOM 可测
- 处理：保留为**人工真机验证项**，不在单测层伪造 DOM 结论；如后续有真机环境，建议用固定页面结构做两条「同名匿名评论不误合并」负向用例

### A1 [说明] 账号健康缓存键仅 userId、先于 scope 校验

- 影响面：成员权限撤销/租户切换后的缓存串扰（P2 边缘）。改动需动 listAccountHealth 缓存键与失效点，属独立小改
- 处理：暂列待办，未在本轮一并改动（避免扩大本轮面）；建议下一轮与缓存失效策略一起处理

---

## 三、门禁与运行态

| 项 | 结果 |
|---|---|
| backend `tsc --noEmit` | 0 |
| frontend `tsc --noEmit` | 0 |
| growth.service.spec | **100 passed**（+4 条 C2/D4） |
| backend sqlite bundle | 重建并同步 `~/.workbuddy/ai-content-backend` |
| frontend build | 通过并同步 `~/.workbuddy/ai-content-frontend/out` |
| 服务状态 | launchd 重启，3011 agent-s health 200；3010 登录页 200 |
| 工作区 | 修复均未提交（与 codex 结论一致，待复核闭环后统一提交） |
