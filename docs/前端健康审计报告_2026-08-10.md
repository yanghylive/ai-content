# 前端健康审计报告——功能接线与用户体验

> 2026-08-10 · 方法：代码级（后端 460+ 端点 vs 前端 lib/api 调用差集 + 占位/TODO 扫描）+ 运行级（模拟器 CDP 扫 6 关键页面 console 错误）

---

## 一、运行级检查（模拟器实测）✅

| 页面 | 结果 |
|---|---|
| /today /content /mine /savings /message /content/xiaohongshu | ✅ 全部无 console 错误、内容正常渲染 |

核心页面运行无异常；以下问题均为**代码级发现的"功能没接上/未完成"**。

---

## 二、后端有、前端没接的功能（未接线清单）

### 🔴 A1. 整模块未接（前端 lib/api 完全无调用封装，6 个）

| 模块 | 端点 | 影响 |
|---|---|---|
| **usage/token**（Token 用量追踪） | `GET /usage/token`、`pre-check`、`report` | 用量统计/预检能力前端完全没用 |
| **poi**（门店 POI 数据） | `/poi` CRUD + report（5 端点） | 门店数据层无前端页面 |
| **video**（AI 成片/商品视频） | `/video/product-copy`、`product-cut`、projects/pipelines | 商品视频剪辑（炼刀对标功能）无入口 |
| **video-generation** | 任务提交/查询 | 无前端 |
| **push-notifications**（Web 推送） | vapid-key、subscribe | 设置页只有本地开关，没真正订阅推送 |
| **client-config**（商业化配置） | `/commercial/client-config` | 配置下发无消费 |

### 🟡 A2. 已接模块缺关键端点（新功能模块）

| 模块 | 缺失端点 | 影响 |
|---|---|---|
| **local-engine** | `browser/ai-action`（AI 网页代操作） | 功能后端有，前端没调用 |
| **redfox** | `radar`、`account/diagnose`、`skills/run`、`call-logs`、`costs/summary` | 竞品雷达/技能执行/成本明细缺 |
| **intelligence** | `dispatches`、`items/:id/import-material|generate-topic|dispatch` | 情报条目动作缺 |
| **growth** | `leads/confirm`、`strategies/:id/apply`、`account-health/*/check`、`acquisition/runs`、`reports` | 获客执行链路缺 |
| **wecom-crm** | `group-msgs/:id/result`、`moments/:id/result`、`configs/:id/contacts` | 企微群发回读缺 |
| **ai-employee** | `workflows` CRUD + runs retry/cancel | 员工工作流编排缺 |
| **savings** | `orders/claim`、`credit/consume`、`watches/disable`、`price-history/*` | 返利提现/比价详情缺 |
| **voice** | `asr`、`tts/stream`、`chat` | 语音链路缺（移动端语音功能） |

---

## 三、未完成/占位功能（用户可见）

| 位置 | 内容 |
|---|---|
| settings-detail.tsx:54 | 「保存接口还没开放（后端开发中）」 |
| solutions/page-legacy.tsx:263 | 「多平台改写正在开发中」；:3478 「暂未开放」 |
| content-workspace-client.tsx:738 | 「当前服务尚未开放草稿保存能力」 |
| interaction/wecom-assistant/page-legacy.tsx:311 | 自动回复开关「暂未开放」（disabled） |
| under-construction.tsx | 通用「该功能正在建设中」占位组件 |
| release-notes / mobile-capabilities | 自述视频工坊/换脸暂未开放 |
| demo/video-studio、demo/wechat-personal | 纯 mock 演示页 |
| login/page.tsx:939,953 | `href="#"` 死链 2 处 |

---

## 四、移动端缺口（桌面 vs 移动）

- **无 isMobile 分支（响应式降级）**：wecom-crm-center、commercial-readiness-center、boss-recruit-center、SessionsPage（agent-workbench 的 414 行移动分支不覆盖它）
- **入口缺口**：solutions/strategies/topics/templates/styles 系列、growth 子页（acquisition/leads）、compliance-v2/risk-v2、intelligence 大部分、workbench 大部分、video 系列——桌面 262 路由 vs 移动约 62 入口

---

## 五、体验风险点

1. **mine 页无 loading/空态骨架**（其它页都有）
2. **savings 页仅 8 行转发 SavingsShell**，shell 有 initialLoading 但无空态/错误态分支
3. console.error 36 处，多为 catch 内日志；**无全局 unhandledrejection 兜底**
4. 部分表单页（wecom-crm/boss-recruit 等）移动端纯桌面布局

---

## 六、建议优先级

**P0（用户直接感知/核心商业）**
1. savings `orders/claim`+`credit/consume` 接入——返利提现链路
2. video product-cut 前端入口——商品视频剪辑（对标功能）
3. growth `leads/confirm`+`acquisition/runs`——获客执行

**P1（能力补全）**
4. redfox radar/skills/run、intelligence dispatches、wecom-crm 回读
5. usage/token 用量页、push-notifications 真订阅

**P2（体验打磨）**
6. mine/savings 补 loading/空态
7. login 死链修复、全局错误兜底
8. 移动端 3 中心页 + SessionsPage 精修

---

## 七、整改追踪（2026-08-10 复核更新）

> 对照本报告逐项核实 + 整改，结论如下。标注：✅ 已修复 / 🔍 核实已接（原审计误报）/ ⏳ 待办

### A1 整模块（6 个）复核

| 模块 | 状态 | 说明 |
|---|---|---|
| usage/token | ✅ 已接入 | `usageTokenApi` + 用量费用页「Token 用量」卡（commit e0d08ed） |
| poi | ⏳ 待办 | 门店数据层仍无前端页面（真缺口） |
| video（product-cut） | ✅ 已接入 | `videoApi` + `/video/product-cut` 页 + content/mine 入口（commit 3ec1bbc） |
| video-generation | ⏳ 待办 | 任务提交/查询仍无前端（真缺口） |
| push-notifications | 🔍 核实已接 | 前端 `use-web-push` hook 已完整实现（SW 注册→订阅→后端登记→取消），设置页开关已接线——原审计误报 |
| client-config | ⏳ 待办 | 商业化配置下发无消费（真缺口） |

### A2 已接模块缺端点（8 个）复核

| 模块 | 状态 | 说明 |
|---|---|---|
| local-engine ai-action | ✅ 已接入 | `runBrowserAiAction` + `/local-engine/ai-action` 页 + 健康中心入口 |
| redfox radar | 🔍 核实已接 | today 页已在用（today/page.tsx:412） |
| redfox skills/run | ✅ 已接入 | `runSkill` + 技能目录「试运行」按钮（commit 8e68dbb） |
| redfox call-logs/costs | 🔍 核实已接 | redfox.ts 有 listCallLogs/getCostSummary，CostsCenter 已在用 |
| intelligence dispatches | 🔍 核实已接 | intelligence.ts:496/505 已封装 |
| growth acquisition/runs | 🔍 核实已接 | growth.ts:448 已封装 |
| growth leads/confirm | ⏳ 边缘缺口 | 「情报→线索」人工确认流，情报页仅静态建议卡（运营增强，非核心） |
| wecom-crm 回读 | 🔍 核实已接 | wecom-crm.ts 有 group-msgs 列表 + result |
| ai-employee workflows | ⏳ 待核实 | 未逐项确认 |
| savings orders/claim、credit/consume | 🔍 后端内部端点 | 前端无需直接调用（提现/兑换/返利直付已全接） |
| voice asr/tts/chat | ⏳ 待核实 | 移动端语音链路未确认 |

### 三、占位功能复核

| 项 | 状态 |
|---|---|
| login 死链（忘记密码/注册） | ✅ 已修复（改为提示联系管理员，commit 7aa0881） |
| settings 保存接口未开放 | ⏳ 后端开发中（真实占位） |
| solutions 改写/workspace 草稿/wecom 自动回复 | ⏳ 真实占位（暂未开放） |

### 四、移动缺口复核

- 内容库（小红书/文章/视频）✅ 已做移动视图（commit 71a1f3a）
- 素材库入口死循环 ✅ 已修复（移除 alias，commit 79ff6eb）
- wecom-crm/commercial-readiness/boss-recruit/SessionsPage：⏳ 评估为表单/卡片响应式可用（grid-cols-2 等），精修待办
- 入口缺口：mine「更多功能」已补 19 项（部分覆盖 intelligence/growth/video）

### 五、体验风险复核

| 项 | 状态 |
|---|---|
| mine 页无骨架 | ✅ 已修复（billing 骨架，commit 7aa0881） |
| savings 页空态/错误态 | ⏳ 待办 |
| 全局 unhandledrejection 兜底 | ⏳ 待办 |
| 表单页移动布局 | ⏳ 待办 |

---

## 八、更新后优先级（2026-08-10）

**P0 ✅ 完成**：savings 提现（核实已接）+ video 商品剪辑（已接）+ growth 获客（核实已接）——核心商业闭环已通

**P1 ✅ 完成**：usage/token（已接）、push 订阅（核实已接）、ai-action（已接）、skills/run（已接）、radar/dispatches/wecom 回读（核实已接）

**待办（原 P1/P2 剩余）**：
1. poi 门店数据前端页、video-generation 前端、client-config 消费（整模块 3 个真缺口）
2. ai-employee workflows、voice asr/tts 核实
3. growth leads/confirm（边缘）
4. savings 空态/错误态、全局错误兜底
5. 移动端 3 中心页 + SessionsPage 精修

---
*审计基于代码扫描 + 模拟器运行；整改追踪基于三轮逐项核实（原「未接线」清单多数为审计 grep 方法误报，已标注）。*
