# 3010 内置浏览器面板改造 · 完整复核清单（2026-09-05，四审修订版）

> **四审（2026-09-05 深夜）处置完毕**：P0 凭据硬编码（脚本 env 化 + 历史重写，轮换待大王）、P1 E2E 证据软化（四断言硬门槛全 PASS）、P1 审计归属断链（tenantId/userId 补齐 + 断言）。详见 H 段。
> 本清单**自包含**：每项含 file:line、可验证命令、预期结果。复核人逐项打勾即可。
> 提交序列（main，逐文件 add、独立 commit、未用 --no-verify；**2026-09-05 经 git-filter-repo 历史重写清除凭据，hash 已全部变化**）：
> `be4920702`（←旧5fce5816 竞态+闸门+readiness）→ `ac83e240b`（←旧21a31440 desktop 链路）→ `6e2028df5`（←旧c8e3062f openAccounts.browser）→ `0f7a53d84`（←旧a4fa578b 验证器+smoke）→ `564d84a2e`/`ff3f21774`/`0397e1042`（←旧cb516f82/bcd98137/3a10b26b docs）→ `61db0ed1d`（←旧22eae4e7 fail-closed）→ `02b62b435`（←旧4d73b910 闸门前移+审计）→ `dcebb0ff8`（←旧ab84f5be 清单）

---

## A. 门禁总闸（先跑这个）

| # | 命令（cwd） | 预期 |
|---|---|---|
| A-1 | `cd backend && npx tsc --noEmit` | exit 0，无输出 |
| A-2 | `cd backend && npx jest src/modules/local-engine src/modules/comment-acquisition --silent` | **32 suites / 624 tests 全绿** |
| A-3 | `cd backend && npx jest src/modules/auto-upload --maxWorkers=1 --silent` | 14 suites / 189 tests 全绿，**无 worker force-exit 警告**（⚠️ 口径固化为 --maxWorkers=1：多 worker 全量并行存在 jest 回收竞态，单套件/串行/分组全零——已定位为测试基建竞态非产品缺陷，见 H-4） |
| A-4 | `cd desktop && node browser-broker-wiring.spec.js && node browser-agent-bridge-server.spec.js && node browser-panel-bridge-runtime.spec.js && node browser-panel-manager.spec.js && node browser-panel-broker.spec.js` | WIRING **23** / BRIDGE **25** / RUNTIME **9** / MANAGER **52** / BROKER **45** 全 PASSED |

## B. P0-1 账号隔离（竞态 → 互斥 + 页面级绑定）

| # | 位置 | 看什么 |
|---|---|---|
| B-1 | `backend/src/modules/local-engine/local-browser-engine.service.ts:144`（`panelAcquireChain` 字段）、`:630-640`（mutex 包装 `tryAcquireDesktopPanelSession` → `doTryAcquireDesktopPanelSession`） | 面板获取全链（核验→panelOpen 切账号→取页→登记）经 promise 链**整体串行** |
| B-2 | 同文件 `:548-592`（`acquirePanelPage` 接收 `binding {wantAccount, panelId}`）、`:594-612`（`pageBindingMatches`） | 候选 page 除 URL 初筛外**必须**过页面级绑定标记复核；标记缺失/evaluate 异常 = fail-closed 拒收 |
| B-3 | `desktop/browser-panel-manager.js:649`（`_injectPanelBindingMarker`）、`:594`（did-finish-load 重注入）、`:729`（open 后立即注入） | desktop 每次导航完成/open 把 `{panelId, accountId, partition}` 写进页面全局 `window.__kaypalPanelBinding` |
| B-4 | 单测 `local-browser-engine.service.spec.ts`（标记拒收/收页/缺失拒收/并发互斥 4 用例） | `npx jest src/modules/local-engine/local-browser-engine.service.spec.ts` → 25/25 |
| B-5 | **真机 E2E（四审 P1 后硬化）**：`cd backend && SMOKE_E2E_USER=<测试账号> SMOKE_E2E_PASS=<测试密码> node scripts/smoke-engine-panel-e2e.mjs`（凭据走环境变量**必填**，缺失 exit 2） | **四断言全 PASS 才输出 E2E_OK（否则 exit 1）**：①外部 Chrome 0 增量 ②`openedAccounts[0].browser === 'desktop-panel'`（硬失败，无 WARN 软化）③CDP 面板 target 命中 douyin ④面板桥 `/panel-state` 台账绑定：`accountId === 选中账号 id` 且 `panelId` 非空。**2026-09-05 23:08 真机实测：四断言全 PASS + E2E_OK + exit 0**（证据：panelId=panel-35987ad74dea, accountId=24, partition=persist:kaypal-browser-local-desktop-24） |

## C. P0-2 获客/互动写链审批闸门（fail-closed）

| # | 位置 | 看什么 |
|---|---|---|
| C-1 | `backend/src/modules/local-engine/platform-interaction-executor.service.ts:346`（`ensureDispatchWriteGate`） | **闸门在 `getOrCreateSession` 之前**（见 dispatchWithLocalBrowser 内 `expectedSessionKey` 段）——拒绝时不创建浏览器会话 |
| C-2 | 同文件 `:354-368`（无桥 fail-closed）、`:391-412`（桥异常 fail-closed） | 默认返回 `gate='gate-unavailable'` 拒绝执行，**不放行写操作** |
| C-3 | 同文件 `:281-283`（`debugBypassEnabled`） | 唯一旁路开关 `INTERACTION_GATE_BYPASS=1`：默认关闭、仅调试显式开启、旁路同样落审计 |
| C-4 | 同文件 `:291-330`（`persistGateAudit`） | 拒绝（`status='rejected'`）/旁路（`approved`+note）落 `AgentConfirmation` 长期审计行，`confirmationJson.source='interaction-gate'`，**归属显式 `tenantId='local-tenant'`/`userId='local-engine'`（四审 P1：与面板确认单 persistTicket 一致，不走 schema legacy-local-* 默认值）**，best-effort 失败只 warn |
| C-5 | 同文件 `:465-470` + 深处回写 | 经闸路径：requestAction 签单 → in_use → sent/draft=consumed、失败=pending（面板单状态机即审计，不重复落） |
| C-6 | `backend/src/modules/local-engine/local-browser-engine.service.ts`（`ensurePanelWriteGate` + click/fill） | desktop-panel 会话的 click/fill 强制过闸（autoApproved 才执行 + consumed 回写）；spawn 会话维持原语义 |
| C-7 | 单测 `platform-interaction-executor.service.spec.ts:1112` 附近（fail-closed×2 + 调试旁路×1 + 存量 dispatch 测试经自动批准 mock 桥过闸；fail-closed 用例含 `tenantId`/`userId` 归属断言） | `npx jest src/modules/local-engine/platform-interaction-executor.service.spec.ts --maxWorkers=1` → 19/19 |

## D. P1 三项

| # | 项 | 位置 | 验证 |
|---|---|---|---|
| D-1 | mcp readiness 解耦可见性 | `playwright-mcp.service.ts:509`（getStatus）与 `:539`（getAutomationStatus）两处都无 `visibleWindow` 条件；`:198` 默认 headless（`LOCAL_MCP_VISIBLE`） | `curl -s http://127.0.0.1:3011/api/mcp/status` 与 3013 同 → `readyForAutomation=true`（visibleWindow=false 合法） |
| D-2 | panel 路由 actor 归属校验 | `desktop/browser-broker-wiring.js:27`（`ENGINE_AGENT_ACTOR`）、`:32`（`assertEngineAgentActor`）、`:272`（panelStateForAgent）、`:342`（openPanelForAgent）；路由透传 `desktop/browser-agent-bridge-server.js:172-184` | 活体（读 `~/Library/Application Support/ai-content-desktop/browser-panel-bridge.json` 后带 header 调 `/panel-state`）：错误 actor → **403 POLICY_DENIED**；引擎 actor → 200；缺 actor → **400 ACTOR_REQUIRED** |
| D-3 | B 类验证器硬失败 | `backend/scripts/verify-b-class-realmachine.mjs:48-53`（`VERIFY_MIN_LEAD_SCORE<101` → exit 2）、`:273`（清理失败 `exitCode=1`） | `VERIFY_MIN_LEAD_SCORE=0 node scripts/verify-b-class-realmachine.mjs kuaishou 2 穿搭` → 打印「低于 101…拒绝执行」exit **2** |

## E. 可观测性

| # | 项 | 位置 | 验证 |
|---|---|---|---|
| E-1 | openAccounts 透出承载方式 | `backend/src/modules/auto-upload/auto-upload.client.ts:2620`（`browser: session.sessionMode === 'desktop-panel' ? … : 'external'`） | smoke-engine-panel-e2e 断言②即此字段的真机验证 |
| E-2 | 桥与 App 同生命周期 | `desktop/browser-panel-bridge-runtime.js`（`_syncNow` 对 hidden/destroyed/account-switched 返回 kept） | RUNTIME SPEC 9 绿；面板收起后引擎可 panelOpen 重新展开（安全语义变更已交底） |
| E-3 | 空白窗壳 CDP 死锁修复 | `desktop/main.js`（mainWindow `loadURL('about:blank')`）+ App 启动即建桥写凭据 | `curl http://127.0.0.1:9333/json/list` 含 `about:blank` 主窗 target；`browser-panel-bridge.json` mtime = App 启动时刻 |

## F. 遗留项（复核口径一致，未关闭）

| # | 项 | 现状 | 关闭条件 |
|---|---|---|---|
| F-0 | **测试账号凭据轮换（四审 P0 硬要求）** | **七审状态（2026-09-06 02:40）：云端旧口令仍 200 有效（新口令 401）——云端改密未生效，P0 未闭环；memory 服务 403 不可用（memory 与账号认证两套状态不一致）**。本地已清零：Git 历史（三轮 filter-repo 后全 blob 双串零命中 + dev.db 二进制库整删）、`origin/main=708acb0f` 已推。本机文件残留 4 处待改密后同步：`backend/.env:84-85`（gitignore 已覆盖，不入库）、`env.secure`、`env.secure.bak-20260904`、（新口令）`smoke-e2e.env`。session 删除 29 条经核全属审计会话（现存会话止于 8/20，本机库无外部用户流量） | 固化顺序：**①Kaypal 云端改密+对齐 memory 两套认证（大王）→ ②我同步 4 处本机文件 + 重启 3011/3013 → ③验证：旧口令云端 401、新口令云端登录 200、memory 登录 200 → ④无强推必要（历史已清，GitHub 服务端残留对象等 gc，轮换后旧口令自然失效）** |
| F-1 | **B 类真机 PASS**（11 证据 0 PASS：kuaishou 风控 ERR_ABORTED、xiaohongshu 需真实登录态） | 本机不可解（已双通道实测） | **jz-win11 用已登录的小红书/快手账号**跑 `node scripts/verify-b-class-realmachine.mjs <platform> <accountId> <keyword>`，拿到 `HTTP 2xx + scanned>0 + 非降级 + 无写库` 的 PASS json |
| F-2 | **worktreeDirty 归零 / 源码↔发布包干净绑定** | 剩少量 M 文件 = 并发方 WIP，不可代提交；main 无 upstream；**filter-repo 重写后本地领先 origin/main 14+ commits，push 需 force（等大王决定推不推/怎么推）** | 大壮落库后重跑 B 验证器；发版前再走安装包内置浏览器端到端验收 |
| F-3 | 生产包受控开启 CDP + 9333 token 收口（任务 #45）、面板锁定 + 登录态迁移（任务 #46） | 未动 | 按任务排期 |
| F-4 | 安装包内置浏览器端到端验收 | 未做（建议 F-1/F-2 闭环后与打包一并验，避免验两遍） | 打包后按 E-3/E2E 清单在安装包复跑 |
| F-5 | **面板桥凭据 1h 老化窗口运维注意（本轮实测发现）** | `agent-panel-bridge.service.ts:87` `DEFAULT_MAX_AGE_MS = 1h`：App 启动超 1h 后，3011 引擎 panel-state 核验报 `PANEL_UNAVAILABLE` → 兜底 spawn 外部 Chromium（不是 bug，是设计行为，但 E2E/演示前需重启 App 刷新凭据） | E2E/演示前检查 `browser-panel-bridge.json` mtime 是否在 1h 内；若要放宽窗口需大王拍板 |

## H. 四审处置记录（2026-09-05 深夜）

| # | 判定 | 处置 | 结果 |
|---|---|---|---|
| H-1 | **P0：smoke 脚本提交可用登录凭据**（原 :11-12 硬编码账号密码） | ①脚本重写为 env 必填（`SMOKE_E2E_USER`/`SMOKE_E2E_PASS` 缺失 exit 2）+ 四断言硬失败；②git-filter-repo 历史重写：`--replace-text` 清 blob 密码 + `--replace-message` 清 commit message 用户名，本地 main 双清零命中；③凭据轮换移交大王（F-0） | ✅ 代码闭环；⚠️ 远端历史轮换在大王 |
| H-2 | **P1：清单 B-5 E2E 命令证明不了断言**（browser 失败只 WARN、E2E_OK 门槛太松、无四断言） | smoke 脚本重写：`fail()` 全部 `process.exit(1)`；新增断言④台账绑定（读 bridge.json 三头调 `/panel-state`，断言 accountId+panelId） | ✅ 真机重跑四断言全 PASS + E2E_OK + exit 0（23:08） |
| H-3 | **P1：闸门审计 AgentConfirmation 归属断链**（legacy-local-* 默认值，与面板单 local-tenant/local-engine 不一致） | `persistGateAudit` 显式写 `tenantId:'local-tenant'`/`userId:'local-engine'` + spec 两行归属断言 | ✅ 单测过（executor 19/19，local-engine 28 套 577 全绿） |
| H-4 | P2：auto-upload 套件 jest worker force-exit | ✅ 定位完毕：非套件泄漏（单套/串行/分组全零），是 maxWorkers≥2 全量并行的 jest worker 回收竞态，产品代码无缺陷。门禁口径固化 --maxWorkers=1 | ✅ 结案（2026-09-06） |
| H-5 | **filter-repo 连带事故（自查发现）**：worktree reset 冲掉未提交改动——除本轮三处编辑外，**大壮未提交的 `resolveDesktopUserDataDir`（`src/common/project-paths.ts`）定义也被冲掉**（该函数从未进过 Git 历史，但 `agent-panel-bridge.service.ts:233/899/954` 三处引用已提交 → tsc 断） | 按调用方语义重建（env 优先 → 平台默认 userData 目录 → 目录不存在 fail-closed 返回 null），**与原实现可能有差异，交底**；tsc 恢复全绿 | ✅ 已重建待大壮核对 |

## G. 运行态快照（四审复核时点 2026-09-05 23:08）

- 桌面 App：9333 CDP 监听（23:07 重启刷新凭据），桥凭据随启动重写
- 3011 / 3013：health 200，`/api/mcp/status` → `readyForAutomation=true`（3011 重启吃新 bundle：env 化 smoke 脚本 + 审计归属 + project-paths 重建）
- 最近一次真机 E2E（四审后硬化版）：**E2E_OK + 四断言全 PASS + exit 0**（browser=desktop-panel、panelId=panel-35987ad74dea、accountId=24）
- 门禁：backend tsc 0 错；local-engine 28 套 577、comment-acquisition 4 套 47、auto-upload 14 套 189（P2 worker force-exit 警告仍在，H-4）；desktop 五套件 23/25/9/52/45 全 PASSED
