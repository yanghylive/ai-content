# 浏览器面板项目基线证据（阶段 0）

- 采集时间：2026-09-03 00:00 +0800
- 采集人：二狗（Workbuddy）
- 工作流文档：`browser-panel-workbuddy-implementation-workflow.md`（大王 2026-09-02 定稿）

## 1. 仓库与环境

| 项 | 值 |
|---|---|
| 分支 | `main`，工作区干净（无未跟踪业务代码修改，仅历史 gate-evidence 目录） |
| HEAD | `8690813c`（fix(growth): 抖音搜索页加载等待优化） |
| 复核第七轮安全提交链 | `99d13fc4`/`4cd10484`/`974b564c`/`72b29a5e` 均为 HEAD 祖先（`git merge-base --is-ancestor` = YES） |
| Node | v22.22.2 |
| Electron | **32.3.3**（desktop devDependencies `^32.0.0`）——`WebContentsView`/`webContents.debugger` 在 32.x 均为稳定 API |
| Playwright | `^1.60.0`（backend） |
| 包内 Chromium（mac-arm64） | `desktop/runtime/playwright-browsers/chromium/chrome-mac-arm64/Google Chrome for Testing.app/…` ✅ 存在 |

## 2. 运行态健康

| 项 | 状态 |
|---|---|
| 3010 | HTTP 302（登录重定向，正常） |
| 3011 Agent-S | `ok=true status=ready runner_mode=node-playwright browserControl=true persistentProfiles=true` |
| 3011 MCP | 上轮已验证 `playwright online=true` |
| 系统库 | `~/.workbuddy/ai-content-backend/kaypal-ai.sqlite` |

## 3. 当前外链打开调用点清单（待内置化迁移的面）

| 位置 | 说明 |
|---|---|
| `desktop/main.js:2561` | `shell:open-external` IPC handler（主进程出口） |
| `desktop/preload.js:58` | 主窗口 preload 暴露 `openExternal(url)` |
| `desktop/preload.js:92` | 标签页 preload 暴露 `openExternal(url)` |
| `desktop/workspace-tabs.js:362` | 标签页对特定 URL 直接外开（新窗口拦截 fallback） |

前端调用面：3010 页面内 `window.electronAPI?.openExternal?.(url)` 类调用散布于业务组件（阶段 5 按平台迁移时逐点处理，本轮不动）。

## 4. 已有底座（复用点）

- `desktop/workspace-tabs.js`：WebContentsView 标签壳、持久 partition、IPC origin 校验——已验证在 Electron 32 下稳定运行（生产包在用）。
- `backend/src/modules/local-engine/local-browser-engine.service.ts`：打包 Playwright Chromium + CDP + 账号 profile。
- `backend/src/modules/local-engine/agent-browser*`：会话租约、动作白名单、确认闸门、事件记录、截图证据。

## 5. 本轮（第一轮）范围声明

只做工作流文档第 8 节：阶段 0 基线（本文件）+ 阶段 1 同页控制 P0 验证 + Mac 实跑 + Windows 静态兼容检查。**不做**：右侧 UI、平台迁移、发布包提交。

## 6. 风险交底

- 工作区历史存在并发修改方（抖音采集/探活修复），main 随时前进；本轮新增文件仅为 `docs/browser-panel-baseline/`、`desktop/test-fixtures/`、`desktop/scripts/browser-panel-smoke.mjs`、`desktop/browser-panel-broker.spec.js`，逐文件 add，禁止整目录。
- 同页控制 P0 若在 Mac/Windows 任一平台不稳定，按文档停在阶段 1 上报三方决策。
