# Content Workspace G1.1 D005 Live Validation

- Date: 2026-07-24 (America/Los_Angeles)
- Requirements: UX-05, UX-08, UX-13
- Result: accepted
- runtime: 3010/3011
- primary-action-count: 1
- historical-brief-source: source-not-recorded

## Scope and outcome

D005 reopened G1.1 only for the combined states that the first G1.1 pass did not cover. The correction does not claim completion of the G2 AI lifecycle.

- A visible workspace exposes at most one primary action.
- A pending rule preview owns the primary action and suppresses footer progression until it is accepted or dismissed.
- Changing the article or workflow step clears the pending preview.
- Version-row actions use the secondary bordered appearance; footer progression remains the primary action.
- A non-empty historical brief without `fieldSources` displays `历史简报，来源未记录` instead of guessing a source.
- Existing provenance is preserved, and a user edit remains `source=user`, `label=已由你修改`, `edited=true`.
- UX-08 remains `partial`: the local rule preview is still disclosed as fixed text logic, and real AI jobs, undo and persisted version recovery remain G2 work.

## Astryx evidence

The local Astryx CLI `0.1.7` was queried with:

```text
node packages/cli/bin/astryx.mjs --zh --detail compact component Button
```

The retrieved Button guidance requires one primary action per view. This pass uses that guidance as interaction evidence. The application still uses HeroUI; no Astryx React component library migration was performed while `CW-D003` remains open.

## Automated verification

- Frontend content-workspace suite: 64/64 passed.
- Action-state behavior tests: pending preview ownership, normal progression, step cleanup and version-row hierarchy passed.
- Historical brief tests: missing provenance, partial provenance and user override persistence passed.
- Static route, zero-loss navigation and publish-boundary guards passed.
- Frontend TypeScript and targeted ESLint passed.
- Backend article/controller/Prisma suites: 36/36 passed.
- Backend TypeScript passed.
- Contract guard passed with G1.1 active and after restoring G2.

## Runtime and responsive verification

The signed-in 3010 frontend and 3011 backend were exercised before cleanup.

| Viewport | State | Result |
| --- | --- | --- |
| 390x844 | Historical brief | Explicit source-not-recorded labels; one visible progression primary; document width 390 px |
| 390x844 | Rule preview | `应用规则建议` was the only primary action; footer progression was suppressed |
| 1024x768 | Rule preview | One primary action; no horizontal overflow |
| 1024x768 | Versions | Footer progression remained the only primary action |
| 1440x900 | Versions | Three-column layout remained intact; footer progression remained the only primary action |

Browser console errors and warnings attributable to the workspace: 0. No publish action was executed.

## Evidence files

- `docs/content-workspace/evidence/g1.1-d005-workspace-390-historical-brief.png` (`3eef36bddb5a9ac3bf5c380550a31faca0cbf14018ecea5bffb39de6bb0deecf`)
- `docs/content-workspace/evidence/g1.1-d005-workspace-390-rule-preview.png` (`701a23dd49635c206890bf29155428824b825b6007717d309f7eb3e3234fe4f2`)
- `docs/content-workspace/evidence/g1.1-d005-workspace-1024-rule-preview.png` (`fe98957851158e9001da14afc4ec706271a06846291b44e3643fba479d584d2b`)
- `docs/content-workspace/evidence/g1.1-d005-workspace-1024-versions.png` (`f2710cc55b74f7e0cb99abd4bd59ee0d6a1862e414294d2deb59fb98dc708384`)
- `docs/content-workspace/evidence/g1.1-d005-workspace-1440-versions.png` (`0cc1de964e90fbbe41de365784d61d7a980fd9c163833362b97de0cde2a7dc93`)

## Cleanup and non-drift checks

The two UI-created D005 QA articles were deleted after validation. The attempted isolated version-row SQL fixture failed before insertion and created no rows. The pre-existing target article was read-only during D005 validation. Baseline prototype and development-plan SHA-256 values remained unchanged.
