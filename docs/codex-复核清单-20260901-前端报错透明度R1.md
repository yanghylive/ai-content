# Codex 复核清单——前端报错透明度 R1 回改（allSettled rejected 分支）

> 生成：2026-09-01 01:57 · 待复核 commit：**`7c14688d`**（R1 回改 10 文件）+ **`b0efcb60`**（channel-console loadAccounts 补刀）
> 背景：Codex 上轮打回 4 条（auto-acquisition / wechat-task / engine-health / channel-console，全部成立），本轮 7c14688d 全修；另同类自查 5 处一并修。本清单供 Codex 逐项复核 R1 是否闭环。
> 自包含要求：每项含 file:line + 可验证命令 + 预期结果；行号为 b0efcb60 实测（非推算）。

---

## 一、复核范围与基准

| 项 | 内容 |
|---|---|
| 待复核 commit | `7c14688d`（主体）+ `b0efcb60`（loadAccounts 补刀，Codex 下轮大概率会打的点，已先修） |
| 复核文件数 | 10 个（§三 A-G） |
| 已跑门禁（提交前） | frontend tsc 0；eslint 10 文件 0（channel-console 含 b0efcb60 复跑 0） |
| 评审基准 commit | `git merge-base --is-ancestor 7c14688d HEAD` 与 `git merge-base --is-ancestor b0efcb60 HEAD` 均应 exit 0；HEAD 允许有后续文档/记忆提交 |

**核心审查命题**：`Promise.allSettled` 的 rejected **永远不进 catch**。本轮统一修法 = rejected 守卫（类型守卫 `filter (r): r is PromiseRejectedResult` 或等价的 `status === "rejected"` 判断）→ 原因上屏（reportLoadError / onError / 检查项），**全 fulfilled 才 clearLoadError**。逐项验证的就是这个模式有没有写对、有没有漏。

---

## 二、门禁命令（先跑，预期全绿）

```bash
cd frontend && ./node_modules/.bin/tsc --noEmit            # 预期：无输出，exit 0
./node_modules/.bin/eslint \
  "src/app/(dashboard)/apps/auto-acquisition/auto-acquisition-center.tsx" \
  "src/app/(dashboard)/engagement/wechat/wechat-task-center.tsx" \
  "src/app/(dashboard)/crm/crm-center.tsx" \
  "src/app/(dashboard)/local-engine/engine-health-center.tsx" \
  "src/app/(dashboard)/engagement/_components/channel-console.tsx" \
  "src/app/(dashboard)/settings/settings-integrations.tsx" \
  "src/app/(dashboard)/local-engine/engine-run-detail.tsx" \
  "src/app/(dashboard)/distribution/publish-flow.tsx" \
  "src/app/(dashboard)/growth/leads/detail/page.tsx" \
  "src/app/(dashboard)/effects/page.tsx"
# 预期：0 problem，exit 0
```

分支覆盖扫描（确认没有第 11 处漏网）：

```bash
grep -rn "Promise.allSettled" frontend/src/app/\(dashboard\) --include="*.tsx" | cut -d: -f1 | sort -u
# 预期 11 个文件：其中 9 个含 rejected 分支守卫（7 个用
# PromiseRejectedResult，publish-flow / engine-run-detail 用自定义状态分支判断），
# 2 个例外已有自己的 rejected 处理：
#   crm/customers/[id]/customer-detail-center.tsx → setPartialErrors
#   content/content-hub-page.tsx → nextErrors → setError
# 复核方式：逐文件查 `PromiseRejectedResult` 或自定义 fulfilled/rejected 分支
# （publish-flow 用 `status === "rejected"`，engine-run-detail 用 `status === "fulfilled"` 的 else），
# 不要只用 `grep -L PromiseRejectedResult`，否则会把上述 2 个自定义守卫误算为漏网。
```

---

## 三、逐项复核（A-G）

### A. [P0] auto-acquisition-center——rejected 守卫 + 全 fulfilled 才 clear

文件：`frontend/src/app/(dashboard)/apps/auto-acquisition/auto-acquisition-center.tsx`

| 检查点 | 位置 | 预期 |
|---|---|---|
| A1 rejected 守卫 | :37-40（`:39 (r): r is PromiseRejectedResult`） | `[overview, configs]` 逐个 filter rejected |
| A2 失败上屏 | :43-48 `reportLoadError(rejected[0].reason, ...)` | 全败（2 个）→「获客数据暂时无法读取」；部分败（1 个）→「获客部分数据暂时无法读取，统计可能不准确」 |
| A3 成功才清错 | :49-51 `else { clearLoadError(); }` | **不得**出现无条件 clearLoadError（grep 该函数体应只有这一处，且在 else 里） |
| A4 数据降级保留 | :27-33 | o/c 仍按 fulfilled 降级——部分失败时已成功的数据照常渲染（设计意图：降级显示 + 原因上屏，不是白屏） |
| A5 catch 仍兜底 | :52-56 | try 内同步异常仍走 catch → reportLoadError |

负向审查（读代码验证，无需跑服务）：
- A-N1：若 overview rejected 且 configs fulfilled → stats 来自 o 的 `?? 0` **且** banner 出现"部分数据"文案。若 Codex 认为"部分败仍渲染 0 统计"应升级（比如给对应卡片标"数据缺失"），请在复核意见中列 P1 建议，不判 P0（当前行为已从"假成功"升级为"降级 + 明示原因"）。
- A-N2：deps 数组应为 `[clearLoadError, reportLoadError]`（:58 附近），漏了会 exhaustive-deps warning。

### B. [P0] wechat-task-center——三路 rejected 守卫

文件：`frontend/src/app/(dashboard)/engagement/wechat/wechat-task-center.tsx`

| 检查点 | 位置 | 预期 |
|---|---|---|
| B1 守卫位置 | :135-142（`:138 PromiseRejectedResult`） | `[contactsResult, tasksResult, sessionResult]` 三路 filter |
| B2 文案区分 | :142-147 | 3 路全败→「微信任务中心数据暂时无法读取」；部分败→「…部分数据暂时无法读取，统计可能不准确」 |
| B3 成功才清错 | :148-150 else clearLoadError() | 同 A3，无无条件清错 |
| B4 插入位置 | 守卫在 `setRecentTasks(sorted...)` **之前**（:151 起） | 失败时最近任务列表仍照常渲染已成功数据 |
| B5 catch 兜底 | :168-172 | 保留 |

### C. [P0] crm-center——同款守卫（自查补修，Codex 未点名）

文件：`frontend/src/app/(dashboard)/crm/crm-center.tsx`

| 检查点 | 位置 | 预期 |
|---|---|---|
| C1 守卫 | :98-105（`:101 PromiseRejectedResult`），`[list, summary]` | 同 A 模式 |
| C2 文案 | :105-110 | 2 路全败→「客户数据暂时无法读取」；部分败→「客户部分数据暂时无法读取，统计可能不准确」 |
| C3 else clear | :111-113 | 同 A3 |
| C4 banner 渲染 | 主 return 顶部（grep `LoadErrorBanner message` 应命中，onRetry → `void fetchData()`） | retry 直调原加载函数 |

### D. [P1] engine-health-center——partial failure 带具体原因

文件：`frontend/src/app/(dashboard)/local-engine/engine-health-center.tsx`

| 检查点 | 位置 | 预期 |
|---|---|---|
| D1 rejectedResults | :84-93（`:87 PromiseRejectedResult`） | 四路（health/readiness/browser/tasks）filter |
| D2 checkFailed 联动 | :90 `const partialFailure = rejectedResults.length > 0` | 原 `.some(...)` 语义等价改写 |
| D3 原因上屏 | :92-94 `reportLoadError(rejectedResults[0].reason, "部分状态检查失败")` | rejected reason 经 toPublicError 落 loadError |
| D4 条件清错 | :155 `if (!partialFailure) clearLoadError();` | **不得**无条件 clear |
| D5 两处卡片吃原因 | :280 与 :440 `{loadError ?? "部分状态检查超时或失败…"}` | 移动/桌面两分支都显示具体原因，无文案时回落原文案 |
| D6 catch 兜底 | :156-165 | 保留（含 reportLoadError） |

### E. [P0] channel-console——独立 loadError + 成功清错不残留 + loadAccounts 补刀

文件：`frontend/src/app/(dashboard)/engagement/_components/channel-console.tsx`

| 检查点 | 位置 | 预期 |
|---|---|---|
| E1 独立状态 | :107-108 `useLoadError()` 与共享 `error` 分离 | refreshTasks/loadAccounts **不得**再写 setError（grep setError 调用点，:169 一带应只有 loadError 系） |
| E2 refreshTasks 成功清错 | :173 `clearLoadError()` 在 fulfilled 路径 | 成功后旧任务加载错误不残留 |
| E3 deps 完整 | :180 `[config.businessRoute, reportLoadError, clearLoadError]` | exhaustive-deps 无 warning |
| E4 loadAccounts 补刀（b0efcb60） | catch `:153-160` | 失败：console.error + reportLoadError + 仍降级 `setAccounts([])`；deps 加了 reportLoadError |
| E5 两分支渲染 | 移动 :419-423（mx-card 风格，文案前缀「任务列表加载失败：」）；桌面 :621-623（LoadErrorBanner + 重试钮 onRetry → `void refreshTasks()`） | 两处都出现且互不冲突 |
| E6 操作 error 不受污染 | grep `setError(` 剩余调用点（:205/:253/:273/:287/:291/:295/:344/:350/:366 附近） | 建任务/确认/CRM 测试的操作错误仍走共享 error，不被任务加载成功清掉 |
| E7 两个加载源竞态 | :195 的 `Promise.all([loadAccounts(), refreshTasks()])` | ~~当前实现待打回 P0~~ → **R2 已修**：拆为账号/任务两个独立 loadError 实例，各自成功只清各自的错，见 §七 R2-1 |

### F. 自查批次（无 Codex 打回，但请复核修法是否成立）

| # | 文件 | 检查点 | 位置/预期 |
|---|---|---|---|
| F1 | settings/settings-integrations.tsx | rejected → onError；:70-78 | catch 兜底仍在（:79-82）；deps 含 onError 原本就有 |
| F2 | local-engine/engine-run-detail.tsx | readiness else 分支补失败检查项 :85-94，message 含 `toPublicError(readinessResult.reason, "无法读取，请重新检查")` | health 失败原有「无法连接引擎」项不变 |
| F3 | distribution/publish-flow.tsx | failedNames 按 sourceNames（文章/平台账号/素材）命名上屏 :171-185；setError 走 toPublicError | 视频模式 articleData 恒 fulfilled（Promise.resolve），不受影响 |
| F4 | growth/leads/detail/page.tsx | :103-116 rejected → setMsg 中性提示（全败/部分败区分）；import 合并 `toActionableError, toPublicError` | msg 渲染为中性灰条（非成功绿），可接受；若 Codex 认为应加 danger 色请列 P2 建议 |
| F5 | effects/page.tsx | 新增 `errMsg` 独立状态 :46（danger 色 + role=alert，:198-200）；rejected 与 catch 都走 setErrMsg（:60-73）；**原 bug 修复**：catch 不再把加载失败写进绿色成功样式的 msg | msg 仅保留成功类通知 |

### G. 横切检查（防第 11 处漏网 + 模式一致性）

| 检查点 | 命令 | 预期 |
|---|---|---|
| G1 全仓 rejected 守卫覆盖 | §二第三条命令 | 11 个 allSettled 文件中 9 个有 rejected 守卫（7 个 `PromiseRejectedResult` + 2 个自定义 fulfilled/rejected 状态分支），仅 2 个例外文件已有自身处理 |
| G2 clearLoadError 位置 | `grep -rn "clearLoadError();" frontend/src/app/\(dashboard\) --include="*.tsx"` 后逐行核对上下文 | 当前 15 处：auto-acquisition :50、crm :112、wechat :149 必须位于 rejected 守卫的 `else`；engine-health :155 必须带 `if (!partialFailure)`；其余才是直接 await 成功路径 |
| G3 LoadErrorBanner 组件 | `frontend/src/components/load-error-banner.tsx` | LoadErrorBanner + useLoadError；toPublicError 兜底；无技术细节外泄 |

---

## 四、P0/P1/P2 判定标准

| 级别 | 判定 |
|---|---|
| P0 | rejected 分支仍假成功（数据降级 + 无任何上屏）；或无条件 clearLoadError 出现在 allSettled 模式函数；或 setError/useLoadError 串台导致操作错误被误清；或并行加载源共享一个可独立清除的 loadError，成功源能抹掉失败源的错误 |
| P1 | 原因上屏但文案不可行动/错误样式误导（如错误显示为成功色）；deps 缺失导致闭包旧值 |
| P2 | 文案措辞、样式风格、可进一步改进项（如部分败时给对应卡片标"数据缺失"角标） |

## 五、已知局限交底（复核时勿重复打回，但欢迎升级建议）

1. **部分败仍降级渲染**（A-N1/B 部分败）：当前设计 = 成功数据照常渲染 + banner 明示"部分数据可能不准确"，未给单卡片标"数据缺失"。升级需动卡片组件，本轮未做。
2. **分支级负向测试未建**：本轮验证 = tsc/eslint + 静态人核 + Codex 上轮的 jest douyin 67 / release-guards 25/25。**没有**断单接口的 jest/E2E 断言 banner 出现——这是上轮"静态门禁全绿但 rejected 漏点"教训的制度化缺口，挂账待做（建议 jest.mock 单接口 reject 断言 banner 文案）。
3. **客舱里的其他 catch**：审计报告 §三 P1（materials 操作 3 处丢原因、ops-workbench 轮询 4 处断流）与 §四 112 文件 toPublicError 批次**尚未开修**，不在本轮复核范围。
4. engine-health D2 的 `partialFailure` 改写与原 `.some()` 语义等价，但 rejected reason 的 console.error 每轮全量打——若嫌吵可后续收敛。
5. effects 页 msg/errMsg 双状态并存是刻意拆分（成功绿/失败红），未合并。

## 六、复核结论回填

- [ ] 门禁 §二 全绿
- [ ] A-G 逐项核对完成，P0/P1 缺陷数：____
- [ ] 结论：通过 / 打回（缺陷清单按 file:line 列出）

---

## 七、R2 回改记录（channel-console 共享 loadError 竞态）

> 2026-09-01 · 回应 Codex R2 结论「不能签收 R1 全绿，唯一阻断项是 channel-console 共享 loadError 竞态」。
> 竞态复述：`Promise.all([loadAccounts(), refreshTasks()])` 并行启动，两路共用一个 `loadError`——账号加载失败上报后，任务加载成功执行 `clearLoadError()` 会把账号错误抹掉，页面重新呈现为"无错误的空账号列表"。
> 修法：按 Codex 建议**拆分两个独立 loadError 实例**，各自成功只清各自的错。

### R2-1 修复点（文件：`frontend/src/app/(dashboard)/engagement/_components/channel-console.tsx`）

| 检查点 | 位置 | 预期 |
|---|---|---|
| 双实例声明 | :107-118 | `accountLoadError` / `reportAccountLoadError` / `clearAccountLoadError` 与 `taskLoadError` / `reportTaskLoadError` / `clearTaskLoadError` 两套，注释说明竞态缘由 |
| 账号成功只清账号 | :163 `clearAccountLoadError()` | 任务侧成功**无法**触达账号错误位 |
| 账号失败上报 | :168 `reportAccountLoadError(err, "平台账号列表暂时无法读取")` | 仍保留降级 `setAccounts([])` + console.error |
| 账号 deps | :171 `[config.accountType, reportAccountLoadError, clearAccountLoadError]` | exhaustive-deps 无 warning |
| 任务成功只清任务 | :184 `clearTaskLoadError()` | 账号侧成功**无法**触达任务错误位 |
| 任务失败上报 | :189 `reportTaskLoadError(err, "任务列表暂时无法读取，请刷新重试")` | catch 兜底保留 |
| 任务 deps | :191 `[config.businessRoute, reportTaskLoadError, clearTaskLoadError]` | exhaustive-deps 无 warning |
| 移动分支双渲染 | :430-438 | 两个 mx-card danger 块：「平台账号加载失败：」/「任务列表加载失败：」 |
| 桌面分支双渲染 | :637-642 | 两个 `<LoadErrorBanner>`，重试各自直调 `void loadAccounts()` / `void refreshTasks()` |
| 操作 error 不受影响 | grep `setError(` | 建任务/确认/CRM 测试的操作错误仍走共享 error（R1 E6 结论不变） |

### R2 验证命令与结果

```bash
cd frontend && ./node_modules/.bin/tsc --noEmit        # 实测：exit 0
./node_modules/.bin/eslint "src/app/(dashboard)/engagement/_components/channel-console.tsx"  # 实测：exit 0
grep -n "loadError" "src/app/(dashboard)/engagement/_components/channel-console.tsx" | grep -v "accountLoadError\|taskLoadError\|useLoadError"
# 实测：仅命中 :107-109 的解释性注释行，无残留裸 loadError 引用
```

### R2 遗留交底

1. 分支级负向测试（mock 单路 reject 断言对应 banner）仍未建——R1 §五-2 的制度缺口原样挂账。
2. 若两路**同时**失败，页面会出两个错误块（移动端两张卡、桌面端两条 banner）——这是拆分后的预期行为，未做去重合并；Codex 若认为需合并展示请列 P2 建议。
